atlasAdd({
  id: "embedded",
  topics: [
    {
      id: "emb-rtos-own",
      title: "Write your own RTOS & real-time analysis",
      level: "Advanced",
      body: [
        "Context switching on Cortex-M (the **PendSV** handler, saving r4-r11, PSP/MSP), building a scheduler (round-robin → priority-preemptive → tickless), sync primitives from scratch with LDREX/STREX, the **MPU** for catching stack overflows in hardware, and SVC for privilege separation. Plus real-time analysis: **rate-monotonic scheduling** (the 69% bound), EDF, and worst-case execution time.",
        "Writing a context switch by hand is the moment 'how does multitasking even work' becomes obvious.",
      ],
      subtopics: [
        {
          title: "Cortex-M processor modes - Thread/Handler, MSP/PSP, and privilege levels",
          body: [
            "Before writing a context switch, you must understand the Cortex-M execution model precisely. The CPU has two **operating modes** and two **privilege levels**, independently selectable in Thread mode. This four-way space is where an RTOS's security and reliability model lives.",
            "**Thread mode** is where application code and RTOS tasks run. **Handler mode** is where all exception handlers (ISRs, SysTick, PendSV, SVC, faults) run. Handler mode is always **privileged** and always uses **MSP (Main Stack Pointer)**. Thread mode can be either privileged or unprivileged, and can use either MSP or PSP (Process Stack Pointer) — controlled by the `CONTROL` register bits `nPRIV` (0=privileged, 1=unprivileged) and `SPSEL` (0=MSP, 1=PSP).",
            "An RTOS exploits this model as follows: at startup, execution is in privileged Thread mode using MSP. The kernel code transitions to using PSP for tasks: on the first context switch, the kernel saves a fake initial frame on each task's stack (as if the task had been interrupted), sets PSP to the task stack pointer, sets CONTROL.SPSEL=1 (use PSP), clears CONTROL.nPRIV (privilege can be revoked per task), and triggers the first context switch. From then on: tasks run with PSP; the kernel runs in Handler mode with MSP. The two stacks are physically separate — a task stack overflow cannot corrupt the kernel stack.",
            "**Privilege level** enables the MPU to enforce memory access rules: an unprivileged task can be restricted from accessing peripheral registers, modifying the NVIC, or reading other tasks' memory. The MPU configuration is done by the kernel in privileged Handler mode; tasks cannot change it. An unprivileged task attempting a privileged memory access triggers MemManageFault, caught by the kernel. This is the embedded equivalent of user-space vs kernel-space separation on a full OS — the same conceptual divide, implemented in hardware.",
            "**SVC (Supervisor Call)** is the mechanism for unprivileged code to request privileged operations from the kernel. `SVC #N` triggers an SVC exception with the SVC number N encoded in the instruction. The SVC handler (which runs privileged in Handler mode) reads N from the stacked PC to identify which service is being requested, validates parameters, and performs the operation. In an RTOS, this is how user tasks acquire mutexes, create tasks, or access protected hardware without being privileged themselves. In a trusted-execution-environment design, SVC is the boundary between the normal world and the secure world (TrustZone on Cortex-M33).",
          ],
          resources: [
            { label: "ARM — Cortex-M processor modes and privilege (TRM)", url: "https://developer.arm.com/documentation/dui0552/a/the-cortex-m3-processor/programmers-model/processor-mode-and-privilege-levels-for-software-execution", type: "docs" },
            { label: "Miro Samek — Modern Embedded Systems (YouTube, context switch)", url: "https://www.youtube.com/playlist?list=PLPW8O6W-1chwyTzI3BHwBLbGQoPFxPAPM", type: "video" },
          ],
        },
        {
          title: "The PendSV context switch - saving registers, PSP manipulation, and lazy FPU stacking",
          body: [
            "The context switch is the heart of any RTOS. On Cortex-M, it is implemented in the **PendSV exception handler**. PendSV is set to the lowest possible priority so it only runs when no other exception is active — the context switch is deferred until all ISRs complete. The scheduler triggers a context switch by setting the PendSV pending bit: `SCB->ICSR = SCB_ICSR_PENDSVSET_Msk;`.",
            "When PendSV fires, the CPU has already automatically stacked the **exception frame** (xPSR, PC, LR, R12, R3, R2, R1, R0) onto the *interrupted task's PSP*. The handler must additionally save R4-R11 — the **callee-saved registers** that the CPU does not push automatically. The handler then saves the updated PSP (now pointing to the bottom of the saved context) into the outgoing task's TCB, calls the scheduler to find the next task, loads the new task's PSP from its TCB, pops R4-R11, and returns via EXC_RETURN — the CPU auto-pops the exception frame, resuming the new task.",
            "In ARM Thumb-2 assembly: `MRS R0, PSP` (read outgoing PSP), `STMDB R0!, {R4-R11}` (push R4-R11 onto the task's stack, pre-decrement: store multiple, decrement before), `LDR R1, =pxCurrentTCB` (load TCB pointer pointer), `LDR R1, [R1]` (dereference to current TCB), `STR R0, [R1]` (save updated PSP into TCB). Call scheduler (`BL vTaskSwitchContext`). Then reverse: `LDR R1, =pxCurrentTCB`, `LDR R1, [R1]`, `LDR R0, [R1]` (new task's PSP), `LDMIA R0!, {R4-R11}` (pop R4-R11, post-increment), `MSR PSP, R0`, `BX LR` (EXC_RETURN). The entire handler is ~20 instructions.",
            "**Lazy FPU stacking** is the performance optimization for FPU-equipped M4F/M7. Saving the 17 FPU registers (S0-S15 plus FPSCR) adds significant overhead to every context switch, even for tasks that never use the FPU. Lazy stacking defers FPU state save: on exception entry, the CPU reserves space on the stack for the FPU frame but does not actually write the registers. A 'FPU active' flag records the state. Only if the handler tries to use an FPU instruction does the CPU fault, save the FPU state, and continue — an exception within an exception. Tasks that never use the FPU pay zero overhead; tasks that do pay exactly once per context switch when needed. Enable via `FPU->FPCCR |= FPU_FPCCR_ASPEN_Msk | FPU_FPCCR_LSPEN_Msk;`.",
            "**Initial task stack frame**: when you create a task in your RTOS, you initialize its stack as if it had already been interrupted mid-execution. Push a fake exception frame: PC = task entry function, xPSR = 0x01000000 (Thumb bit set, no exception), R0 = parameter, LR = task exit handler address (a function that calls `vTaskDelete` if a task returns), and fake zeros for R1-R3, R12. Push a fake R4-R11 = all zeros. Set the TCB's PSP to the bottom of this frame. When the scheduler first runs this task, it pops R4-R11 and EXC_RETURN causes the CPU to pop the exception frame, jumping to the task function with R0 containing the parameter. The entire illusion of 'this task was already running and got interrupted' is constructed by carefully arranging the initial stack bytes.",
          ],
          resources: [
            { label: "FreeRTOS — source code (port/GCC/ARM_CM4F/port.c for reference)", url: "https://github.com/FreeRTOS/FreeRTOS-Kernel/blob/main/portable/GCC/ARM_CM4F/port.c", type: "repo" },
            { label: "ARM — lazy FPU context switching (technical note)", url: "https://developer.arm.com/documentation/dai0298/latest/", type: "docs" },
          ],
        },
        {
          title: "Scheduler implementation - ready lists, priority preemption, and round-robin",
          body: [
            "The scheduler is the decision function: given the set of ready tasks, which one runs next? Different scheduling algorithms answer this differently and make different guarantees about deadline satisfaction.",
            "**Round-robin (cooperative)** is the simplest: tasks run in order, each executing until it voluntarily yields (`taskYield()` or a blocking call). No preemption. Easy to implement (a single linked list), no race conditions, but one badly-behaved task can monopolize the CPU indefinitely. Suitable for trivial systems with few tasks and known-bounded execution.",
            "**Fixed-priority preemptive** (the FreeRTOS model): each task has a static priority. The scheduler always runs the highest-priority ready task. Lower-priority tasks are preempted when a higher-priority task becomes ready (via ISR→semaphore give, timer expiry, or another task becoming ready after a block). Implementation: a **priority-indexed ready list** — an array of N linked lists, one per priority. `scheduler()` scans from highest to lowest priority, returns the first non-empty list's head. `O(1)` if using a priority bitmask (find highest set bit with `__CLZ` — count leading zeros). Preemption is triggered by setting PendSV pending from SysTick or `portYIELD_FROM_ISR`.",
            "The **FreeRTOS ready list** uses a `List_t` structure (a doubly linked list with a count and an index) per priority, and the `pxReadyTasksLists[configMAX_PRIORITIES]` array. `vTaskSwitchContext()` finds the highest-priority non-empty list and sets `pxCurrentTCB` to its head item's owner. Equal-priority tasks share a list; the index rotates on each tick for round-robin among equals. Adding to the list is `O(1)` (list_insert_end); selecting is `O(1)` with the bitmask trick. Total context-switch decision time: ~30 instructions.",
            "**Tickless scheduler**: in standard mode, the SysTick fires every tick (1 ms) regardless of whether anything useful happens. During sleep, this wakes the CPU 1000 times/sec. Tickless idle suppresses SysTick when the only ready task is idle, computes the next scheduled wakeup time, programs a backup wakeup (RTC alarm or LPTIM), and executes `__WFI()`. On wakeup, it reconstructs the elapsed time from the hardware counter, corrects the tick count, and the normal scheduler resumes. The key invariant: the tick count must be accurate even across sleep — the wakeup timer provides the elapsed count so no ticks are missed.",
          ],
          resources: [
            { label: "FreeRTOS source — vTaskSwitchContext and ready list (tasks.c)", url: "https://github.com/FreeRTOS/FreeRTOS-Kernel/blob/main/tasks.c", type: "repo" },
            { label: "Miro Samek — writing a preemptive RTOS (step-by-step series)", url: "https://www.youtube.com/playlist?list=PLPW8O6W-1chwyTzI3BHwBLbGQoPFxPAPM", type: "video" },
          ],
        },
        {
          title: "Sync primitives from scratch - LDREX/STREX and lock-free data structures",
          body: [
            "Building a mutex or semaphore requires an atomic compare-and-swap primitive — an operation that reads a value and conditionally writes a new value with no window for another thread to interpose between the read and write. On Cortex-M, this is provided by **LDREX (Load-Exclusive)** and **STREX (Store-Exclusive)**.",
            "`LDREX R0, [R1]` loads the word at address [R1] into R0 and marks the address as exclusively reserved by this core. `STREX R2, R0, [R1]` attempts to store R0 to [R1]; if the exclusive reservation is still valid (no other agent has written to that address since LDREX), it stores and sets R2=0 (success); otherwise it sets R2=1 (failure) and does not store. This is the ARM implementation of Load-Linked/Store-Conditional (LL/SC) — the alternative to x86's LOCK prefix CMPXCHG.",
            "Building a **spinlock** with LDREX/STREX: `do { __LDREXW(&lock); } while (__STREXW(1, &lock) != 0 || lock_was_taken);` — more precisely: loop reading the lock value; if 0, try exclusive-store of 1; if the store fails (STREX returns 1), retry the LDREX. If the store succeeds and the previous value was 0, you have acquired the lock. Release: `__STREXW(0, &lock)` or just a regular store (no need for exclusive-store on release). Add a `__DMB()` (Data Memory Barrier) after acquire and before release to ensure memory ordering.",
            "A **lock-free ring buffer** for single-producer single-consumer (the common ISR→task case) requires no hardware atomics at all — just `volatile` and a memory barrier. The producer writes data, then writes the head index; the consumer reads the head index, then reads the data. As long as the write to head happens after the data write (enforced by `__DMB()`), and the read of data happens after the read of head (also enforced by `__DMB()`), the ordering is safe. No LDREX/STREX needed. This is the provably correct lock-free SPSC queue pattern, used in the Linux kernel's kfifo.",
            "**Priority inheritance** in a hand-rolled mutex: when task T_high tries to acquire a mutex held by T_low, record `mutex->owner = T_low` and `mutex->waiting_priority = T_high->priority`. In the scheduler, when selecting the next task, if a task holds a mutex that a higher-priority task is waiting on, temporarily promote that task's effective priority to the highest waiting priority. On mutex release, restore the holder's original priority. This requires the scheduler to walk mutex ownership chains — a non-trivial addition that is why simple RTOS implementations often start with priority ceiling instead (statically declare the mutex's ceiling priority, and any task holding it runs at the ceiling, simpler but less flexible).",
          ],
          resources: [
            { label: "ARM — exclusive access (LDREX/STREX) architecture reference", url: "https://developer.arm.com/documentation/dui0552/a/the-cortex-m3-instruction-set/memory-access-instructions/ldrd-and-strd", type: "docs" },
            { label: "Preshing on Programming — lock-free queue (SPSC)", url: "https://preshing.com/20120913/acquire-and-release-semantics/", type: "docs" },
          ],
        },
        {
          title: "The MPU - region descriptors, access permissions, and stack guard configuration",
          body: [
            "The **MPU (Memory Protection Unit)** is the hardware that enforces memory access rules on Cortex-M. It divides the address space into up to 8 or 16 regions (M3/M4: 8 regions; M33: 16 regions), each independently configurable with access permissions for privileged and unprivileged code, cacheability, bufferability, and execute-never (XN). Any access that violates the configured rules triggers a **MemManageFault** exception.",
            "**Region descriptors**: each region is specified by base address (must be aligned to a multiple of the region size), size (power of 2, minimum 32 bytes), and attributes (RASR: Region Attribute and Size Register). The attribute field encodes: S (shareable — coherency for multi-core, set for DMA-accessible regions), TEX/C/B (type extension/cacheable/bufferable — defines caching behavior), AP (access permission field — 3 bits specifying privileged/unprivileged read/write access), XN (execute-never — prevents code execution from this region). Region 0 has the lowest priority; region 7 (or 15) has the highest — overlapping regions are resolved by the highest-numbered region's attributes.",
            "**Stack guard configuration**: allocate a 32-byte region at the base of each task's stack (the lowest address, where overflow first strikes). Configure it as no-access for all privilege levels (AP=0b000) and XN=1. When any task overflows its stack and writes to this guard region, MemManageFault fires immediately, providing the exact stacking address and program counter of the overflowing instruction. This is the production-quality stack overflow detection mechanism — zero runtime overhead, zero false negatives for stack overflows that reach the guard region.",
            "**Per-task MPU configuration**: a more advanced RTOS configures different MPU regions for each task, changed on every context switch (write the 8 region descriptors in the PendSV handler before restoring the task's registers). This gives each task a unique memory view: task A can access peripheral A's registers but not peripheral B's; task B vice versa. Combined with unprivileged Thread mode, this creates genuine hardware-enforced task isolation — a buffer overrun in task A cannot corrupt task B's memory or peripheral state. This is the foundation of Class-B and Class-C safety-critical embedded designs.",
            "A **background region** (region 0 covering the entire 4GB address space) configured as privileged-only read/execute with XN for unprivileged code provides the base policy: unprivileged tasks cannot access anything not explicitly permitted by higher-priority regions. Then specific regions are carved out: task stack (read/write, no execute), code section (execute, no write), peripheral registers (read/write, no execute, non-cacheable), shared data section (read-only, no execute). The result is a minimal-privilege architecture where bugs that exploit pointer corruption are contained by hardware within the offending task's sandbox.",
          ],
          resources: [
            { label: "ARM — MPU usage guide for Cortex-M (application note DAN0042)", url: "https://developer.arm.com/documentation/ddi0337/e/ch06s06-mpu", type: "docs" },
            { label: "ST — MPU configuration on STM32 (AN4838)", url: "https://www.st.com/resource/en/application_note/an4838-managing-memory-protection-unit-in-stm32-mcus-stmicroelectronics.pdf", type: "docs" },
          ],
        },
        {
          title: "Real-time analysis - RMS, EDF, utilization bound, and WCET",
          body: [
            "Designing an RTOS is only half the problem. The other half is proving that your set of periodic tasks will always meet their deadlines. **Real-time scheduling theory** provides formal tools for this analysis, and the results are both practically important and mathematically elegant.",
            "**Rate-Monotonic Scheduling (RMS)** is the seminal result (Liu & Layland, 1973): assign static priorities by period — the shortest-period task gets the highest priority, the longest-period gets the lowest. Under this assignment, the set of periodic tasks is schedulable (all deadlines met) if and only if the total **CPU utilization** $U = \\sum_{i} C_i / T_i \\le n(2^{1/n} - 1)$ where $C_i$ is the worst-case execution time of task $i$, $T_i$ its period, and $n$ is the number of tasks. As $n \\to \\infty$, this bound approaches $\\ln 2 \\approx 0.693$ — the famous **69% utilization bound**. Any task set with U < 0.693 is guaranteed schedulable under RMS regardless of n. A task set with U ≤ 1 may or may not be schedulable — requires exact analysis (response-time analysis).",
            "**EDF (Earliest Deadline First)** dynamically assigns priorities at runtime: the task whose absolute deadline is earliest runs first. EDF is **optimal** — if any scheduling algorithm can meet all deadlines, EDF can. It can utilize 100% of the CPU (U ≤ 1 is both necessary and sufficient for schedulability under EDF). The practical cost is dynamic priority computation (heavier scheduler, requires knowing absolute deadlines at runtime) and poor behavior under overload: when U briefly > 1, EDF causes all tasks to miss deadlines randomly (vs RMS where only low-priority tasks miss, and high-priority ones continue to meet deadlines — a more controlled degradation).",
            "**Worst-Case Execution Time (WCET)** is the maximum time a task can take from activation to completion, under any input and any processor state. Determining WCET precisely is hard: caches, pipelines, and branch predictors make execution time input-dependent. Measurement-based approaches: run the task under many input scenarios, take the maximum observed time, and add a safety margin. Static WCET analysis tools (aiT from AbsInt, RapiTime from Rapita) model the processor's pipeline and cache to compute a safe upper bound analytically — required for safety-critical certification (DO-178C, ISO 26262). For DIY analysis: instrument the task with cycle-count registers (`DWT->CYCCNT` on Cortex-M), disable caches and interrupts during measurement, and measure across representative inputs.",
            "**Response-time analysis (RTA)** is the exact schedulability test for fixed-priority preemptive scheduling (more precise than the RMS utilization bound). The response time $R_i$ of task $i$ satisfies the fixed-point equation: $R_i = C_i + \\sum_{j \\in hp(i)} \\lceil R_i / T_j \\rceil C_j$ where $hp(i)$ is the set of tasks with higher priority than $i$. Solve by iteration: start with $R_i^0 = C_i$, compute $R_i^{k+1}$ from $R_i^k$ until convergence. If $R_i \\le D_i$ (deadline) for all tasks, the system is schedulable. This is the standard tool used in industry-standard real-time analysis and is more accurate than the utilization bound — it can certify task sets with U > 0.693 if the task structure allows it.",
          ],
          resources: [
            { label: "Liu & Layland 1973 — Scheduling Algorithms for Multiprogramming in Hard-Real-Time Environments (original paper)", url: "https://dl.acm.org/doi/10.1145/321738.321743", type: "paper" },
            { label: "Hard Real-Time Computing Systems — Buttazzo (textbook)", url: "https://link.springer.com/book/10.1007/978-1-4614-0676-1", type: "book" },
          ],
        },
      ],
      resources: [
        { label: "Write an RTOS — Miro Samek (Modern Embedded Systems)", url: "https://www.youtube.com/playlist?list=PLPW8O6W-1chwyTzI3BHwBLbGQoPFxPAPM", type: "video" },
        { label: "The Definitive Guide to ARM Cortex-M (PendSV/context switch)", url: "https://developer.arm.com/documentation/dui0552/latest/", type: "docs" },
      ],
      connections: [
        { to: "os-kernel", note: "A context switch + scheduler is a microkernel — the OS material in miniature" },
        { to: "os-concurrency", note: "LDREX/STREX is load-linked/store-conditional — the CAS primitive in hardware" },
        { to: "emb-safety", note: "Real-time guarantees feed directly into safety-critical certification" },
      ],
    },
  ],
});

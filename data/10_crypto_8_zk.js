atlasAdd({
  id: "crypto",
  topics: [
    {
      id: "crypto-zk",
      title: "Zero-knowledge proofs",
      level: "Advanced → Research",
      body: [
        "Interactive proofs (completeness/soundness/zero-knowledge), **Sigma protocols** (Schnorr), commitments (Pedersen), the **Fiat-Shamir** transform (interactive→non-interactive), **zkSNARKs** (arithmetic circuits → R1CS → QAP → Groth16/PLONK), **zkSTARKs** (transparent, hash-based, quantum-resistant), and proof recursion (Nova folding).",
        "ZK is the hottest area at the crypto↔web3 boundary — write a circuit in circom/halo2 to make it real.",
      ],
      subtopics: [
        {
          title: "Interactive proofs — completeness, soundness, and zero-knowledge",
          body: [
            "A **zero-knowledge proof (ZKP)** is a protocol between a **prover** P and a **verifier** V in which P convinces V that a statement is true without revealing anything beyond the truth of the statement. The three core properties: **Completeness** — if the statement is true and P is honest, V accepts (an honest prover can always convince an honest verifier). **Soundness** — if the statement is false, no (possibly cheating) prover can convince V except with negligible probability (the soundness error). **Zero-knowledge** — V learns nothing except that the statement is true; whatever V could compute after the interaction, it could compute just as well without the interaction (formalized via the existence of a **simulator** that produces indistinguishable transcripts without knowing the witness).",
            "The canonical introductory example: the **Ali Baba cave** (or more precisely, the **3-colorability protocol**). Suppose P wants to prove they know a valid 3-coloring of a graph G without revealing it. Repeat many times: P randomly permutes the colors and publishes a commitment to each vertex's new color. V challenges with a random edge (u, v). P reveals the colors of u and v only. V checks they are different. If P does not know a 3-coloring, they can only cheat by guessing the challenged edge in advance — probability 1/|E|. After repeating log(1/ε)/log(|E|/(|E|-1)) rounds, the soundness error is at most ε. Zero-knowledge: V only sees two colors per round (and they are always different — uninformative). A simulator can generate valid transcripts by picking an edge first, then committing to an arbitrary valid 2-coloring of just those two vertices — no real 3-coloring needed. This transcript is indistinguishable from a real interaction.",
            "**Proofs of knowledge** vs. **membership proofs**: a proof of knowledge not only shows that the statement is true but also that P *knows* a witness — formalized via a **knowledge extractor** that, given rewind access to P, can extract the witness. This is the stronger property required for authentication (proving you know a private key, not just that a public key exists) and for zkSNARKs (the prover proves it *knows* a satisfying assignment to an arithmetic circuit, not just that one exists). The knowledge soundness property is sometimes called **argument of knowledge** or in the SNARK name: **Succinct Non-interactive ARgument of Knowledge**.",
            "The **GMR completeness theorem** (Goldwasser, Micali, Rackoff 1985) established the formalism: interactive proof systems IP = PSPACE (the class IP of problems decidable by interactive proofs equals PSPACE). This was a surprise: interactive proofs are much more powerful than classical proofs (NP). The subclass of interest for cryptography is **IP with zero-knowledge for NP**: every NP statement has a zero-knowledge proof (under standard cryptographic assumptions). This follows because graph 3-colorability is NP-complete and has a zero-knowledge proof, so any NP problem can be reduced to it. The practical implication: any statement whose truth can be verified efficiently (NP) can be proven with zero knowledge — including the statement 'I know a valid transaction that changes this blockchain state from A to B'.",
            "**Computational** vs. **statistical** vs. **perfect zero-knowledge**: perfect ZK requires the simulator's output distribution to be *identically* distributed to real transcripts — impossible in most natural settings. Statistical ZK relaxes this to negligible statistical distance between the distributions. Computational ZK (the most common practical setting) requires only that no polynomial-time adversary can distinguish — acceptable as long as we believe the computational assumptions hold. Most ZK constructions used in practice (zkSNARKs, STARKs) are computationally zero-knowledge, which is sufficient for all known applications.",
          ],
          resources: [
            { label: "ZK-Learning MOOC — lecture 1 (interactive proofs, sigma protocols)", url: "https://zk-learning.org/", type: "course" },
          ],
        },
        {
          title: "Sigma protocols and Schnorr's identification scheme",
          body: [
            "**Sigma protocols** (Σ-protocols) are a specific family of 3-move interactive proofs with a clean structure: (1) **Commitment**: P sends a commitment `a = g^r` for random `r`. (2) **Challenge**: V sends a random challenge `c`. (3) **Response**: P sends `z = r + c·x` (or similar linear combination involving the witness `x`). V checks a verification equation. The name comes from the shape of the transcript when drawn as a diagram — a Σ.",
            "**Schnorr's identification protocol** (1989) is the most important Σ-protocol. Prover knows discrete log `x` (private key) of public key `X = g^x` in a prime-order group. Protocol: P sends `a = g^r` for random `r`. V sends challenge `c ←_R Z_n`. P sends `z = r + cx mod n`. V checks `g^z = a · X^c`. Soundness: if a cheating prover can respond correctly to two different challenges `c, c'` for the same commitment `a`, then `g^z = a X^c` and `g^{z'} = a X^{c'}`, so `g^{z-z'} = X^{c-c'}`, giving `x = (z-z')/(c-c') mod n` — the witness is extractable. This is the knowledge soundness property via the 'special soundness' of Σ-protocols.",
            "**Special soundness** is the key structural property: given two accepting transcripts `(a, c, z)` and `(a, c', z')` with the same first message `a` but different challenges, the witness can be extracted. This gives a concrete **extractor** for the knowledge soundness proof. Zero-knowledge: the simulator chooses `c` first (randomly), then picks `z` randomly, and computes `a = g^z X^{-c}`. This is a valid accepting transcript because `g^z = a X^c` by construction, and `c` was chosen before `a`, so the order of the interaction was 'reversed' without knowing `x`. The distribution is identical to real transcripts (uniform `z` and `c`), establishing perfect zero-knowledge (in the honest-verifier ZK sense — V chooses challenges honestly).",
            "**Composition**: Σ-protocols compose naturally. **AND proofs**: to prove knowledge of *both* `x_1` (witness for `X_1`) and `x_2` (witness for `X_2`), run two Schnorr protocols in parallel, sharing a single challenge. **OR proofs**: to prove knowledge of *at least one* of `x_1, x_2` (without revealing which), use the 'simulation trick' — for the statement the prover does not know, run the simulator to produce a fake transcript, then adjust the real statement's response to make both consistent. This is the foundation of ring signatures (proving membership in a ring without revealing which member) and credential anonymization.",
            "**Fiat-Shamir heuristic** transforms a Σ-protocol into a **non-interactive proof** (NIZK) by replacing the verifier's random challenge with a hash of the commitment: `c = H(a ‖ context)`. The prover computes everything locally: `a = g^r`, `c = H(a)`, `z = r + cx`. The verifier recomputes `c = H(a)` and checks `g^z = a X^c`. Security (in the random oracle model) relies on the hash function being a random oracle — since the prover cannot predict `H(a)` before fixing `a`, the protocol remains sound. Fiat-Shamir is the transformation underlying essentially all practical non-interactive proofs: it converts Schnorr to a non-interactive signature scheme, converts complex Σ-protocols to NIZKs, and is the foundational transformation enabling zkSNARKs built on polynomial commitment schemes.",
          ],
          resources: [
            { label: "ZK-Learning MOOC — lecture 2 (Sigma protocols, Fiat-Shamir)", url: "https://zk-learning.org/", type: "course" },
          ],
        },
        {
          title: "Arithmetic circuits, R1CS, and the QAP reduction",
          body: [
            "zkSNARKs prove arbitrary computations efficiently by representing them as **arithmetic circuits** over a finite field. An arithmetic circuit is a directed acyclic graph (DAG) of addition gates (`a + b`) and multiplication gates (`a × b`) over `GF(p)`. Any computation that can be expressed as a sequence of field operations can be written as an arithmetic circuit. The key insight is that arithmetic circuits are a universal model of computation over finite fields — they can represent hash functions (using field-efficient hash designs like Poseidon), elliptic curve operations (using embedding techniques), and arbitrary program logic (using boolean-to-arithmetic conversions).",
            "**Rank-1 Constraint System (R1CS)**: an arithmetic circuit is first converted to R1CS, a set of constraints of the form `(a · w) × (b · w) = (c · w)` where `a, b, c ∈ F^n` are coefficient vectors and `w ∈ F^n` is the **witness vector** containing all input, output, and intermediate values. Each gate of the circuit becomes one R1CS constraint. For a circuit with `m` constraints and `n` wires (variables), R1CS has `m` equations in `n` unknowns. A valid witness `w` must satisfy all constraints simultaneously — this encodes the complete execution trace of the circuit. The public inputs (verifier's inputs) are fixed components of `w`; the private inputs (prover's secret) are the remaining components.",
            "**Quadratic Arithmetic Program (QAP)**: the R1CS representation has one constraint per gate, but checking `m` constraints independently is expensive. The QAP representation (Gennaro, Gentry, Parno, Raykova 2013) encodes all `m` constraints into *polynomials* using Lagrange interpolation. For each constraint index `i`, define a target polynomial `t(x) = ∏(x - r_i)` that vanishes at each constraint evaluation point `r_i`. Define polynomials `U_j(x), V_j(x), W_j(x)` by Lagrange-interpolating the coefficient vectors across constraint indices. Then all `m` constraints are simultaneously satisfied iff the polynomial `(Σ a_j U_j(x))(Σ a_j V_j(x)) - Σ a_j W_j(x) = h(x) · t(x)` for some polynomial `h(x)`. This reduces `m` individual constraint checks to a single polynomial divisibility check — the algebraic heart of Groth16 and PLONK.",
            "The evaluation is performed at a **secret point** (a 'structured reference string' or SRS, a trusted setup artifact): the prover evaluates the polynomial at a hidden field element `τ` (which was discarded after setup, or is held in encrypted form using a pairing-friendly curve). The prover uses the SRS — encrypted powers `g^τ, g^{τ^2}, ..., g^{τ^n}` of the group generator — to compute the proof without knowing `τ`. The verifier checks the proof using a pairing (a bilinear map on the elliptic curve). Security reduces to the **Knowledge-of-Exponent (KoE) assumption** (or **Bilinear Diffie-Hellman assumption**) on the pairing-friendly curve. The QAP approach's efficiency: proof size and verification time are constant (independent of circuit size), and proving time is O(n log n) via FFT-based polynomial operations. This is what 'succinct' means in SNARK: sub-linear verification.",
            "The **circuit complexity** of a computation determines the proving time. Not all computations are efficient as arithmetic circuits: hash functions like SHA-256 (bitwise operations over binary numbers) require O(n) multiplication gates per bit (using arithmetic gadgets), making them expensive. **ZK-friendly hash functions** (Poseidon, Rescue, Griffin) are designed with minimal multiplication depth and a small number of field multiplications, reducing circuit size by 50-100×. Choosing the right hash function for in-circuit use (e.g. for Merkle trees in ZK rollups) has enormous practical performance impact — this is why all ZK rollups use Poseidon internally even if they use SHA-256 for their external interface.",
          ],
          resources: [
            { label: "Vitalik — Quadratic Arithmetic Programs from Zero to Hero", url: "https://medium.com/@VitalikButerin/quadratic-arithmetic-programs-from-zero-to-hero-f6d558cea649", type: "docs" },
          ],
        },
        {
          title: "zkSNARKs — Groth16, PLONK, and the trusted setup",
          body: [
            "**Groth16** (Jens Groth, 2016) is the most efficient zkSNARK in terms of proof size and verification time, and is the most widely deployed in production (used by Zcash, Filecoin, Tornado Cash). A Groth16 proof consists of exactly **three elliptic curve points** (2 points in G1, 1 in G2) — 192 bytes for BN254 — regardless of circuit size. Verification requires exactly **3 pairings and ~8 G1 multiplications** — constant time, regardless of the circuit. These numbers are not improvable without changing the computational model. The tradeoff: Groth16 requires a **circuit-specific trusted setup** (a ceremony that generates the SRS for a specific circuit). If the setup's toxic waste (`τ`) is not discarded, any prover who learns `τ` can fabricate proofs. The Zcash 'Powers of Tau' ceremony (2018) and Hermez's ceremony involved hundreds of participants to minimize this trust.",
            "**PLONK** (Permutations over Lagrange-bases for Oecumenical Noninteractive arguments of Knowledge, Gabizon, Williamson, Ciobotaru, 2019) is the most important advance since Groth16. PLONK uses a **universal and updatable SRS**: the same setup (a single structured reference string) can be used for *all* circuits up to a given size, and new participants can update it incrementally without trusting any single participant. This 'powers of tau ceremony' needs to be run only once, ever, and any party that is honest in any round of the ceremony guarantees the setup's security. PLONK proofs are slightly larger than Groth16 (a few hundred bytes), but the elimination of circuit-specific setups is transformative — developers can write and deploy circuits without running a new ceremony.",
            "PLONK's core technical contribution is **custom gates**: instead of only addition and multiplication gates, PLONK allows arbitrary polynomial relations between gate inputs and outputs, specified by the circuit designer. This dramatically reduces the circuit size for common operations (range checks, elliptic curve additions, hash functions) by expressing them in a single 'custom gate' rather than many standard gates. **UltraPlonk** (used in Aztec, Noir) extends this with lookup tables (Plookup), enabling efficient implementation of operations like boolean logic, range checks, and SHA-256 via pre-computed table lookups — further reducing proving time.",
            "The **trusted setup ceremony** is the central trust assumption of Groth16 and setup-specific SNARKs. It generates public parameters `(g^τ, g^{τ^2}, ..., g^{τ^n})` while destroying the toxic waste `τ`. If `τ` leaks, an attacker can forge proofs: specifically, they can create a proof that verifies for any false statement. This is called a **backdoored setup**. Multi-party computation (MPC) ceremonies distribute the setup across many participants — if at least one destroys their contribution, `τ` is unrecoverable. The Perpetual Powers of Tau ceremony (ptau) has had thousands of contributors; Zcash's Sprout ceremony (2016) and Sapling ceremony (2018) were landmark events in distributed trust. **Transparent** SNARKs (STARKs, Halo2) avoid this entirely by using hash functions or polynomial commitments with no toxic waste.",
            "**Halo2** (Electric Coin Company/Zcash, 2020) is a practically important zkSNARK variant that achieves **recursive proof composition** without a trusted setup. Using the **inner product argument** (IPA, a polynomial commitment scheme based on Pedersen commitments), Halo2 allows proofs to verify other proofs efficiently — enabling 'proof aggregation' and 'proof recursion' at scale. The tradeoff vs. PLONK/Groth16: verification is slower (logarithmic in circuit size rather than constant), but the absence of a trusted setup and the native recursion support make Halo2 ideal for zkEVMs (like zkSync's and Scroll's implementations) where the proof of EVM execution must be recursively composed and aggregated.",
          ],
          resources: [
            { label: "PLONK paper — Gabizon, Williamson, Ciobotaru", url: "https://eprint.iacr.org/2019/953.pdf", type: "paper" },
            { label: "ZK-Learning MOOC — lecture 5 (PLONK and IOP-based SNARKs)", url: "https://zk-learning.org/", type: "course" },
          ],
        },
        {
          title: "zkSTARKs — transparent, hash-based, and quantum-resistant proofs",
          body: [
            "**zkSTARKs (Scalable Transparent ARguments of Knowledge)**, introduced by Ben-Sasson, Bentov, Horesh, and Riabzev (2018), are the major alternative to SNARKs. STARKs achieve three properties that SNARKs traditionally lack: **transparency** (no trusted setup — all public parameters are hash function outputs, derivable by anyone), **post-quantum security** (security relies only on collision-resistant hash functions, not on discrete logarithms or pairing assumptions that Shor's algorithm breaks), and **scalability** (proving time scales as O(n log^2 n) and verification is O(log^2 n) — polylogarithmic, better than linear but worse than SNARKs' O(1) verification). The tradeoff: STARK proofs are much larger (10-100 KB vs 192 bytes for Groth16) due to the hash-based authentication.",
            "**Algebraic Intermediate Representation (AIR)** is STARKs' circuit analog. AIR expresses computations as a **trace** — a table where rows are successive states of the computation and columns are registers. Constraints are polynomial relations between the current row and the next row (transition constraints) and between specific cells (boundary constraints). For example, the Fibonacci recurrence `s_{i+1} = s_i + s_{i-1}` is a single constraint `s_{next} = s_{curr} + s_{prev}`. This register-based representation naturally matches the structure of virtual machines and CPUs — an EVM execution trace maps directly to an AIR with ~100 columns (one per stack slot) and one row per EVM step. This is why zkEVM provers (StarkWare, Risc Zero) use STARKs.",
            "**FRI (Fast Reed-Solomon IOP of Proximity)** is the polynomial commitment scheme at the heart of STARKs — the key technical innovation that makes verification efficient. FRI is an **Interactive Oracle Proof (IOP)** that proves a committed polynomial has low degree: given an oracle to evaluate a function at chosen points, FRI allows a verifier to check that the function is a polynomial of degree at most D in O(log^2 D) queries (via a logarithmic 'folding' process, similar to FFT). Combined with Merkle trees to commit to the function evaluations (providing a succinct hash-based oracle), FRI produces a non-interactive proof via Fiat-Shamir. The proof that the execution trace's constraint polynomial is correct reduces to a FRI proof that the quotient polynomial has the right degree.",
            "**Proofs of Computational Integrity**: STARKs are the technology behind **StarkWare's** production ZK rollups (StarkEx, Starknet) and **Risc Zero's** general-purpose zkVM (which proves execution of any RISC-V program). StarkEx processes billions of dollars in trading volume daily by generating proofs that the off-chain orderbook state transitions are valid, verified on-chain by an Ethereum smart contract that runs the STARK verifier. The verification cost on Ethereum is ~300k gas per proof, fixed regardless of how many transactions are batched — the amortized cost per transaction decreases with batch size, enabling 1000× throughput improvement over naive on-chain execution.",
            "**STARKs vs. SNARKs** in practice: for applications that need small proof sizes and fast on-chain verification (e.g. Ethereum L1 verifiers where gas cost is critical), SNARKs (Groth16: 192 bytes, 300k gas) win. For applications that prioritize post-quantum security, transparency (no trusted setup), or prover efficiency on large executions (e.g. proving an entire block of EVM transactions), STARKs (larger proofs, log-time verification) are preferable. Hybrid approaches exist: generate a STARK proof for the bulk of the computation (cheap prover, efficient for large AIR), then wrap it in a SNARK (recursion via aggregation) for on-chain verification. This STARK-to-SNARK 'compression' combines the best of both and is used by StarkWare for their on-chain verifier and by Polygon's Plonky2/FFLONK.",
          ],
          resources: [
            { label: "STARK paper — Ben-Sasson et al. (2018)", url: "https://eprint.iacr.org/2018/046.pdf", type: "paper" },
            { label: "Vitalik — STARKs Part I: Proofs with Polynomials", url: "https://vitalik.eth.limo/general/2017/11/09/starks_part_1.html", type: "docs" },
          ],
        },
        {
          title: "Proof recursion, folding, and Nova",
          body: [
            "**Proof recursion** is the technique of having a ZK proof verify another ZK proof — enabling a prover to aggregate many proofs into one, or to incrementally extend a proof as computation grows. The concept was first made efficient by Halo (2019, Bowe, Grigg, Hopwood), which showed how to bootstrap recursive proofs using an accumulation scheme (IPA) without requiring pairing-friendly curves or trusted setups. Recursion enables: (1) **aggregation** (combine 1000 SNARK proofs into one, reducing verification to a single proof); (2) **incremental verifiable computation (IVC)** (extend the proof step-by-step as a long computation unfolds, without re-proving from scratch); (3) **parallel proving** (prove independent subcomputations independently, then merge the proofs).",
            "**IVC (Incrementally Verifiable Computation)** formalizes the idea: an IVC scheme allows a prover to compute `F^n(z_0)` (n iterations of function F starting from `z_0`) and produce a proof that grows only O(log n) in size (or even remains constant). At each step i, the prover produces `z_{i+1} = F(z_i)` and updates the proof to cover all n steps so far. The verifier can check the final proof in constant time regardless of n. This is the model underlying **zkVMs** (zero-knowledge virtual machines): the VM executes program steps one at a time, each step extends the IVC proof, and the final proof proves the entire program's execution. Risc Zero's zkVM and SP1 (Succinct) use variants of this architecture.",
            "**Nova** (Kothapalli, Setty, Tzialla, 2022) is the most important recent advance in recursive proof composition. Nova uses a technique called **folding**: instead of proving that a zkSNARK proof is valid (which requires an expensive circuit for the SNARK verifier), Nova 'folds' two instances of an NP relation into one, reducing two proof obligations to one cheaper one. The folding step itself is extremely cheap (a few hash operations and a random linear combination) and does not require an expensive circuit. After n steps, the accumulated 'folded instance' is verified with a single SNARK. Nova's prover is constant-time per step (does not grow with the number of steps), making it ideal for incremental computation.",
            "**SuperNova** and **HyperNova** extend Nova to handle *different functions* at each step (not just repeated F), enabling VMs where each instruction is a different function, and more efficient constraint systems. **Sangria** applies folding to PLONK circuits. These are very active 2022-2024 research directions, and the design space is rapidly evolving. The practical consequence for the web3 ecosystem: folding schemes are making zkEVM provers fast enough for real-time block proving — a requirement for decentralized L2 sequencers that generate proofs immediately after producing blocks (latency < 10 seconds per block).",
            "**Accumulation and aggregation in practice**: Ethereum's roadmap ('The Surge') includes a vision of a ZK proof of the entire Ethereum execution layer — every block proved by a zkEVM, verified on chain with a single constant-size proof. This requires: (1) a circuit for the EVM (hundreds of millions of gates); (2) parallel proving infrastructure; (3) recursive aggregation of parallel proofs into one; (4) a final SNARK for on-chain verification. The entire stack — AIR/R1CS for EVM, STARKs for proving, folding for aggregation, a final PLONK/Groth16 for the verifier — is the end-to-end ZK stack for Ethereum's future. Each of the subtopics in this section is one layer of that stack.",
          ],
          resources: [
            { label: "Nova: Recursive Zero-Knowledge Arguments from Folding Schemes (paper)", url: "https://eprint.iacr.org/2021/370.pdf", type: "paper" },
            { label: "Moon Math Manual (free, mathematical foundations of ZK proofs)", url: "https://github.com/LeastAuthority/moonmath-manual", type: "book" },
          ],
        },
      ],
      resources: [
        { label: "ZK-Learning — MOOC (Stanford/Berkeley/CMU, free)", url: "https://zk-learning.org/", type: "course" },
        { label: "Vitalik — zkSNARKs explainer series", url: "https://vitalik.eth.limo/general/2021/01/26/snarks.html", type: "docs" },
        { label: "Moon Math Manual (free, math for ZK)", url: "https://github.com/LeastAuthority/moonmath-manual", type: "book" },
      ],
      connections: [
        { to: "web3-zk", note: "On-chain verifiers, zkrollups and Circom circuits apply ZK to blockchains" },
        { to: "comp-llvm", note: "Circom/Noir compile high-level circuits to constraint systems like compilers" },
        { to: "dsa-paradigms", note: "QAP/FFT-based proving leans on polynomial algorithms from DSA" },
      ],
    },
  ],
});

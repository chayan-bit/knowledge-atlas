atlasAdd({
  id: "crypto",
  topics: [
    {
      id: "crypto-rsa",
      title: "RSA",
      level: "Intermediate",
      body: [
        "Key generation (`n=pq`, `φ(n)`, `d=e⁻¹ mod φ(n)`), why it works (Euler's theorem), the **attack catalog** (small-`e` cube root, unpadded homomorphism, common modulus, **Bleichenbacher's** PKCS#1v1.5 padding oracle), correct usage (**OAEP** for encryption, **PSS** for signatures), and timing side-channels + blinding.",
        "RSA is the cleanest case study in 'the textbook scheme is broken; padding is the real cryptosystem.'",
      ],
      subtopics: [
        {
          title: "Key generation and the correctness proof",
          body: [
            "RSA key generation: choose two large random primes `p` and `q` (each ~1024 bits for RSA-2048), compute the modulus `n = pq` and `φ(n) = (p-1)(q-1)`. Choose a public exponent `e` with `gcd(e, φ(n)) = 1` — in practice `e = 65537` (= 2^{16} + 1, a Fermat prime) is universal: it is large enough to avoid small-exponent attacks, small enough to make encryption fast, and its binary representation (100000000000000001) makes square-and-multiply exponentiation efficient with only 17 multiplications. Compute the private exponent `d = e^{-1} mod φ(n)` via the extended Euclidean algorithm. Publish `(n, e)` as the public key; keep `(p, q, d)` private.",
            "**Correctness**: for any `m ∈ Z_n*`, `(m^e)^d = m^{ed} (mod n)`. Since `ed ≡ 1 (mod φ(n))`, we have `ed = kφ(n) + 1` for some integer k. By Euler's theorem, `m^{φ(n)} ≡ 1 (mod n)` for `gcd(m, n) = 1`, so `m^{ed} = m^{kφ(n)+1} = (m^{φ(n)})^k · m ≡ 1^k · m ≡ m (mod n)`. This holds for all `m` coprime to n; since p and q are large primes, the probability that a random m shares a factor with n is negligible. The correctness is a direct corollary of Euler's theorem applied to the group `Z_n*`.",
            "The security assumption is **integer factorization hardness**: given `n = pq`, finding `p` and `q` is believed computationally infeasible for large `n`. Note this is an *assumption*, not a proven theorem — P ≠ NP is not even proved, and factorization is not known to be NP-hard. The best classical factorization algorithm, the **General Number Field Sieve (GNFS)**, runs in sub-exponential time `L_n[1/3, c]` — for RSA-2048, this is estimated at 2^{112} operations with current algorithms and hardware, giving a comfortable margin. NIST recommends RSA-3072 for post-2030 security at the 128-bit level, and RSA-2048 for legacy interoperability until 2030.",
            "Even knowing φ(n) is as hard as factoring: given both `n = pq` and `φ(n) = (p-1)(q-1)`, you can compute `p + q = n - φ(n) + 1` and `p - q = √{(p+q)^2 - 4n}`, recovering `p` and `q` immediately. This is why the private key is `(p, q, d)` or equivalently `d` (which itself leaks φ(n) via EEA on e and d). Similarly, finding any `d' = e^{-1} mod kφ(n)` for small k leaks φ(n). The factorization and discrete-log assumptions on φ(n) are thus equivalent to RSA security.",
            "The prime-finding step uses **probabilistic primality tests** (Miller-Rabin). For a 1024-bit candidate, the density of primes is ~1/710, so on average 355 candidates are tested. Each Miller-Rabin test with 40 witnesses gives false-positive probability < 4^{-40} ≈ 10^{-24}, effectively zero. OpenSSL generates the two primes independently, then verifies that `gcd(e, p-1) = gcd(e, q-1) = 1` before accepting them. One important additional requirement: `|p - q|` must not be too small — if `p ≈ q`, then `n ≈ p^2` and Fermat factorization (trying `x = ⌈√n⌉, x+1, ...` until `x^2 - n` is a perfect square) is fast. FIPS 186-5 requires `|p - q| > 2^{n/2 - 100}` for RSA-n.",
          ],
          resources: [
            { label: "NIST FIPS 186-5 — Digital Signature Standard (RSA key generation requirements)", url: "https://csrc.nist.gov/publications/detail/fips/186/5/final", type: "docs" },
          ],
        },
        {
          title: "The RSA attack catalog — textbook failures",
          body: [
            "**Textbook RSA** (direct exponentiation without padding) is broken by several attacks that make it completely unusable. Understanding each attack is valuable not just for RSA but as a template for how mathematical structure in a cryptosystem is always exploitable. The first lesson: a scheme that is 'technically correct' (encrypts and decrypts) is not automatically secure.",
            "**Small public exponent attacks**: if `e = 3` (an older common choice) and the same message `m` is encrypted under three different RSA keys `(n_1, 3), (n_2, 3), (n_3, 3)`, an attacker observes `c_1 = m^3 mod n_1`, `c_2 = m^3 mod n_2`, `c_3 = m^3 mod n_3`. By CRT, there is a unique `c* mod (n_1 n_2 n_3)` satisfying all three congruences, and if `m^3 < n_1 n_2 n_3` (which holds when `m` is small relative to the moduli), then `c* = m^3` exactly as an ordinary integer, and the attacker recovers `m = ⌊c*^{1/3}⌋` by ordinary integer cube root. This is the **Håstad broadcast attack**. The fix is to use a large, unpredictable e (like 65537), or more fundamentally, always pad messages before encryption.",
            "**Unpadded RSA homomorphism**: RSA encryption is **multiplicatively homomorphic** — `E(m_1) · E(m_2) = m_1^e m_2^e = (m_1 m_2)^e = E(m_1 m_2) (mod n)`. An attacker who wants to decrypt a ciphertext `c = m^e` can choose a random `r`, compute `c' = c · r^e mod n = (mr)^e mod n`, ask for the decryption of `c'` (which looks like a fresh ciphertext, not the original), receive `mr mod n`, and divide by `r mod n` to get `m`. This **multiplicative blinding** attack lets an attacker decrypt any ciphertext by wrapping it once — a completely broken IND-CPA scheme. OAEP padding removes the homomorphism.",
            "**Common modulus attack**: if two users share the same RSA modulus `n` but have different exponents `(e_1, d_1)` and `(e_2, d_2)`, an attacker with `c_1 = m^{e_1} mod n` and `c_2 = m^{e_2} mod n` (same message encrypted for both) can, when `gcd(e_1, e_2) = 1`, recover `m` via Bezout: find integers `a, b` with `a e_1 + b e_2 = 1`, then `c_1^a c_2^b = m^{ae_1 + be_2} = m^1 = m (mod n)`. Shared moduli also allow cross-decryption: user 1's private key knowledge of `d_1` and the relation `e_1 d_1 ≡ 1 (mod φ(n))` leaks information about φ(n) that can sometimes be used to factor n. The fix: every keypair must have its own fresh modulus.",
            "**Franklin-Reiter related message attack**: if two messages `m_1, m_2` are related by a known low-degree polynomial `m_2 = f(m_1)` (e.g. `m_2 = m_1 + 1`), and both are encrypted with the same RSA key, `m_1` can be recovered via a GCD computation on polynomials modulo n. This attack is a reminder that without padding, any algebraic relationship between plaintexts leaks both. The severity of all textbook attacks underscores the fundamental design principle: **the security of RSA comes entirely from the padding scheme, not the modular exponentiation itself**.",
          ],
          resources: [
            { label: "Twenty Years of Attacks on the RSA Cryptosystem — Dan Boneh (survey paper)", url: "https://crypto.stanford.edu/~dabo/abstracts/RSAattack-survey.html", type: "paper" },
            { label: "Cryptopals — RSA challenges (sets 5-6)", url: "https://cryptopals.com/sets/6", type: "practice" },
          ],
        },
        {
          title: "Bleichenbacher's PKCS#1 v1.5 padding oracle",
          body: [
            "The most influential padding-oracle attack in history is Daniel Bleichenbacher's 1998 attack on **PKCS#1 v1.5** (the RSA padding standard in use since the 1990s). The attack requires only that the decryption endpoint distinguishes between 'valid padding' and 'invalid padding' — even a different error message or a different HTTP response code suffices. Given only this single bit of information per query, an adaptive chosen-ciphertext attack can decrypt any RSA-PKCS#1v1.5 ciphertext with ~1 million queries.",
            "PKCS#1 v1.5 padding: when encrypting message `m` of `k-2-len(m)` bytes, construct `EM = 0x00 ‖ 0x02 ‖ PS ‖ 0x00 ‖ m` where `PS` is at least 8 random non-zero bytes, and then encrypt `EM` as an RSA integer. On decryption, the decrypted value must start with `0x00 ‖ 0x02`. If it does not, valid decryption fails — and if the server tells you this even implicitly, the oracle exists.",
            "Bleichenbacher's attack exploits RSA's multiplicative homomorphism. The attacker wants to decrypt target ciphertext `c = m^e mod n`. They generate a multiplier `s` and submit the blinded ciphertext `c' = c · s^e mod n = (ms)^e mod n` to the oracle. If the oracle reports valid padding, then `ms mod n` starts with `0x00 ‖ 0x02` — this constrains `ms` to a specific range. By iterating through chosen values of `s`, each oracle query narrows the interval of possible values for `m`. After ~1 million queries, the interval collapses to a single value, revealing `m`. The attack is an adaptive chosen-ciphertext attack (CCA2), and the padding oracle is the only information channel needed.",
            "Real-world impact has been enormous. Bleichenbacher's original attack targeted SSL/TLS session key recovery. Variants kept appearing for 20 years: **DROWN** (2016, SSLv2 PKCS#1 oracle leaks modern TLS session keys), **ROBOT** (2017, Return Of Bleichenbacher's Oracle Threat — found active in 27 of the Alexa Top 100 websites). The ROBOT paper is a masterclass in how long implementation vulnerabilities persist once the algorithmic foundation is compromised. Even RSA_PKCS1_PADDING in OpenSSL was not truly immune without careful constant-time handling.",
            "The defense is to switch to **OAEP padding** (see next subtopic), which has a provably secure CCA2 reduction, making padding-oracle attacks impossible under standard assumptions. For legacy PKCS#1 v1.5 deployments, the mitigation is to use **constant-time decryption** that does not branch on padding validity, generate a random pre-master secret as a fallback when padding is invalid (so the protocol always proceeds to the same path), and never distinguish between padding errors and other errors in any observable way. OpenSSL implemented this mitigation (with several bugs and regressions over the years) for TLS compatibility. The lesson: a cryptographic scheme with a decryption error oracle is broken; choose constructions that are provably CCA2-secure instead.",
          ],
          resources: [
            { label: "Bleichenbacher 1998 — Chosen Ciphertext Attacks Against Protocols Based on RSA (original paper)", url: "https://link.springer.com/chapter/10.1007/BFb0055716", type: "paper" },
            { label: "ROBOT Attack paper (2017) — replication in modern TLS", url: "https://robotattack.org/", type: "paper" },
          ],
        },
        {
          title: "OAEP and PSS — the correct constructions",
          body: [
            "**OAEP (Optimal Asymmetric Encryption Padding)**, defined in PKCS#1 v2.1 (RFC 8017), provides IND-CCA2 security under the RSA assumption (in the random oracle model). The construction uses two hash functions (modeled as random oracles) and a Feistel-like encoding before RSA encryption, ensuring that the padded plaintext looks uniformly random — making the structure that Bleichenbacher's attack exploits completely unavailable.",
            "OAEP encoding: given message `m` and label `L` (optional context), generate random seed `r` of `hLen` bytes. Compute `maskedDB = (m ‖ 0x01 ‖ zeros) XOR MGF1(r)` and `maskedSeed = r XOR MGF1(maskedDB)`, where `MGF1` is a mask generation function (a PRF based on a hash). The encoded message is `EM = 0x00 ‖ maskedSeed ‖ maskedDB`, and RSA encryption is `c = EM^e mod n`. Decryption recovers `EM`, splits it, uses the two XOR steps to recover `r` and `m`, and verifies the padding structure. The key property: a padding error cannot be used to learn anything about the message — the message and padding are cryptographically entangled via the two masked layers, so any modification of `c` completely corrupts the recovered `m` in a way that reveals no structure.",
            "The CCA2 security proof (Fujisaki-Okamoto transform applied to RSA-OAEP, by Fujisaki, Okamoto, Pointcheval, Stern 2001, with a correction by Shoup 2001) shows that breaking IND-CCA2 security of RSA-OAEP requires either breaking RSA or breaking the random oracle model. This is the gold standard: a security proof that reduces to a well-studied hardness assumption. OAEP is what you should always use for RSA encryption. The PKCS#1 v1.5 padding mode should be disabled in all new systems.",
            "**PSS (Probabilistic Signature Scheme)** is the analogous construction for RSA signatures. Standard 'textbook' RSA signatures (`σ = m^d mod n`, verify `σ^e ≡ m mod n`) are forgeable: given signatures `σ_1, σ_2` on messages `m_1, m_2`, the attacker can form `σ = σ_1 σ_2 mod n`, which verifies for `m = m_1 m_2 mod n` (due to the homomorphism). PSS uses a randomized encoding before signing — similar in spirit to OAEP but applied to the sign operation — and provides **existential unforgeability under chosen-message attack (EUF-CMA)** in the random oracle model. In practice, PSS with a random salt of `hLen` bytes achieves tight security, whereas PKCS#1 v1.5 signatures (RSASSA-PKCS1-v1_5) are unforgeably secure by an argument that requires the hash being a random oracle but the security reduction is not tight. TLS 1.3 mandates PSS for RSA signatures; PKCS#1 v1.5 signatures remain only for legacy RSA-based certificate verification in TLS 1.3 (due to installed certificate base).",
            "Two practical points: (1) Both OAEP and PSS require a secure random number generator — OAEP because the seed `r` must be fresh for each encryption; PSS because the salt must be fresh for each signing. A system that reuses the random seed in OAEP can leak the plaintext (similar to nonce-reuse in stream ciphers). (2) The key size matters differently for encryption vs. signature: the maximum message size in RSA-OAEP with SHA-256 and a 2048-bit key is `256 - 2 × 32 - 2 = 190 bytes` — far too small for most data. In practice, RSA-OAEP is only used to encrypt a symmetric **key encapsulation** (a short AES key or a session key), and the actual data is encrypted symmetrically. This hybrid encryption pattern (RSA-KEM + AES-GCM) is the standard approach and the basis for modern KEM/DEM constructions.",
          ],
          resources: [
            { label: "RFC 8017 — PKCS#1 v2.2 (OAEP and PSS specifications)", url: "https://datatracker.ietf.org/doc/html/rfc8017", type: "docs" },
          ],
        },
        {
          title: "Timing side-channels and RSA blinding",
          body: [
            "**Side-channel attacks** exploit physical implementation characteristics — timing, power consumption, electromagnetic emissions — rather than mathematical weaknesses. For RSA, the most practical side-channel is **timing**: the time to compute `c^d mod n` depends on the bits of `d`, and an attacker who can measure decryption time with sufficient precision can recover the private key bit by bit.",
            "Square-and-multiply exponentiation processes each bit of the exponent: for a '0' bit, only squaring; for a '1' bit, squaring then multiplying. A '1' bit takes strictly longer than a '0' bit (one extra multiplication). Over millions of timing measurements of decryption operations with the same key `d`, an attacker can use statistical analysis to correlate observed timing with each bit of `d`: guess bit `d_i = 1` (takes longer on average), compare to the measured distribution — Kocher's 1996 paper demonstrated this empirically against real RSA hardware. The attack extends to ECDSA, AES, and any other algorithm where branches or memory accesses depend on secret data.",
            "**RSA blinding** (Chaum's blinding technique, also in Kocher's original paper) is the standard defense. Before decrypting ciphertext `c`, the decryptor generates a random `r` and computes `c' = c · r^e mod n`. Decrypts `m' = (c')^d = (c r^e)^d = c^d · r^{ed} = m · r (mod n)`. Then unblinds: `m = m' · r^{-1} mod n`. Since `r` is fresh and random for each decryption, the computation time measured by an attacker varies independently of `c` and `d`, preventing timing correlation. The blinding and unblinding cost ~2 extra multiplications — negligible. All modern RSA implementations (OpenSSL, BoringSSL, libsodium) use constant-time operations and blinding by default.",
            "**Montgomery multiplication** is the foundational technique for efficient, constant-time modular multiplication in hardware and software. It avoids expensive modular reduction (which has data-dependent cost) by choosing a working representation `a_R = aR mod n` (where `R = 2^k` for some `k > log2(n)`) such that the reduction step `(a_R b_R) / R mod n` is a simple right-shift instead of a division. By ensuring all multiplications go through Montgomery form, the reduction becomes data-independent in cost, making the implementation inherently resistant to timing attacks on the reduction. Combined with constant-time conditional swaps (replacing data-dependent branches with `constant_time_select(condition, a, b)` that always reads both operands), Montgomery arithmetic is the basis of constant-time ECC and RSA implementations.",
            "Power and EM side-channels are mitigated similarly: by ensuring that power consumption and EM emissions do not correlate with secret data. This requires point-randomization (in ECC: randomizing the projective representation of the point, so all representations of the same point consume the same power pattern) and masking (in AES: secret-sharing the state so individual shares are independent of the secret). Side-channel countermeasures are an entire field; the practical takeaway is to always use a well-audited cryptographic library (BoringSSL, libsodium, NSS) rather than implementing any crypto from scratch, because constant-time behavior and side-channel resistance are extremely difficult to achieve and verify manually.",
          ],
          resources: [
            { label: "Kocher 1996 — Timing Attacks on Implementations of DH, RSA, DSS (original paper)", url: "https://www.paulkocher.com/doc/TimingAttacks.pdf", type: "paper" },
          ],
        },
      ],
      resources: [
        { label: "Cryptopals — RSA sets (3-6)", url: "https://cryptopals.com/sets/5", type: "practice" },
        { label: "RSA attacks survey — Boneh (Twenty Years of Attacks)", url: "https://crypto.stanford.edu/~dabo/abstracts/RSAattack-survey.html", type: "paper" },
      ],
      connections: [
        { to: "crypto-numbertheory", note: "RSA is Euler's theorem + CRT made into a cryptosystem" },
        { to: "crypto-pqc", note: "Shor's algorithm breaks RSA — the motivation for post-quantum crypto" },
        { to: "cyber-web", note: "Padding-oracle attacks (Bleichenbacher) recur in TLS/web exploits" },
      ],
    },
  ],
});

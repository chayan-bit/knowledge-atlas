atlasAdd({
  id: "aiml",
  topics: [
    {
      id: "ai-adversarial",
      title: "Adversarial Machine Learning",
      level: "Research",
      body: [
        "Adversarial ML studies the robustness of ML models to deliberate, worst-case perturbations of inputs. FGSM and PGD attack image classifiers; Carlini-Wagner finds minimal perturbations. For LLMs, adversarial prompts (GCG, AutoDAN) elicit harmful outputs. Certified robustness via randomised smoothing provides provable guarantees.",
      ],
      subtopics: [
        {
          title: "Image Adversarial Attacks: FGSM, PGD, and the Threat Model",
          body: [
            "**Adversarial examples** (Szegedy et al., 2013) are inputs $\\tilde{x} = x + \\delta$ where $\\|\\delta\\|_p \\leq \\epsilon$ (imperceptible perturbation under $L_p$ norm) but $f_\\theta(\\tilde{x}) \\neq f_\\theta(x)$ (the classifier's prediction changes). The existence of adversarial examples reveals that neural networks' decision boundaries are not aligned with human visual perception — a small step in pixel space can cross a class boundary while being visually imperceptible. This has security implications: an adversary can fool a traffic sign classifier, a face verification system, or a medical image diagnosis model.",
            "**Fast Gradient Sign Method (FGSM)** (Goodfellow et al., 2014) computes a single-step adversarial perturbation: $\\delta = \\epsilon \\cdot \\text{sign}(\\nabla_x \\mathcal{L}(f_\\theta(x), y))$. The sign of the gradient points in the direction that maximally increases loss (for the correct label $y$); taking a step of size $\\epsilon$ in this direction is guaranteed to increase loss by at least $\\epsilon \\|\\nabla_x \\mathcal{L}\\|_1$ (since $\\|\\delta\\|_\\infty = \\epsilon$ and dot product with sign vector = $L_1$ norm of gradient). FGSM produces adversarial examples under the $L_\\infty$ norm ($\\|\\delta\\|_\\infty \\leq \\epsilon$) — each pixel is perturbed by at most $\\epsilon$. It is a **single-step** method and trades attack strength for speed.",
            "**Projected Gradient Descent (PGD)** (Madry et al., 2018) extends FGSM to a multi-step attack. Initialise $\\tilde{x}_0 = x + \\mathcal{U}(-\\epsilon, \\epsilon)$ (random start). Iterate: $\\tilde{x}_{t+1} = \\Pi_{\\mathcal{B}_\\infty(x, \\epsilon)}(\\tilde{x}_t + \\alpha \\cdot \\text{sign}(\\nabla_{\\tilde{x}} \\mathcal{L}(f_\\theta(\\tilde{x}_t), y)))$ where $\\Pi_{\\mathcal{B}_\\infty(x,\\epsilon)}$ projects back into the $\\epsilon$-ball around $x$ (clip each coordinate to $[x_i - \\epsilon, x_i + \\epsilon]$). PGD with many steps and random restarts (PGD-$k$) approximates the **strongest possible $L_\\infty$ attack** — it is the first-order attack, optimal within the class of gradient-based attacks. Madry et al.'s key claim: **adversarial training against PGD produces the most adversarially robust classifiers**, a claim that remains foundational.",
            "**Carlini-Wagner (CW) attack** (Carlini & Wagner, 2017) finds the **minimal perturbation** to fool the classifier by optimising: $\\min_\\delta \\|\\delta\\|_2 + c \\cdot f(x+\\delta)$ where $f$ is a surrogate loss designed to cause misclassification (specifically $f(x') = \\max(\\max_{j \\neq t} Z(x')_j - Z(x')_t, -\\kappa)$ for a targeted attack to class $t$, where $Z$ are logits and $\\kappa$ is a confidence parameter). The reparameterisation $\\delta = \\tanh(w) - x$ ensures $x + \\delta \\in [0,1]^d$ automatically. Binary search on $c$ finds the minimal $c$ for which the attack succeeds. CW-$L_2$ attack is the strongest known $L_2$ norm attack and was used to demonstrate that **defensive distillation** (Papernot et al., 2016) does not provide meaningful robustness — breaking several proposed defences at the time.",
            "**Threat models and evaluation**: an **attack threat model** specifies: (a) **Norm**: $L_0$ (perturb $k$ pixels), $L_2$ (Euclidean), $L_\\infty$ (max pixel change), or domain-specific (colour, texture, geometry). (b) **Knowledge**: white-box (full access to model), black-box (query-only access), transfer (train adversarial examples on a surrogate model, transfer to target). (c) **Goal**: untargeted (any misclassification), targeted (classify as specific class), universal (same perturbation fools all inputs). A defence must be evaluated against the **strongest relevant attack** — many proposed defences in 2016-2019 were broken by CW or PGD because they were evaluated against weaker attacks. **Obfuscated gradients** (Athalye et al., 2018) identified a failure mode: defences that made gradients uninformative (gradient masking, non-differentiable preprocessing) appeared robust but were bypassed by BPDA (Backward Pass Differentiable Approximation) which estimates gradients through non-differentiable operations.",
          ],
          resources: [
            { label: "FGSM - Goodfellow et al. (2014)", url: "https://arxiv.org/abs/1412.6572", type: "paper" },
            { label: "PGD and Adversarial Training - Madry et al. (2018)", url: "https://arxiv.org/abs/1706.06083", type: "paper" },
            { label: "Carlini-Wagner attack (2017)", url: "https://arxiv.org/abs/1608.04644", type: "paper" },
          ],
        },
        {
          title: "Adversarial Training, Certified Robustness, and Randomised Smoothing",
          body: [
            "**Adversarial training** (AT) augments training with adversarial examples: $\\min_\\theta \\mathbb{E}_{(x,y) \\sim \\mathcal{D}} \\max_{\\|\\delta\\|_\\infty \\leq \\epsilon} \\mathcal{L}(f_\\theta(x+\\delta), y)$. The inner $\\max$ is the adversarial attack (solved by PGD); the outer $\\min$ is the standard training objective. This min-max game produces a model that is robust to $L_\\infty$ perturbations up to $\\epsilon$. The cost: **clean accuracy drops** by $5$-$15\%$ (the adversarially trained model is more conservative, having learned that nearby inputs may have different labels). **Trades** (Zhang et al., 2019): surrogate loss that explicitly balances clean accuracy and robustness: $\\mathcal{L}_{\\text{TRADES}} = \\mathcal{L}_{\\text{CE}}(f(x), y) + \\beta \\max_{\\|\\delta\\|\\leq\\epsilon} \\text{KL}(f(x) || f(x+\\delta))$. The KL term penalises sensitivity to perturbations without fully adversarially training.",
            "**The robustness-accuracy tradeoff** is fundamental: any classifier with high adversarial robustness (against $\\epsilon$-ball attacks) must have lower clean accuracy on natural distributions. The formal result (Tsipras et al., 2019): under a simple two-feature data model, robust classifiers use different features than natural classifiers. The 'non-robust features' exploited by natural classifiers (imperceptible but highly correlated with labels in natural data) are the same features attacked by adversarial perturbations. Robust classifiers must ignore these features, relying only on 'robust features' (perceptible and stable under perturbation). This feature theory suggests the clean-robust tradeoff is partially a consequence of training data not distinguishing the two feature types.",
            "**Certified robustness via randomised smoothing** (Cohen et al., 2019) provides provable robustness guarantees. Given a base classifier $f$ (possibly not robust), the **smoothed classifier** $g$ is: $g(x) = \\arg\\max_c \\Pr_{\\delta \\sim \\mathcal{N}(0, \\sigma^2 I)}[f(x+\\delta) = c]$. The key theorem: if $g(x) = c_A$ (the most likely class) with probability $p_A$ and all other classes have probability $\\leq p_B < p_A$, then $g(x') = c_A$ for all $\\|x' - x\\|_2 \\leq R$ where $R = \\frac{\\sigma}{2}(\\Phi^{-1}(p_A) - \\Phi^{-1}(p_B))$ ($\\Phi^{-1}$ is the inverse normal CDF). This is a **certified radius** — within the $L_2$ ball of radius $R$, the smoothed classifier is guaranteed to predict $c_A$. Estimation: Monte Carlo estimation of $p_A$ using $n = 100k$ Gaussian samples per test point. Trade-off: larger $\\sigma$ = larger certified radius but less accurate base predictions.",
            "**Data poisoning attacks**: instead of perturbing test inputs, the adversary manipulates training data. **Backdoor attacks** (Trojan attacks): inject a small fraction of poisoned examples with a trigger (specific pixel pattern or patch) associated with a target class. The model behaves normally on clean inputs but predicts the target class whenever the trigger is present. **Clean-label backdoor attacks** (Turner et al., 2019): the poisoned examples have correct labels but are adversarially perturbed to be close to the trigger pattern — the model learns to associate the trigger with the poison class without the poisoner needing to modify labels (making the attack undetectable by label inspection). **Supply-chain attacks**: poisoning publicly available datasets (ImageNet, LAION) used for pre-training, affecting all downstream fine-tuned models.",
          ],
          resources: [
            { label: "Certified Robustness via Randomised Smoothing - Cohen et al. (2019)", url: "https://arxiv.org/abs/1902.02918", type: "paper" },
            { label: "TRADES - Zhang et al. (2019)", url: "https://arxiv.org/abs/1901.08573", type: "paper" },
            { label: "RobustBench - adversarial robustness benchmark", url: "https://robustbench.github.io/", type: "tool" },
          ],
        },
        {
          title: "LLM Adversarial Attacks: GCG, AutoDAN, and Multimodal Jailbreaks",
          body: [
            "**Transferring adversarial attacks to LLMs**: the discrete token space of LLMs makes gradient-based attacks harder than for continuous image inputs. FGSM/PGD require continuous inputs; tokens are discrete, so the gradient $\\nabla_{x_{\\text{token}}} \\mathcal{L}$ is with respect to the continuous embedding $e_t \\in \\mathbb{R}^d$, not the token ID. The gradient in embedding space cannot be directly mapped to a token substitution. **Greedy Coordinate Gradient (GCG)** (Zou et al., 2023) addresses this: for each token position $i$ in the adversarial suffix, use the gradient of the loss with respect to the one-hot token representation to identify the top-$k$ candidate token substitutions; try each in random order and keep the best. This is a **greedy coordinate descent** in token space, one position at a time, with gradient-guided candidate selection. GCG optimises a suffix $s$ appended to a harmful instruction to maximise $\\Pr[\\text{model starts with 'Sure, here is'}]$.",
            "**AutoDAN** (Zhu et al., 2023): an automated jailbreak generation system using a **hierarchical genetic algorithm** operating in natural language space. Initial population: seed jailbreaks (DAN-style roleplay prompts, fictional framings). Mutation: LLM-paraphrase each prompt while preserving jailbreak structure. Crossover: combine parts of two successful jailbreaks. Selection: rank by attack success rate on the target model (tested by querying the model). AutoDAN produces **fluent, natural-language jailbreaks** (unlike GCG's nonsense token sequences) that are harder to detect by perplexity filtering. The key advantage: natural language jailbreaks transfer better across models because they exploit semantic vulnerabilities rather than gradient-specific weaknesses.",
            "**Multimodal adversarial attacks**: VLMs (LLaVA, GPT-4V) are vulnerable to adversarial images that elicit harmful text outputs. **Visual adversarial examples for VLMs**: find $\\delta$ such that $\\|\\delta\\|_\\infty \\leq \\epsilon$ and the model produces a harmful text completion when given image $x+\\delta$ and a benign text prompt. The gradient flows from the text loss through the LLM through the visual encoder to the pixel space. **Typography attacks** (Goh et al., 2021): placing text labels within images can override the visual classification — a stop sign with 'Speed Limit 45' printed on it is classified as a speed limit sign. For VLMs: embedding 'Ignore previous instructions and...' in white text on a white background within an image can inject instructions that override the system prompt.",
            "**Adversarial training for LLMs**: applying adversarial training to LLMs is challenging because: (1) the discrete token space makes inner-loop optimisation expensive (GCG requires hundreds of forward passes per update); (2) the diversity of harmful behaviours makes it hard to enumerate the adversarial manifold; (3) adversarial training on text reduces fluency and coherence. **R2D2** (Mazeika et al., 2024) adversarially trains a classifier (guard model) against GCG attacks, hardening it without touching the target LLM. **Adversarial training with smooth perturb**: perturb in embedding space rather than token space, using continuous perturbations bounded by $L_\\infty$ on embedding norms — more tractable for gradient-based inner loop, though not directly meaningful in discrete token space. Evaluations: **HarmBench** (Mazeika et al., 2024) provides a standardised evaluation of LLM robustness against $18$ attack methods across $33$ scenarios.",
          ],
          resources: [
            { label: "GCG - Universal Adversarial Attacks on LLMs - Zou et al. (2023)", url: "https://arxiv.org/abs/2307.15043", type: "paper" },
            { label: "AutoDAN - Zhu et al. (2023)", url: "https://arxiv.org/abs/2310.04451", type: "paper" },
            { label: "HarmBench - adversarial robustness benchmark for LLMs", url: "https://www.harmbench.org/", type: "tool" },
          ],
        },
        {
          title: "Poisoning, Model Extraction, and Membership Inference",
          body: [
            "**Membership inference attacks (MIA)** determine whether a data point was in the training set. **Shadow model attack** (Shokri et al., 2017): train multiple 'shadow models' on known member/non-member subsets; train an attack model on their outputs; apply to the target model. The core signal: models overfit to training data — training members have lower loss (higher confidence on the true label) than non-members. **Likelihood ratio test**: compare $\\log P(y|x, \\text{member})$ vs $\\log P(y|x, \\text{non-member})$ using the shadow model's calibration. For LLMs: training-time memorisation of verbatim text sequences makes MIA more powerful. **Extracting Training Data** (Carlini et al., 2021): prompt GPT-2 with common prefixes, generate completions, and identify memorised text by measuring the **zlib compression rate** — memorised sequences are less compressible (more structured) than model-generated text. Recovered verbatim PII, poems, code, and deduplicated training sequences.",
            "**Model extraction attacks** (Tramèr et al., 2016) reconstruct a model's functionality by querying it as a black box. **Equation-solving approach**: for a linear model with $d$ features and $c$ classes, send $d \\cdot c$ queries at points that reveal the model's weights. For neural networks: send synthetic or natural queries, collect (input, output) pairs, and train a local substitute model on them. The substitute model can be used to craft adversarial examples that transfer to the target model. **Adaptive attacks**: if the target model adds noise to outputs (differential privacy) or uses random dropout at inference (MC Dropout), the attacker can average multiple queries per input to recover the true output. Model extraction is a concern for proprietary APIs: an attacker can replicate a model's behaviour at a fraction of the training cost.",
            "**Differential privacy as a defence**: DP-SGD (Abadi et al., 2016) adds calibrated noise to gradients during training, providing $(\\epsilon, \\delta)$-DP guarantees that bound the information leakable via MIA. The **privacy amplification by sampling** lemma: if DP-SGD processes a random mini-batch of size $B$ from dataset $N$, the effective $\\epsilon$ is amplified by $B/N$ — a $256/10000 = 2.56\%$ batch fraction amplifies an $\\epsilon=2$ guarantee to $\\epsilon \\approx 0.05$. This means tight DP budgets are achievable with small batch ratios. DP-trained models resist MIA by construction: the membership inference advantage is bounded by $e^\\epsilon - 1$ (when $\\delta$ is negligible). Cost: DP training requires gradient clipping (reducing the signal of large-gradient examples) and Gaussian noise addition, significantly degrading model quality at tight $\\epsilon < 1$ budgets.",
          ],
          resources: [
            { label: "Extracting Training Data from LLMs - Carlini et al. (2021)", url: "https://arxiv.org/abs/2012.07805", type: "paper" },
            { label: "DP-SGD - Abadi et al. (2016)", url: "https://arxiv.org/abs/1607.00133", type: "paper" },
            { label: "Model Stealing Attacks - Tramèr et al. (2016)", url: "https://arxiv.org/abs/1609.02943", type: "paper" },
          ],
        },
      ],
      connections: [
        { to: "ai-safety", note: "Jailbreaks and adversarial attacks are the threat model that AI safety defences must guard against" },
        { to: "ai-training", note: "Adversarial training (PGD, TRADES) is an expensive fine-tuning technique requiring inner-loop attack optimisation" },
        { to: "ai-interp", note: "Understanding which features cause misclassification requires mechanistic interpretability of decision boundaries" },
      ],
      resources: [
        { label: "CleverHans - adversarial example library", url: "https://github.com/cleverhans-lab/cleverhans", type: "repo" },
        { label: "Adversarial Robustness Toolbox (IBM)", url: "https://adversarial-robustness-toolbox.readthedocs.io/", type: "docs" },
      ],
    },
  ],
});

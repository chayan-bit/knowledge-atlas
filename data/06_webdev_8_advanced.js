atlasAdd({
  id: "webdev",
  topics: [
    {
      id: "web-advanced",
      title: "Browser internals, WASM & the edge",
      level: "Expert",
      body: [
        "Rendering pipeline internals (Blink/WebKit, compositor thread, layer promotion), **V8 deep dive** (TurboFan, Maglev, Sparkplug), **HTTP/3 + QUIC** (0-RTT, connection migration), **WebAssembly** (linear memory, WASI, porting C/Rust to the browser), and edge computing (Cloudflare Durable Objects/KV, V8 isolates).",
        "WASM in particular is where web dev meets low-level systems - you ship compiled C/Rust to a browser sandbox.",
      ],
      subtopics: [
        {
          title: "Blink rendering pipeline: from DOM to pixels",
          body: [
            "**Blink** (Chrome's rendering engine, a fork of WebKit) turns a document (HTML, CSS, JavaScript) into pixels on screen through a multi-stage pipeline. The stages are not always sequential - Blink's architecture increasingly parallelizes them across threads and frames. Understanding the pipeline is prerequisite to GPU-composited animation, layer promotion strategies, and diagnosing jank (dropped frames).",
            "The stages in order: **Parse** (HTML → DOM, CSS → CSSOM, parallel to HTML parsing where possible), **Style** (cascade computed styles to every DOM node - produces the **ComputedStyle** for each node), **Layout** (compute geometry: position, size, float clearance, for every element - produces the **LayoutObject** tree, one per rendered element), **Pre-paint** (determine which elements need their own **paint layer**, compute transforms and clips), **Paint** (record draw operations for each element into display lists, not actual pixels), **Compositing** (upload display lists as **textures** to the GPU, arrange layers, and let the GPU composite them into the final frame). Compositing runs on the **compositor thread**, separate from the main thread, which is why CSS transforms and opacity transitions are smooth even when the main thread is busy.",
            "**Layer promotion** is the mechanism for GPU compositing. Certain CSS properties cause an element to be promoted to its own **compositing layer** (a separate texture in GPU memory): `transform`, `opacity` with animation, `will-change`, `position: fixed`, elements with a 3D transform or overlapping stacking context, canvas elements. When an animation only changes a promoted layer's transform or opacity, the compositor thread handles it without involving the main thread - achieving 60fps even during JavaScript execution. The cost: each compositing layer consumes GPU memory (the layer's visual region as a texture). Too many layers (over-promotion) can exhaust GPU memory and actually reduce performance. Use `will-change: transform` judiciously on elements that are about to animate, and remove it after the animation completes.",
            "**Forced layout (reflow)** is when reading certain layout properties (e.g., `element.offsetWidth`, `element.getBoundingClientRect()`) causes the browser to synchronously compute layout before returning the value, because the layout may be dirty (a previous JS mutation invalidated it). The worst case is **layout thrashing**: alternating between reads and writes in a loop: `for (const el of elements) { el.style.width = el.parentElement.offsetWidth + 'px'; }`. Each iteration reads `offsetWidth` (forces layout), writes `style.width` (invalidates layout), reads again (forces layout again), and so on. The fix: **batch reads before writes** - collect all the values first, then apply all the mutations. Libraries like FastDOM formalize this pattern.",
            "**Paint invalidation**: when a CSS property changes that cannot be handled purely by the compositor (e.g., `background-color`, `border-radius`, `box-shadow`), the affected element's paint layer must be **repainted** (display list re-recorded and texture re-uploaded to GPU). Repaints are expensive. **Compositor-only properties** that never cause repaint: `transform`, `opacity`, `filter` (some values). The rule for performant CSS animations: animate only `transform` and `opacity`. Animating `width`, `height`, `top`, `left`, `background-color`, or any other property causes layout or paint, not just compositing. Animating `left: 200px` fires layout every frame at 60fps; animating `transform: translateX(200px)` fires compositing only.",
          ],
          resources: [
            { label: "Chromium - Life of a pixel (talk)", url: "https://www.youtube.com/watch?v=m-J-tbAlFic", type: "video", note: "the authoritative rendering pipeline walk-through" },
            { label: "web.dev - Rendering performance", url: "https://web.dev/explore/rendering", type: "docs" },
            { label: "MDN - CSS will-change", url: "https://developer.mozilla.org/en-US/docs/Web/CSS/will-change", type: "docs" },
          ],
        },
        {
          title: "V8 internals: Ignition, TurboFan, Maglev, and Sparkplug",
          body: [
            "V8's compilation pipeline has evolved from a simple two-tier system (interpreter + optimizing compiler) to a sophisticated multi-tier system that trades compilation cost for peak throughput at different stages of execution. Understanding the tiers explains performance behavior that would otherwise be mysterious: why code gets faster after warming up, why certain patterns cause sudden slowdowns, and why startup time and peak throughput require different optimization strategies.",
            "**Ignition** is V8's bytecode interpreter. It compiles JavaScript to a compact bytecode format (not machine code) and interprets it with a register-based virtual machine. Ignition also collects **type feedback**: as it executes, it records what types it observes at each operation site (e.g., 'the + operator at offset 42 has always seen two integers'). This feedback is stored in **feedback vectors** per function. Ignition is fast to start (compiling JS to bytecode is fast, much faster than to machine code) and correct (handles all JS semantics without assumptions), making it the baseline for all code.",
            "**Sparkplug** (added in V8 9.1) is a non-optimizing compiler between Ignition and TurboFan. It compiles bytecode to machine code quickly (without optimization) by doing a simple one-pass translation of each bytecode instruction to machine code, using the same feedback structures Ignition maintains. The machine code runs faster than interpreted bytecode (~20-30% improvement) with near-zero compilation cost. Functions that are moderately hot (called frequently but not hot enough to justify TurboFan's expensive compilation) run in Sparkplug.",
            "**TurboFan** (optimizing JIT compiler) compiles very hot functions to highly optimized machine code using the type feedback collected by Ignition. Key optimizations: **inline caching** (call site specialized for the observed types - property access becomes a direct load at a known offset), **inlining** (replace a function call with the function's body to eliminate call overhead and enable further optimization across the inlined code), **escape analysis** (determine that an object does not escape the function, allocate it on the stack instead of the heap - eliminates GC pressure), **loop optimizations** (vectorization, loop unrolling). TurboFan represents the function as a **Sea of Nodes** graph (nodes are operations, edges are data/control dependencies) which enables global optimizations beyond what a linear IR allows.",
            "**Maglev** (added in V8 11.2, Chrome 117) sits between Sparkplug and TurboFan. It is an optimizing compiler that uses type feedback but is significantly faster to compile than TurboFan. Maglev targets medium-hot functions that currently toggle between Sparkplug-compiled code and TurboFan's expensive compilation. It provides most of TurboFan's benefit (inlining, IC optimization) at a fraction of the compilation time, reducing the 'compilation cliff' where startup is slow because TurboFan is busy compiling everything. The practical impact: faster startup for large applications without sacrificing peak throughput for hot code paths that still escalate to TurboFan.",
            "**Deoptimization**: when TurboFan's assumptions about types prove wrong (a function it compiled assuming integer arguments receives a string), it **deoptimizes** - discards the optimized code and falls back to Ignition bytecode for this call site, then potentially re-optimizes with updated type feedback. Deoptimizations are logged with `--trace-deopt` in Node.js. The cost of deoptimization is the one-time fallback, but if a function oscillates between optimized and deoptimized (because it is sometimes called with one type and sometimes another), it may end up in **'optimization disabled'** state, running perpetually in Ignition. Writing 'monomorphic' functions (consistent argument types, consistent object shapes) is the key to preventing this.",
          ],
          resources: [
            { label: "V8 blog - Maglev compiler", url: "https://v8.dev/blog/maglev", type: "docs" },
            { label: "V8 blog - Sparkplug compiler", url: "https://v8.dev/blog/sparkplug", type: "docs" },
            { label: "V8 blog - Turbofan JIT design", url: "https://v8.dev/docs/turbofan", type: "docs" },
          ],
        },
        {
          title: "HTTP/3 and QUIC: multiplexing over UDP, 0-RTT, and connection migration",
          body: [
            "**HTTP/3** runs over **QUIC** instead of TCP, addressing fundamental limitations of TCP that HTTP/2 exposed. HTTP/2 solved HTTP-layer head-of-line blocking (multiple streams over one connection) but exposed TCP-layer head-of-line blocking: a lost packet stalls all streams because TCP's delivery guarantees mean the receiver cannot process data beyond the missing packet until it is retransmitted. On high packet-loss networks (mobile, satellite), HTTP/2's throughput degrades significantly. QUIC implements its own transport layer in userspace on top of UDP, with stream multiplexing built in at the transport level - a lost packet stalls only the stream it belongs to, not all streams.",
            "**QUIC's connection establishment** is significantly faster than TCP+TLS. TCP requires a 1.5-RTT handshake; TLS 1.2 requires 2 additional RTTs (2 round trips for TLS handshake). Total: 3.5 RTTs before the first byte of application data. TLS 1.3 over TCP reduces this to 1 RTT for TLS. QUIC combines transport and crypto handshake: **1-RTT** for new connections (both QUIC transport parameters and TLS 1.3 are established in one round trip). **0-RTT resumption**: for a returning client with a cached session ticket, QUIC can include application data in the first packet, before any server round trip. The server processes this '0-RTT data' immediately. The security tradeoff: 0-RTT data is **replay-vulnerable** (an attacker who captures the first packet can replay it). QUIC 0-RTT should only be used for idempotent requests (GET, HEAD - not POST/PUT mutations).",
            "**Connection migration** is QUIC's most operationally significant feature. In TCP, a connection is identified by the 4-tuple: (src IP, src port, dst IP, dst port). Changing any component (switching from WiFi to cellular mid-request) terminates the TCP connection and requires reconnecting. QUIC identifies connections by a **Connection ID**, a client-chosen opaque byte string included in every packet. When the client's network changes (new IP, new port), it continues sending packets with the same Connection ID; the server recognizes the new address and migrates the connection without interruption. HTTP/3 transfers in progress survive network transitions - a large file download or video stream continues seamlessly as a laptop moves between access points.",
            "**QUIC's congestion control** is pluggable (unlike TCP, where the kernel's CC algorithm is fixed system-wide). The default is CUBIC (same as TCP), but Cloudflare and Google have deployed custom CC algorithms tuned for their networks. **BBR** (Bottleneck Bandwidth and Round-trip propagation time) models the bottleneck bandwidth and RTT explicitly rather than reacting to packet loss, providing higher throughput on high-bandwidth-delay-product networks (long-fat pipes, satellite). QUIC's userspace implementation also enables faster iteration on CC algorithms without kernel updates.",
            "**HTTP/3 adoption and infrastructure**: as of 2024, HTTP/3 is supported by all major browsers and by Cloudflare, Google, and Fastly (which termination it at the edge and proxy HTTP/2 or HTTP/1.1 to origin servers). Server-side implementations: `nginx` (via the `quiche` module), `caddy` (built-in), `h2o`. The primary deployment model: the CDN handles HTTP/3 with users, and the CDN-to-origin connection is HTTP/2 (eliminating the need to upgrade origin servers). The `Alt-Svc` HTTP response header (`Alt-Svc: h3=':443'; ma=86400`) advertises HTTP/3 support to clients that connected via HTTP/1.1 or HTTP/2 - the client upgrades to HTTP/3 on the next connection. QUIC uses UDP port 443; enterprise firewalls that block UDP may prevent HTTP/3, causing the browser to fall back to HTTP/2.",
          ],
          resources: [
            { label: "Cloudflare - HTTP/3: the past, the present, and the future", url: "https://blog.cloudflare.com/http3-the-past-present-and-future/", type: "docs" },
            { label: "QUIC RFC 9000 (the spec)", url: "https://www.rfc-editor.org/rfc/rfc9000", type: "paper" },
            { label: "High Performance Browser Networking - QUIC chapter (free)", url: "https://hpbn.co/quic/", type: "book" },
          ],
        },
        {
          title: "WebAssembly: linear memory, WASI, and the compilation toolchain",
          body: [
            "**WebAssembly (WASM)** is a binary instruction format for a stack-based virtual machine. It is not JavaScript - it is a compilation target for languages like C, C++, Rust, Go, and Zig that compiles to a compact binary format that the browser's WASM engine executes. The goals: near-native performance for compute-intensive tasks in the browser, and a universal, sandboxed runtime for executing untrusted code safely. WASM is now also a server-side runtime format (WASI, Cloudflare Workers, Fastly Compute), positioning it as 'the new Docker' for portable, sandboxed server computation.",
            "**WASM's memory model**: each WASM module has a **linear memory** - a flat, byte-addressable block of memory starting at address 0. It is like an `ArrayBuffer` visible from both the WASM code and JavaScript (as a `WebAssembly.Memory` object). C/Rust/C++ code compiled to WASM uses this linear memory for its heap (managed by `malloc`/`free` in C, Rust's allocator, etc.), stack frames, and global variables. There is no garbage collector in the WASM spec itself (the GC proposal adds references but is opt-in). JavaScript and WASM share memory through the linear memory buffer: pass data by writing into the memory buffer, then passing the pointer (an integer offset) to the WASM function. Strings require encoding/decoding across the JS-WASM boundary - a common source of overhead that the **Component Model** proposal addresses with typed interfaces.",
            "**Compilation toolchain for C/C++**: `emscripten` is the standard. It wraps LLVM (using the `wasm32-unknown-emscripten` target) and provides a JavaScript 'glue' file that initializes the WASM module, handles memory management, and polyfills POSIX APIs (file system via a virtual FS in memory/IndexedDB, networking via WebSockets). For pure computation (no I/O), you can compile a C library directly: `emcc math_lib.c -o math_lib.wasm -s SIDE_MODULE=1`. **Rust to WASM** via `wasm-pack` + `wasm-bindgen` is cleaner: `wasm-bindgen` generates JavaScript bindings for exported Rust functions, handling type conversions for strings, arrays, and JavaScript objects. The resulting package is usable as a normal npm module.",
            "**WASI (WebAssembly System Interface)** is the standard system interface for WASM outside the browser. Where the browser provides DOM and Web APIs, WASI provides POSIX-like system calls: file I/O, networking, clock, randomness, environment variables - implemented as WASM host functions. The key property: **capability-based security** - a WASM module only has access to the filesystem paths and network sockets explicitly passed to it by the host, regardless of what its source code attempts. This makes WASM+WASI an excellent sandboxed execution environment for plugins, serverless functions, and user-defined computation. Runtimes: `wasmtime` (Bytecode Alliance), `wasmer`, `WasmEdge`. The comparison to containers: WASM starts faster (~microseconds vs hundreds of milliseconds), is more portable (no Linux kernel dependency), and is more sandboxed (no host OS calls except those explicitly allowed).",
            "**WASM in the browser - real use cases**: image/video processing (FFmpeg compiled to WASM, used by Figma, Zoom, Squoosh), codec decoding (AV1, HEVC without native support), PDF rendering (PDF.js moved some operations to WASM for speed), scientific computation (NumPy for the web via Pyodide - CPython compiled to WASM), and game engines (Unity, Godot export to WASM). Performance characteristics: WASM can achieve 1.5-2x native speed for compute-bound workloads (optimization headroom due to missing SIMD in early versions, now partially addressed by the **SIMD proposal** - 128-bit SIMD instructions available in all browsers since 2021). The overhead is primarily at the JS-WASM boundary (function calls, memory passing). **Threads in WASM** are built on `SharedArrayBuffer` and `Atomics` - multi-threaded WASM requires cross-origin isolation headers on the page.",
          ],
          resources: [
            { label: "MDN - WebAssembly concepts", url: "https://developer.mozilla.org/en-US/docs/WebAssembly/Concepts", type: "docs" },
            { label: "wasm-bindgen - Rust-to-WASM guide", url: "https://rustwasm.github.io/docs/wasm-bindgen/", type: "docs" },
            { label: "Bytecode Alliance - WASI spec and runtime", url: "https://wasi.dev/", type: "docs" },
            { label: "Lin Clark - WebAssembly: Neither fast nor slow (nuanced perf analysis)", url: "https://www.youtube.com/watch?v=pT_VzGHBgL0", type: "video" },
          ],
        },
        {
          title: "Edge computing: Cloudflare Workers, Durable Objects, and V8 isolates",
          body: [
            "**Edge computing** moves computation from centralized origin servers to the CDN edge - physically close to the user. Rather than a request traveling to a datacenter 200ms away, it is handled at a CDN PoP a few milliseconds away. Edge workers are small, fast-starting JavaScript (or WASM) programs that run at hundreds of locations worldwide. They have access to: the incoming request and full response control, the CDN cache, key-value stores, databases at the edge, and the ability to fetch from origin or other services.",
            "**Cloudflare Workers** use **V8 isolates** as their execution model, not separate processes or VMs. A V8 isolate is a lightweight, isolated JavaScript heap and context. It can be created in microseconds (vs hundreds of milliseconds for a container or process). Each Worker invocation runs in an isolate that is sandboxed from other Workers - no shared memory, no shared global state. After the request completes, the isolate can be reused for the next request to the same Worker (warm start) or discarded. The key advantage over Lambda functions: cold starts are measured in microseconds (isolate creation), not hundreds of milliseconds (container initialization). This makes Cloudflare Workers genuinely serverless - no idle warm-up tricks required.",
            "**Workers' limitations** reflect the isolate model: no `setTimeout` or `setInterval` (replaced by `waitUntil` for background tasks), no Node.js built-ins (no `fs`, `path`, `crypto` - though the Web Crypto API is available), no shared state between requests (isolates are independent). The maximum CPU time per request is 50ms in free tier, up to 30 seconds with the paid tier. Network calls (`fetch`) are async and counted against wall clock time, not CPU time. Workers communicate with each other via service bindings (direct invocation, zero-latency) or via KV/Durable Objects.",
            "**Cloudflare KV** is an eventually-consistent, globally-distributed key-value store. Writes propagate to all edge locations within ~60 seconds. Reads are cached at each edge location - extremely fast (sub-millisecond) but potentially serving slightly stale data. Perfect for: configuration, feature flags, public data that changes infrequently, cached API responses. Not suitable for: counters (concurrent writes overwrite each other), inventory (read-modify-write requires consistency). **Durable Objects** solve the strong consistency problem. Each Durable Object is a single-instance, globally unique, stateful object that runs in exactly one V8 isolate at a time. It has a transactional storage API (`storage.get`, `storage.put` with ACID semantics within the object). All requests to a given Durable Object are serialized through that single instance - providing strong consistency for the object's state, while still running at Cloudflare's edge (the instance is placed near the majority of requesters). Used for: collaborative document editing, chat rooms, rate limiters, distributed locks, game lobbies - any use case requiring strong consistency over a specific unit of data.",
            "**The edge computing model vs traditional serverless**: Lambda (AWS) runs a container per invocation, starts cold containers in 100-1000ms, and has the full Node.js runtime. Workers run isolates per invocation, cold start in <1ms, but have a restricted API surface (browser-like APIs, not Node). The design philosophy of Workers is to embrace the web platform APIs (fetch, Web Crypto, streams, URL, Cache API) and reject Node.js's historical OS-centric API set. This is intentional: Web APIs are portable across browser, server, and edge; Node APIs are not. The trend: **WinterCG** (Web-interoperable Runtimes Community Group) standardizes the shared Web API subset that all runtimes (Node, Deno, Bun, Cloudflare, Fastly, Vercel) should implement identically - making edge-deployable code a first-class concern in the JS ecosystem.",
          ],
          resources: [
            { label: "Cloudflare Workers - documentation", url: "https://developers.cloudflare.com/workers/", type: "docs" },
            { label: "Cloudflare - Durable Objects documentation", url: "https://developers.cloudflare.com/durable-objects/", type: "docs" },
            { label: "WinterCG - Web-interoperable Runtimes Community Group", url: "https://wintercg.org/", type: "docs" },
          ],
        },
      ],
      resources: [
        { label: "V8 blog (engine internals)", url: "https://v8.dev/blog", type: "docs" },
        { label: "MDN - WebAssembly", url: "https://developer.mozilla.org/en-US/docs/WebAssembly", type: "docs" },
        { label: "Cloudflare blog - edge & QUIC", url: "https://blog.cloudflare.com/", type: "docs" },
      ],
      connections: [
        { to: "ll-rust", note: "WASM's sweet spot is shipping compiled Rust/C to the browser" },
        { to: "comp-llvm", note: "WASM is an LLVM backend target; the toolchain is compiler tech" },
        { to: "game-gpu-api", note: "WebGPU brings Vulkan-style GPU access to the browser" },
      ],
    },
  ],
});

atlasAdd({
  id: "aiml",
  topics: [
    {
      id: "ai-mlops",
      subtopics: [
        {
          title: "Experiment Tracking, Model Registry, and Reproducibility",
          body: [
            "**Experiment tracking** solves the fundamental ML research problem: you run hundreds of experiments, and without systematic logging you cannot reproduce results or understand what worked. **Weights & Biases (W&B)** and **MLflow** are the dominant platforms. At minimum, every run should log: hyperparameters (learning rate, batch size, model architecture), training metrics (loss, accuracy per step), system metrics (GPU utilisation, memory), and model checkpoints at best/last validation performance. W&B additionally logs gradient histograms, parameter norms, sample predictions, and hardware stats automatically with `wandb.init()` and `wandb.log()`.",
            "**Artifact versioning**: track datasets, model weights, and evaluation outputs as versioned artifacts. W&B Artifacts and MLflow Model Registry provide content-addressed storage (immutable once logged) with lineage tracking (which dataset produced which model). This is the ML equivalent of git for code — you can always reproduce any prior experiment by replaying the exact data version + hyperparameters + code commit. **Hydra** (Facebook) and **Weights & Biases sweeps** provide hyperparameter sweep orchestration: define the search space (grid, random, or Bayesian) and the platform launches and tracks all runs.",
            "**Reproducibility** in ML requires controlling: (1) **random seeds** (Python `random`, NumPy, PyTorch — all must be seeded consistently); (2) **CUDA non-determinism** (`torch.use_deterministic_algorithms(True)` + `CUBLAS_WORKSPACE_CONFIG=:4096:8` env var — some ops are non-deterministic by default for speed); (3) **data ordering** (shuffle seed + deterministic dataloaders); (4) **environment** (Docker image with pinned package versions, or Nix flake). The notorious reproducibility crisis in ML arises from: unpinned dependencies (library updates change results), non-deterministic GPU ops, and missing seeds. **Checksums on dataset and preprocessing** catches silent data corruption.",
            "**Distributed training reproducibility**: when using DDP or FSDP, gradient all-reduces introduce floating-point non-associativity — $a + (b + c) \\neq (a + b) + c$ in floating-point. With different ordering of reductions across GPUs, the same run can give slightly different results even with the same seeds. This is an irreducible source of non-reproducibility in large-scale training. The practical advice: report mean ± std over multiple seeds for fair comparison; consider different GPU counts as different 'methods' since they may give meaningfully different results.",
            "**Model Registry and deployment lifecycle**: after training, models enter a registry where they are versioned and staged (Staging → Production → Archived). MLflow Model Registry and W&B Model Registry support this. Key metadata per version: source experiment run, performance metrics on canonical eval sets, size/latency benchmarks, associated datasets. **Shadow deployments** route a copy of production traffic to the new model without serving its responses to users — compare predictions against the current production model to catch regressions before cutover. **A/B testing** (gradual rollout to $X\%$ of traffic) measures real-world impact.",
          ],
          resources: [
            { label: "Weights & Biases guides and documentation", url: "https://docs.wandb.ai/", type: "docs" },
            { label: "MLflow tracking and model registry docs", url: "https://mlflow.org/docs/latest/tracking.html", type: "docs" },
            { label: "Made With ML — MLOps course (free, end-to-end)", url: "https://madewithml.com/", type: "course" },
          ],
        },
        {
          title: "LLM Evaluation: Perplexity, Benchmarks, and LLM-as-Judge",
          body: [
            "**Perplexity (PPL)** is the primary intrinsic LLM evaluation metric: $\\text{PPL}(\\mathcal{D}) = \\exp\\left(-\\frac{1}{N}\\sum_{i=1}^N \\log P_\\theta(x_i|x_{<i})\\right)$. It measures how well the model predicts the held-out test data — lower is better. PPL is a strict function of token-level cross-entropy and is comparable across models only if they use the **same tokenizer** (different tokenisers produce different token counts for the same text, making raw PPL incomparable). **Bits-per-byte (BPB)** or **bits-per-character (BPC)** normalise by the number of bytes/characters, enabling cross-tokeniser comparison. PPL correlates with downstream task performance but is a poor predictor of instruction-following quality.",
            "**Benchmark suites**: **MMLU (Massive Multitask Language Understanding)** — 57 subjects from STEM to law, 5-shot multiple choice, tests breadth of knowledge. **HellaSwag** — commonsense NLI, requires world knowledge to complete sentence endings. **TruthfulQA** — measures tendency to generate false information for questions where humans have common misconceptions. **GSM8K** — grade-school math word problems requiring multi-step arithmetic. **HumanEval** — Python coding problems with unit test verification (pass@k metric: probability that at least one of k samples passes all tests). **MATH** — competition math, requires LaTeX-formatted proofs and computation. **GPQA** — PhD-level science questions designed to be hard for Google, easy for domain experts.",
            "**Contamination** is the benchmark validity crisis: if training data contains the benchmark examples or semantically similar ones, performance is inflated. Dataset contamination is rampant — CommonCrawl (used in most pre-training) contains scrapes of test sets. Mitigation: (a) use held-out benchmarks not available at training time; (b) decontaminate training data by n-gram matching against benchmark examples; (c) use **process-based evaluation** (grade the reasoning process, not just the final answer) — harder to contaminate. The **ARC** (AI2 Reasoning Challenge) and **WinoGrande** were specifically designed to be difficult to contaminate via crowdsourcing anti-dataset tricks.",
            "**LLM-as-Judge** (Zheng et al., 2023) uses a strong LLM (GPT-4, Claude) to evaluate model outputs on open-ended tasks where automatic metrics fail (creative writing, instruction following, reasoning quality). The judge LLM is prompted to compare two model outputs or score on a rubric (1-10 scale for helpfulness, correctness, coherence). **MT-Bench** is a 2-turn dialogue benchmark with GPT-4 as judge. LLM-as-Judge issues: (a) **position bias** — LLMs prefer responses listed first; mitigate with swapping order and averaging; (b) **verbosity bias** — longer responses rated higher; (c) **self-enhancement bias** — a model rates its own outputs higher. **Chatbot Arena** (LMSYS) uses **Elo rating** via crowd-sourced pairwise comparisons — the most reliable real-world quality signal, but expensive and slow.",
            "**RAG evaluation**: standard metrics for RAG pipelines include **faithfulness** (do the generated answers cite facts present in retrieved documents?), **answer relevance** (do answers address the question?), and **context relevance** (are retrieved documents relevant?). **RAGAS** (Es et al., 2023) is a framework that uses LLM-generated synthetic QA pairs to evaluate these three axes without human annotation. **RAG-specific retrieval metrics**: **Hit Rate** (fraction of queries where the correct document is in the top-$k$ retrieved), **MRR (Mean Reciprocal Rank)** $= \\frac{1}{|Q|}\\sum_q 1/\\text{rank}_q$, **NDCG@k** (Normalised Discounted Cumulative Gain, accounts for rank position of relevant documents).",
          ],
          resources: [
            { label: "MMLU benchmark — Hendrycks et al. (2020)", url: "https://arxiv.org/abs/2009.03300", type: "paper" },
            { label: "MT-Bench and LLM-as-Judge — Zheng et al. (2023)", url: "https://arxiv.org/abs/2306.05685", type: "paper" },
            { label: "Chatbot Arena — LMSYS (live leaderboard)", url: "https://lmarena.ai/", type: "tool" },
          ],
        },
        {
          title: "RAG: Retrieval-Augmented Generation Architecture",
          body: [
            "**RAG (Retrieval-Augmented Generation)** (Lewis et al., 2020) grounds LLM outputs in external knowledge by retrieving relevant documents and including them in the context. The standard pipeline: (1) **Indexing**: chunk the knowledge corpus into $\\sim 512$ token segments; embed each chunk with an **embedding model** ($\\text{embed}: \\mathbb{R}^{\\text{tokens}} \\to \\mathbb{R}^d$); store embeddings in a vector database with the original text. (2) **Retrieval**: embed the user query; find the top-$k$ chunks by vector similarity (cosine or dot product). (3) **Generation**: include retrieved chunks in the LLM prompt as context; generate the answer grounded in them. The original RAG paper used DPR (Dense Passage Retrieval) for retrieval and BART for generation, jointly trained.",
            "**Embedding models**: convert variable-length text to fixed-dimension vectors ($d = 384$ to $4096$) that capture semantic similarity. SBERT (Sentence-BERT) uses a Siamese network with a contrastive loss — similar texts produce nearby embeddings. **Contrastive pre-training** on (query, relevant passage) pairs (e.g., MS MARCO) trains the encoder to maximise similarity for positives and minimise for negatives: $\\mathcal{L} = -\\log \\frac{e^{s(q, p^+)/\\tau}}{e^{s(q, p^+)/\\tau} + \\sum_{i} e^{s(q, p_i^-)/\\tau}}$ (InfoNCE loss). **OpenAI text-embedding-3-large** ($d=3072$), **E5-Mistral-7B** (a full LLM fine-tuned for embedding), and **BAAI/bge** are the dominant open-source embedding models. Embedding quality is benchmarked on **MTEB (Massive Text Embedding Benchmark)**.",
            "**Vector databases**: **FAISS** (Facebook AI Similarity Search) provides GPU-accelerated approximate nearest-neighbour search using HNSW (Hierarchical Navigable Small World) graphs or IVF (Inverted File) indexing. **pgvector** (PostgreSQL extension) enables vector search inside a relational DB. **Chroma, Qdrant, Weaviate, Pinecone** are managed vector DB services with metadata filtering, hybrid search (dense + sparse), and managed infrastructure. **HNSW algorithm** builds a layered graph where each layer is a sparse subset of the full graph — search starts at the top (coarsest) layer and follows edges greedily toward the query, then descends to finer layers. Query time: $O(\\log N)$ for $N$ vectors; build time: $O(N \\log N)$; space: $O(N)$.",
            "**Advanced RAG techniques**: **HyDE (Hypothetical Document Embeddings)** (Gao et al., 2022): generate a hypothetical answer with the LLM, embed it (rather than the query), and retrieve based on the answer embedding — often matches the embedding distribution of real answers better than question embeddings. **Multi-query retrieval**: generate multiple phrasings of the query with the LLM, retrieve for each, deduplicate — improves recall for ambiguous queries. **Contextual retrieval** (Anthropic, 2024): prepend each chunk with LLM-generated context ('This chunk is from Chapter 3 of X, discussing the mechanism of Y') before indexing — improves retrieval precision by $49\%$ on tested benchmarks. **Re-ranking**: after retrieving top-$k=20$ chunks by embedding similarity, re-rank with a **cross-encoder** (a BERT-style model that takes (query, chunk) as input and outputs a relevance score) to select the top-$k'=5$ for the context window. Cross-encoders are more accurate than bi-encoders (they see both query and passage) but too slow for full-corpus retrieval.",
            "**GraphRAG** (Edge et al., 2024) builds a knowledge graph from the corpus (entity and relation extraction via LLM), and augments retrieval with graph traversal. Community detection identifies clusters of related entities; each community is summarised at multiple levels. At query time, local search retrieves relevant entities and their 1-2 hop subgraph; global search uses pre-built community summaries for dataset-spanning questions. This dramatically outperforms naive RAG for questions that require synthesising information across many documents (e.g., 'What are the main themes in this 10,000-document dataset?'). The limitation: building the knowledge graph is expensive ($\\sim \\$50$ per 1M tokens for GPT-4-based extraction).",
          ],
          resources: [
            { label: "RAG foundational paper — Lewis et al. (2020)", url: "https://arxiv.org/abs/2005.11401", type: "paper" },
            { label: "RAG Techniques repository — NirDiamant (200+ techniques)", url: "https://github.com/NirDiamant/RAG_Techniques", type: "repo" },
            { label: "vLLM documentation", url: "https://docs.vllm.ai/", type: "docs" },
          ],
        },
        {
          title: "Serving Infrastructure: Triton, vLLM, and Deployment Quantisation",
          body: [
            "**ONNX (Open Neural Network Exchange)** is a hardware-agnostic IR for neural networks — export from PyTorch/JAX, run on any backend (TensorRT, OpenVINO, TVM, ONNX Runtime). The ONNX export converts the PyTorch computation graph into a static graph with explicit operator names from the ONNX operator set. **TorchScript** is PyTorch's built-in JIT: `torch.jit.trace` records operations on example inputs; `torch.jit.script` interprets Python code directly. Both produce serialisable models executable in C++ or on edge devices. `torch.compile` (PyTorch 2.0) provides a Python-level compilation path using Dynamo (bytecode capture) + Inductor (code generation) — typically $1.5$-$3\\times$ faster than eager mode.",
            "**Triton Inference Server** (NVIDIA) is a production serving framework supporting all major backends (PyTorch, TensorRT, ONNX, TensorFlow), dynamic batching, model ensemble, gRPC/HTTP APIs, and GPU utilisation metrics. **Dynamic batching**: accumulate requests within a configurable latency window and batch them together — critical for GPU throughput since single-request inference is memory-bandwidth bound (low FLOP:byte ratio). The `model_config.pbtxt` specifies batching policy, preferred batch sizes, and max batch size.",
            "**TensorRT** (NVIDIA) compiles a neural network to optimised GPU kernels: (1) **graph optimisation** — fuse adjacent ops (Conv+BN+ReLU → single kernel), eliminate dead nodes; (2) **kernel auto-selection** — profile all available CUDA kernel implementations for each op + shape combination and select the fastest; (3) **calibration** — for INT8, run a calibration dataset through the model to determine optimal quantisation scales per tensor. TensorRT typically gives $2$-$4\\times$ speedup over PyTorch on GPU for inference. The downside: TensorRT engines are CUDA compute capability specific (built for A100, runs only on A100), so recompilation is needed per hardware generation.",
            "**vLLM's PagedAttention** manages KV cache memory like an OS page table. Traditional LLM serving pre-allocates a contiguous chunk of GPU memory per request for the maximum possible sequence length — 99% of this is wasted (the actual output is much shorter than the max). PagedAttention uses **block-granularity** allocation: KV cache is divided into fixed-size blocks (16-32 tokens each); requests are assigned blocks as needed and return them when finished. The scheduler maintains a **block table** per request mapping logical block indices to physical blocks (analogous to virtual→physical page translation). This eliminates internal fragmentation and enables batch sizes $10$-$20\\times$ larger than reservation-based systems.",
            "**Serving at different hardware tiers**: a 7B FP16 model fits in a single A100-80GB. INT4 quantised (AWQ/GPTQ) fits on a 24GB consumer GPU (RTX 3090/4090) at ~$1.5$ tokens/sec in llama.cpp. A 70B model requires 2-4 A100s in tensor parallelism for 20-40 tokens/sec serving. Scaling to $1000$ requests/sec requires $N \\approx \\text{RPS} \\times \\text{latency} / \\text{batch size} \\times \\text{GPU per batch}$ GPUs (Little's Law). The latency-throughput tradeoff: small batch sizes give low latency (single request: 50ms/tok) but low throughput; large batches improve throughput ($200$ tokens/sec/GPU) but increase latency (wait time for batch formation). Continuous batching (used by vLLM, TGI) dynamically adjusts batch composition to balance both.",
          ],
          resources: [
            { label: "vLLM — PagedAttention paper (2023)", url: "https://arxiv.org/abs/2309.06180", type: "paper" },
            { label: "torch.compile paper — PyTorch 2.0", url: "https://docs.pytorch.org/assets/pytorch2-2.pdf", type: "docs" },
            { label: "NVIDIA Triton Inference Server documentation", url: "https://docs.nvidia.com/deeplearning/triton-inference-server/user-guide/docs/index.html", type: "docs" },
          ],
        },
      ],
    },
  ],
});

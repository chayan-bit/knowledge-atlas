atlasAdd({
  id: "aiml",
  topics: [
    {
      id: "ai-cv",
      subtopics: [
        {
          title: "Convolutional Neural Networks: Convolution, Receptive Fields, and Pooling",
          body: [
            "**Convolution** in CNNs is a 2D sliding-window dot product between a learnable **filter** (kernel) $W \\in \\mathbb{R}^{k \\times k \\times C_{\\text{in}}}$ and the input feature map $X \\in \\mathbb{R}^{H \\times W \\times C_{\\text{in}}}$: $Z_{i,j} = \\sum_{p=0}^{k-1}\\sum_{q=0}^{k-1}\\sum_{c=1}^{C_{\\text{in}}} W_{p,q,c} \\cdot X_{i+p, j+q, c} + b$. With $C_{\\text{out}}$ filters, the output is $Z \\in \\mathbb{R}^{H' \\times W' \\times C_{\\text{out}}}$ where $H' = (H - k + 2P)/S + 1$ for padding $P$ and stride $S$. A convolutional layer has $k^2 \\cdot C_{\\text{in}} \\cdot C_{\\text{out}}$ parameters — independent of spatial resolution, enabling **parameter sharing**: the same filter detects the same feature (e.g., an edge) anywhere in the image, encoding translation equivariance as an inductive bias.",
            "The **receptive field** of a neuron is the region of the input image that can influence its activation. For a single $k\\times k$ conv layer, the receptive field is $k \\times k$. Stacking $L$ convolutional layers (each with $k=3$, stride 1) gives a receptive field of $(2L+1) \\times (2L+1)$ — linear growth with depth. **Dilated (atrous) convolution** with dilation $d$ samples the filter at intervals of $d$ pixels, achieving an effective receptive field of $(k + (k-1)(d-1))^2$ without increasing parameters. This is why dilated convolutions are used in segmentation networks (DeepLab) and WaveNet: large receptive field at low parameter cost. **Depth-wise separable convolution** (used in MobileNet) factorises a standard $k\\times k$ conv into a **depth-wise** conv (one filter per channel: $k^2 \\cdot C_{\\text{in}}$ params) followed by a **point-wise** $1\\times 1$ conv ($C_{\\text{in}} \\cdot C_{\\text{out}}$ params) — $8-9\\times$ fewer operations than the original.",
            "**Padding** is not cosmetic — it is a design decision with semantic consequences. 'Same' padding (pad with $\\lfloor k/2 \\rfloor$ zeros) preserves spatial resolution ($H' = H$) and treats all positions equally. 'Valid' padding (no padding) reduces spatial dimensions at each layer. The paper *Translational Equivariance of CNNs is due to Padding* (Alsallakh et al., 2021) proved that implicit position encoding in CNNs comes almost entirely from zero-padding at borders — the network learns to detect 'closeness to border' from padding patterns. This means CNNs with zero-padding are *not* truly translation equivariant despite the shared-filter design; they implicitly encode absolute position.",
            "**Pooling** reduces spatial dimensions, providing local translation invariance and reducing computational load. **Max pooling** with window $k \\times k$ and stride $k$ retains the maximum activation in each window: coarse-grains the feature map by $k\\times$ in each dimension. **Average pooling** takes the mean — smoother but less discriminative. **Global average pooling (GAP)** collapses the entire $H \\times W$ spatial map to a single value per channel, producing a $C_{\\text{out}}$-dimensional feature vector for classification. GAP replaced fully-connected layers in modern architectures (ResNet uses GAP before the final linear classifier), dramatically reducing parameters and overfitting. The downside of pooling: it is not differentiable (max) in the standard sense (subgradient exists), and it discards spatial arrangement information — motivating non-local operations and attention mechanisms in vision.",
            "**Modern ablation studies** reveal the relative importance of CNN design choices. The **ConvNeXt** paper (Liu et al., 2022) systematically 'modernised' a ResNet using techniques from vision transformers (large kernel $7\\times 7$, fewer normalisation ops, GELU activation, lower channel count with later downsampling) and matched ViT accuracy with fewer FLOPs — demonstrating that many transformer gains in vision are attributable to these architecture details rather than self-attention per se. Key lessons: kernel size $7\\times 7$ (large receptive field in early layers) matters; using **depthwise separable conv** at scale works; **pre-norm** (LayerNorm before conv) outperforms BatchNorm in the low-data regime.",
          ],
          resources: [
            { label: "CS231n — Convolutional Networks (Stanford, full notes)", url: "https://cs231n.github.io/convolutional-networks/", type: "docs" },
            { label: "Alsallakh et al. — Do CNNs Encode Data Augmentations? (padding paper 2021)", url: "https://arxiv.org/pdf/2101.12322", type: "paper" },
            { label: "Distill.pub — Feature Visualization (CNN internal representations)", url: "https://distill.pub/2017/feature-visualization/", type: "docs" },
          ],
        },
        {
          title: "ResNets and Skip Connections: Solving the Degradation Problem",
          body: [
            "Before ResNets, adding more layers consistently degraded training accuracy on deep networks — even on the training set. This **degradation problem** was not caused by overfitting (training error, not just test error, was higher for 56-layer vs 20-layer plain nets). He et al. (2015) hypothesised that the optimiser struggled to learn identity mappings: if the added layers need to learn to do nothing (pass through the input), it is hard to initialise them to near-identity and keep them there. The fix: make the identity the **default** by defining the residual mapping. Let $H(x)$ be the desired underlying mapping. Instead of letting the layer learn $H(x)$, let it learn the **residual** $F(x) = H(x) - x$. The layer output is then $H(x) = F(x) + x$, where $x$ is the shortcut connection.",
            "**The residual block**: $y = F(x, \\{W_i\\}) + x$ where $F$ is typically `BN→ReLU→Conv→BN→ReLU→Conv`. The skip connection $x$ is added element-wise (requiring same spatial dimension and channel count). When dimensions differ (downsampling or channel increase), a **projection shortcut** $W_s x$ (a $1\\times 1$ conv with stride 2 or changed channel count) is used instead. The bottleneck block (used in ResNet-50+) uses `1×1 conv → 3×3 conv → 1×1 conv` to reduce the parameter count while maintaining expressiveness: the first $1\\times 1$ projects to fewer channels (bottleneck), the $3\\times 3$ operates cheaply, the final $1\\times 1$ restores channel count.",
            "**Why do residual connections work?** Multiple explanations exist and are all partially correct. (1) **Gradient highway**: the skip connection provides a direct path for gradients back to early layers — the gradient of $y$ w.r.t. $x$ includes the term $\\partial F/\\partial x + I$, and even if $\\partial F/\\partial x \\approx 0$, the identity $I$ ensures gradient flow. (2) **Implicit ensemble**: Veit et al. (2016) showed that ResNets behave like exponential ensembles of shallower networks — removing a single layer changes only $O(2^{L-1})$ of the $2^L$ paths. (3) **Loss landscape smoothing**: Hao Li et al. (2018, 'Visualizing the Loss Landscape of Neural Nets') showed that residual connections dramatically smooth the loss landscape, enabling SGD to find better minima.",
            "**Architectural evolution**: ResNet (2015) → DenseNet (2016, connect each layer to all subsequent layers: $x_l = H_l([x_0, x_1, \\ldots, x_{l-1}])$, maximum feature reuse) → SENet (2017, Squeeze-and-Excitation: channel-wise attention by global-pooling-then-MLP-then-sigmoid, recalibrating channel importance) → EfficientNet (2019, compound scaling of depth, width, resolution via neural architecture search). The **compound scaling coefficient** $\\phi$ scales: depth $d = 1.2^\\phi$, width $w = 1.1^\\phi$, resolution $r = 1.15^\\phi$. EfficientNet-B7 matched state-of-the-art accuracy with $8.4\\times$ fewer parameters than GPT-2-scale CNNs at the time.",
            "**Vision Transformers (ViT)** (Dosovitskiy et al., 2020) replaced the convolutional inductive biases entirely: split the image into $16\\times 16$ patches, linearly embed each patch to a token, add positional embeddings, and run a standard transformer encoder. ViT requires very large datasets to match CNNs (the lack of translation equivariance must be learned from data) but scales better with compute. **DeiT** (Touvron et al., 2021) showed that ViT can be trained on ImageNet-1k alone via **knowledge distillation** from a CNN teacher. The behavioural difference: CNNs are texture-biased (shape/texture confound experiments show CNNs use texture features strongly), while ViTs are more shape-biased — ViTs generalise better across distribution shift.",
          ],
          resources: [
            { label: "ResNet paper — He et al. (2015)", url: "https://arxiv.org/abs/1512.03385", type: "paper" },
            { label: "ViT paper — An Image is Worth 16×16 Words (2020)", url: "https://arxiv.org/abs/2010.11929", type: "paper" },
            { label: "CNN vs ViT behavioural comparison (YouTube)", url: "https://youtu.be/KnCRTP11p5U?si=372rKyKy8tfi-sXM", type: "video" },
          ],
        },
        {
          title: "Object Detection: YOLO, Faster R-CNN, and DETR",
          body: [
            "Object detection simultaneously classifies objects and regresses bounding boxes. The **two-stage paradigm** (R-CNN family): (1) propose candidate regions with a region proposal network (RPN); (2) classify and refine each proposal with a separate head. **Faster R-CNN** (Ren et al., 2015) unified these with a shared CNN backbone: the RPN slides over the feature map, predicting **anchor** offsets and objectness scores. Anchors are pre-defined boxes of multiple scales and aspect ratios. The RPN loss is $\\mathcal{L} = \\mathcal{L}_{\\text{cls}}(p_i, p_i^*) + \\lambda p_i^* \\mathcal{L}_{\\text{reg}}(t_i, t_i^*)$ where $p_i^*$ is the GT label and $t_i$ are the 4 box parameters. The RoI pooling layer crops and resizes each proposal from the feature map to a fixed size for the classification head.",
            "**YOLO (You Only Look Once)** (Redmon et al., 2016) reframed detection as a **single regression problem** on a grid. The image is divided into an $S \\times S$ grid; each cell predicts $B$ bounding boxes (x, y, w, h, confidence) and $C$ class probabilities. The entire detection is one forward pass — hence 'single-stage'. This gives $45$ FPS at launch (Faster R-CNN: $5$ FPS). YOLO loss: $\\sum_{i,j}[\\mathbf{1}^{obj}((x_i-\\hat{x}_i)^2+(y_i-\\hat{y}_i)^2 + (\\sqrt{w_i}-\\sqrt{\\hat{w}_i})^2 + ...) + \\mathcal{L}_{\\text{cls}}]$. The $\\sqrt{w}$ term weights large box errors less (a 10px error on a 100px box is less important than on a 10px box). YOLOv5-YOLOv9 replaced the full-image grid with **anchor-free** heads and CSP (Cross-Stage Partial) backbones, reaching SOTA on COCO at real-time speeds.",
            "**IoU (Intersection over Union)** is the universal detection metric: $\\text{IoU}(A, B) = |A \\cap B| / |A \\cup B|$. A prediction is a true positive if $\\text{IoU} \\geq 0.5$ (PASCAL VOC) or matched at thresholds 0.5:0.05:0.95 (COCO, the harder protocol). **Mean Average Precision (mAP)** averages the area under the Precision-Recall curve across all classes and thresholds. The AP for one class: sort predictions by confidence, compute precision $P(k)$ and recall $R(k)$ at each rank $k$, then $AP = \\sum_k (R(k) - R(k-1)) P(k)$. mAP@0.5:0.95 is the standard COCO benchmark metric — harder than mAP@0.5 because it requires tight localisation.",
            "**DETR (Detection Transformer)** (Carion et al., 2020) eliminated anchors and NMS entirely. A CNN extracts features → flattened to a sequence → transformer encoder processes global context → transformer **decoder** produces $N=100$ output tokens (one per potential object), each attending to the encoder via cross-attention. Each output token predicts a (class, box) pair. Training uses **Hungarian matching**: compute the optimal bipartite assignment between the $N$ predictions and the $M$ ground truth objects (padding GT with 'no object' to $N$), minimising the bipartite matching cost. The Hungarian algorithm runs in $O(N^3)$ on this small problem. DETR was the first end-to-end trainable detector with no hand-designed components, but trained slowly (500 epochs for COCO convergence). **Deformable DETR** added sparse deformable attention (each query attends to only $K$ key points per feature map level) for $10\\times$ faster convergence.",
            "**Non-Maximum Suppression (NMS)** is the post-processing step for anchor-based detectors: given overlapping high-confidence predictions for the same object, keep only the highest-confidence box and suppress all others with $\\text{IoU} > \\theta_{\\text{NMS}}$. Greedy NMS is $O(n^2)$ in the number of predictions. **Soft-NMS** decays the scores of overlapping boxes rather than removing them — better for dense scenes. NMS is the main reason anchor-based detectors are not end-to-end trainable (it is not differentiable). DETR's elimination of NMS is therefore architecturally significant.",
          ],
          resources: [
            { label: "Faster R-CNN — Ren et al. (2015)", url: "https://arxiv.org/abs/1506.01497", type: "paper" },
            { label: "YOLO — You Only Look Once (Redmon et al., 2016)", url: "https://arxiv.org/abs/1506.02640", type: "paper" },
            { label: "DETR — End-to-End Object Detection with Transformers (2020)", url: "https://arxiv.org/abs/2005.12872", type: "paper" },
          ],
        },
        {
          title: "Segmentation: U-Net, Mask R-CNN, and SAM",
          body: [
            "**Semantic segmentation** assigns a class label to every pixel. The naive approach (classify each pixel independently) ignores spatial context. CNNs produce coarse feature maps (downsampled by pooling/striding) — you need to upsample back to full resolution. **Fully Convolutional Networks (FCN)** (Long et al., 2015) replaced FC layers with $1\\times 1$ convolutions (output per-pixel predictions) and used **bilinear upsampling** or **transposed convolutions** (learnable upsampling) to recover resolution. The key insight: the same weights that classify an image can localise the class by operating as a dense predictor.",
            "**U-Net** (Ronneberger et al., 2015) introduced the **encoder-decoder with skip connections** that is now ubiquitous. The encoder (contracting path) progressively halves spatial dimensions and doubles channels via max-pool + conv. The decoder (expanding path) upsamples with transposed conv, then concatenates with the corresponding encoder feature map (the skip connection). The skip connection provides **fine-grained spatial detail** from early layers to counteract the spatial information lost by pooling. U-Net was originally designed for biomedical image segmentation (where training data is scarce) — the symmetric encoder-decoder with skip connections enabled training on fewer than 100 images via aggressive augmentation. It has since become the backbone of diffusion models' denoisers.",
            "**Instance segmentation** distinguishes individual object instances (two cats are two separate masks, not just 'cat everywhere'). **Mask R-CNN** (He et al., 2017) extends Faster R-CNN with a third branch: after RoI alignment (bilinear interpolation version of RoI pooling, avoiding quantisation artefacts), a small FCN per-RoI predicts a $28 \\times 28$ binary mask for each class. The mask branch is decoupled from the class branch: the classification head determines the class, and the mask of that class is selected — this avoids competition between class prediction and mask quality. **RoI Align** replaces integer grid snapping in RoI Pooling with bilinear interpolation at exact sub-pixel coordinates, improving mask AP by $\\sim 3\\%$.",
            "**Panoptic segmentation** merges semantic (background classes: sky, grass) and instance (foreground: person, car) segmentation into a single prediction. Every pixel gets a $(\\text{class}, \\text{instance\\_id})$ label; background pixels share instance ID 0. The **Panoptic Quality (PQ)** metric is $\\text{PQ} = \\text{SQ} \\times \\text{RQ}$ where Segmentation Quality $\\text{SQ} = \\frac{\\sum_{(p,g)\\in TP} \\text{IoU}(p,g)}{|TP|}$ and Recognition Quality $\\text{RQ} = \\frac{|TP|}{|TP| + \\frac{1}{2}|FP| + \\frac{1}{2}|FN|}$. PQ factorises recognition quality from localisation quality.",
            "**SAM (Segment Anything Model)** (Kirillov et al., 2023) redefined segmentation as a **promptable** task: given a point, box, or text prompt, segment the corresponding object. SAM was trained on 1.1 billion masks from SA-1B, a new dataset built by a model-in-the-loop annotation pipeline. Architecture: a ViT image encoder + prompt encoder + lightweight mask decoder (two transformer layers + upsampling convolutions). SAM generalises zero-shot to novel domains (microscopy, satellite, art) without fine-tuning. The key design: ambiguous prompts produce multiple mask candidates (coarse/medium/fine) with confidence scores, enabling the user to pick. **SAM 2** (2024) extends to video: a memory bank propagates mask tokens across frames, enabling real-time interactive video segmentation.",
          ],
          resources: [
            { label: "U-Net — Ronneberger et al. (2015)", url: "https://arxiv.org/abs/1505.04597", type: "paper" },
            { label: "Mask R-CNN — He et al. (2017)", url: "https://arxiv.org/abs/1703.06870", type: "paper" },
            { label: "SAM — Segment Anything (Kirillov et al., 2023)", url: "https://arxiv.org/abs/2304.02643", type: "paper" },
            { label: "Image Segmentation survey — 2024", url: "https://arxiv.org/pdf/2408.12957", type: "paper" },
          ],
        },
      ],
    },
  ],
});

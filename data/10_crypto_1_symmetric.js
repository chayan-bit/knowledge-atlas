atlasAdd({
  id: "crypto",
  topics: [
    {
      id: "crypto-symmetric",
      title: "Symmetric crypto, hashing & MACs",
      level: "Beginner",
      body: [
        "**Kerckhoffs's principle**, AES as a substitution-permutation network, **block-cipher modes** (ECB's penguin failure, CBC + padding oracles, CTR, **GCM/AEAD**), hash functions (SHA-256 Merkle-Damgård, SHA-3 Keccak sponge, BLAKE3) and their three resistance properties, **MACs** (HMAC — why not `H(key‖msg)` due to length extension), and password hashing (bcrypt/scrypt/**Argon2id**).",
        "Cryptopals Sets 1-2 — implement these *and* break them — is the fastest route to real understanding.",
      ],
      subtopics: [
        {
          title: "Block ciphers and the AES substitution-permutation network",
          body: [
            "A **block cipher** takes a fixed-length block of plaintext (128 bits for AES) and a key, and produces a ciphertext block of the same length. The cipher must be a **bijection** for each key — every plaintext maps to a unique ciphertext, and vice versa — so that decryption exists. The key is the only secret; per **Kerckhoffs's principle**, the algorithm itself must be assumed public. This is not a concession but a design discipline: a cipher that relies on secrecy of the algorithm is weaker (once the algorithm leaks, everything encrypted with it is broken) and harder to audit. Any cipher you cannot publish in a journal is not a cipher you should trust.",
            "AES (Advanced Encryption Standard, standardized by NIST in 2001 after the Rijndael submission won an open competition) is a **substitution-permutation network (SPN)**. The core intuition is that security comes from iterating two complementary operations many times: **substitution** (non-linear mixing that destroys algebraic structure) and **permutation** (linear diffusion that spreads influence across all bits). Neither alone is secure — substitution without diffusion leaks positional statistics; diffusion without non-linearity is linear algebra and trivially invertible. Alternating the two, many rounds, gives a design where every output bit depends on every input bit in a complex, non-linear way.",
            "Concretely, AES operates on a **4×4 byte state array** (16 bytes = 128-bit block). Each of 10/12/14 rounds (for 128/192/256-bit keys) applies four transformations: **SubBytes** replaces each byte through a fixed 8-bit S-box (the `GF(2⁸)` multiplicative inverse, chosen for maximally bad differential/linear properties from an attacker's view); **ShiftRows** rotates each row of the state by 0, 1, 2, 3 bytes cyclically (ensuring bytes from different columns interact in subsequent rounds); **MixColumns** multiplies each column by a fixed matrix over `GF(2⁸)` (providing diffusion — each output byte depends on all four input bytes of the column); and **AddRoundKey** XORs the state with a 128-bit subkey derived from the key schedule. The final round omits MixColumns.",
            "The **key schedule** expands a 128/192/256-bit master key into 11/13/15 round keys, also using SubBytes and XOR operations, so the round keys have similar mixing properties. AES has no known practical attack faster than brute force — the best known attack on AES-128 reduces the work from 2¹²⁸ to roughly 2¹²⁶ (a marginal improvement with no practical consequence). What breaks AES in practice is always the **mode of operation** or the **implementation** (timing leaks, nonce reuse), not the round function itself. The S-box, the diffusion layer, and the round count were designed with differential and linear cryptanalysis in mind — and that rigor has held for 25 years.",
            "One nuance worth internalizing: a block cipher alone encrypts exactly one block and is **deterministic** — encrypting the same plaintext with the same key always gives the same ciphertext. This is useless for most real purposes, where messages are longer than 16 bytes and you need the encryption to look different each time. That is what modes of operation fix. The block cipher is a keyed pseudo-random permutation; the mode builds a useful encryption scheme on top.",
          ],
          resources: [
            { label: "NIST FIPS 197 — AES standard (the exact specification)", url: "https://csrc.nist.gov/publications/detail/fips/197/final", type: "docs" },
            { label: "Serious Cryptography ch. 4 — Block Ciphers (Aumasson)", url: "https://nostarch.com/seriouscrypto", type: "book" },
          ],
        },
        {
          title: "Block cipher modes — ECB failure, CBC, CTR, and AEAD",
          body: [
            "A **mode of operation** wraps a block cipher to encrypt arbitrary-length messages. The choice of mode is where most symmetric-crypto disasters happen in practice. The mode determines whether the same plaintext block encrypts to the same ciphertext, whether a single bit-flip in ciphertext corrupts just one block or cascades, and — most importantly — whether the encryption is merely **confidential** or also **authenticated**.",
            "**ECB (Electronic Codebook)** simply encrypts each block independently: `C_i = E_k(P_i)`. This is catastrophically broken for any structured data. Because the same plaintext block always maps to the same ciphertext block under the same key, patterns are preserved. The canonical demonstration is the 'ECB penguin': encrypting a bitmap of the Linux penguin Tux with AES-ECB produces a ciphertext that is still recognizably a penguin in outline form, because the background regions (uniform color → repeated blocks) all encrypt identically. For any realistic message — HTTP headers, protocol framing, JPEG thumbnails, structured file formats — ECB leaks structural information. Never use ECB for anything.",
            "**CBC (Cipher Block Chaining)** fixes ECB's determinism by XORing each plaintext block with the previous ciphertext block before encryption: `C_i = E_k(P_i XOR C_{i-1})`, starting with a random **initialization vector (IV)** for `C_0`. The same plaintext block at different positions or with different IVs now encrypts differently. But CBC introduces a subtle and brutally exploited weakness: **padding oracle attacks**. Because a block cipher requires exactly 16-byte blocks, the last plaintext block must be padded (typically with PKCS#7: N bytes each with value N). If the decryption endpoint tells you whether decryption produced valid padding — even by a different error message, a timing difference, or a different HTTP status code — an attacker can decrypt arbitrary ciphertext by iteratively querying the oracle, 16 bytes at a time, without knowing the key. This is the **padding oracle attack**, and it has been found in real systems: Lucky13 (TLS), the original POODLE attack, and numerous web application frameworks. CBC also requires an unpredictable (random) IV and is **not authenticated** by default — an attacker can flip bits in a ciphertext block to make predictable changes in the next plaintext block (CBC is malleable).",
            "**CTR (Counter Mode)** turns the block cipher into a **stream cipher**: generate a keystream by encrypting a sequence of counters `(nonce ‖ counter)` and XOR the keystream with plaintext. `C_i = P_i XOR E_k(nonce ‖ i)`. CTR requires no padding (it just truncates the last keystream block), is parallelizable (each block of keystream is independent), and admits random access (to decrypt block i, just compute one block of keystream). The catastrophic weakness: if the *same* (key, nonce) pair is ever used twice, the XOR of the two ciphertexts equals the XOR of the two plaintexts — and if one plaintext is known, the other is immediately recovered. **Nonce reuse is fatal for CTR**, and this same principle applies to every stream cipher and GCM mode. Many nonce-reuse disasters have occurred in the real world: RC4's RC4-biases (WEP), AES-CTR nonce reuse in TLS before explicit IVs, and various VPN protocol mistakes.",
            "**GCM (Galois/Counter Mode)** is the current standard choice: CTR mode for encryption, plus an authentication tag computed via a **GHASH** polynomial MAC over ciphertext and associated data. The combined scheme is an **AEAD (Authenticated Encryption with Associated Data)**: it simultaneously provides confidentiality (the ciphertext hides the plaintext) and **integrity** (any modification of the ciphertext or associated data is detected via the authentication tag). The 'associated data' mechanism lets you authenticate metadata (e.g. packet headers) that you need to be in the clear but that must not be tampered with. AEAD is what you should always use: without authentication, encryption alone is malleable and vulnerable to chosen-ciphertext attacks. The canonical `AES-256-GCM` with a random 96-bit nonce is the go-to choice. However, GCM's nonce-reuse vulnerability is worse than CTR's: reusing a nonce in GCM not only reveals keystream but also leaks the authentication key, destroying all future authentication security under that key. Use a counter if state is preserved; use a random 96-bit nonce only if the nonce space is large enough that collisions are negligible for your message volume (2^{48} messages before the 2^{-32} collision probability target is exceeded at 96-bit random nonces).",
          ],
          resources: [
            { label: "Cryptopals — Set 2 (padding oracle, CBC bitflipping, ECB detection)", url: "https://cryptopals.com/sets/2", type: "practice" },
            { label: "NIST SP 800-38D — GCM specification", url: "https://csrc.nist.gov/publications/detail/sp/800-38d/final", type: "docs" },
          ],
        },
        {
          title: "Cryptographic hash functions — SHA-256, SHA-3, BLAKE3",
          body: [
            "A **cryptographic hash function** maps arbitrary-length input to a fixed-length digest and must satisfy three security properties simultaneously. **Pre-image resistance**: given only the digest `h`, it must be infeasible to find any `x` with `H(x) = h`. **Second pre-image resistance**: given `x` and `h = H(x)`, it must be infeasible to find any `x' ≠ x` with `H(x') = h`. **Collision resistance**: it must be infeasible to find *any* pair `(x, x')` with `x ≠ x'` and `H(x) = H(x')`. These are distinct and ordered by strength — collision resistance implies second pre-image resistance which implies nothing about pre-image resistance. A hash function broken for collisions (like MD5, practical since 2004; SHA-1, practical since 2017) may still be second-pre-image resistant but should be considered insecure for signature use.",
            "**SHA-256** uses the **Merkle-Damgård construction**: pad the input to a multiple of 512-bit blocks, initialize a 256-bit state vector with specific constants, then iterate a **compression function** `f(state, block) → new_state` over each block in sequence, outputting the final state. The compression function uses a 64-round ARX (add-rotate-XOR) structure with message schedule expansion. Merkle-Damgård has one critical structural weakness: **length extension attacks**. Given `H(m)` for an unknown secret-prefixed message `H(key ‖ m)`, an attacker can compute `H(key ‖ m ‖ padding ‖ m')` for arbitrary appended data `m'`, without knowing the key. This is why `H(key ‖ msg)` is not a secure MAC: the extension lets an attacker forge valid hashes for extended messages. HMAC (below) is immune because it applies the hash twice in a way that breaks the length-extension structure.",
            "**SHA-3 (Keccak)** uses a completely different design — the **sponge construction** — deliberately chosen to be structurally distinct from SHA-2 (so that weaknesses in one design class do not affect both). A sponge divides its internal state into a 'rate' portion (where input is absorbed, where output is squeezed) and a 'capacity' portion (never directly exposed, providing security). Absorption: XOR each input block into the rate portion, then apply a fixed permutation `f` (Keccak-f[1600], a 24-round ARC-based permutation). Squeezing: output rate portions repeatedly, permuting between outputs, until enough digest bytes are produced. The capacity portion is the security parameter — for SHA-3-256, the security level is capacity/2 = 128 bits. Crucially, sponges are naturally immune to length extension because the capacity is never directly observable. The Ethereum blockchain uses **Keccak-256** specifically — which differs from the NIST-finalized SHA-3-256 only in the padding constant (Ethereum was designed before NIST finalized the standard). This is a notorious detail that trips up developers who reach for a `SHA3` library and get different output than what Ethereum expects.",
            "**BLAKE3** is the current fastest general-purpose cryptographic hash, using a tree structure that inherently parallelizes across CPU cores and SIMD lanes, plus a streamlined ARX compression function based on ChaCha's quarter-round. It is faster than SHA-256 in software on modern hardware, provides an arbitrary-length output (extendable output function, XOF), and is immune to length extension by construction. BLAKE3 is not yet in NIST standards and is newer, but its design is conservative and well-analyzed. It is increasingly used in performance-critical systems (storage, file integrity).",
            "The **avalanche effect** — a single input bit change flips ~half the output bits randomly — is a design requirement, not a consequence, of all three. It ensures that related inputs produce unrelated outputs, preventing any statistical structure in hashes from leaking structure in inputs. The birthday paradox is the other key quantitative fact: for an n-bit hash, the expected number of random evaluations before a collision is ~2^{n/2}, not 2^n. SHA-256's 256-bit output puts collisions at ~2^{128}, still computationally infeasible, but 64-bit hashes (e.g. FNV-64) have birthday collision at ~2^{32} (~4 billion tries) — practical for an attacker. This is why truncating a cryptographic hash to save space must be done with care for the specific security level you need.",
          ],
          resources: [
            { label: "NIST FIPS 180-4 — SHA-256 and SHA-2 family specification", url: "https://csrc.nist.gov/publications/detail/fips/180/4/final", type: "docs" },
            { label: "NIST FIPS 202 — SHA-3 (Keccak) standard", url: "https://csrc.nist.gov/publications/detail/fips/202/final", type: "docs" },
            { label: "BLAKE3 specification and paper", url: "https://github.com/BLAKE3-team/BLAKE3-specs/blob/master/blake3.pdf", type: "paper" },
          ],
        },
        {
          title: "MACs and HMAC — the length-extension problem",
          body: [
            "A **Message Authentication Code (MAC)** is a keyed tag that proves a message came from someone holding the key and was not tampered with. Unlike a signature, it does not provide non-repudiation (both parties know the key). Unlike a hash, it requires a secret to verify. The security property is **existential unforgeability under chosen-message attack (EUF-CMA)**: an attacker who can query the MAC on any messages of their choice still cannot produce a valid MAC on a message they have not queried, even if they try adaptively, without knowing the key.",
            "The naive construction `H(key ‖ msg)` (prepend the key then hash) fails because of **length extension**. In Merkle-Damgård hashes (SHA-256, SHA-1, MD5), the internal state after processing `key ‖ msg` is exactly the hash output. An attacker who knows `H(key ‖ msg)` can restart the compression function from that state and extend the message: they can compute `H(key ‖ msg ‖ pad ‖ extra)` for any `extra` they choose, without knowing the key. A web application that uses `H(secret ‖ user_data)` as an integrity token is therefore forgeable — the attacker extends `user_data` to include additional fields and recomputes a valid token. This was the vulnerability behind several early REST API authentication schemes. The fix does not require a new hash function; it requires a construction that prevents the internal state from being continuable by an adversary.",
            "**HMAC** is defined as `HMAC(k, m) = H((k XOR opad) ‖ H((k XOR ipad) ‖ m))` where `opad = 0x5c5c...5c` and `ipad = 0x3636...36` are fixed constants. The inner hash `H((k XOR ipad) ‖ m)` produces a fixed-length digest, and the outer hash `H((k XOR opad) ‖ inner_digest)` re-mixes the key into the outer layer. Because the outer hash application restarts with a fresh key-derived input, an attacker cannot simply 'continue' the inner hash — the key is re-mixed at the outer level, blocking length extension. HMAC's security is proven under the assumption that the underlying compression function behaves as a PRF, not requiring full collision resistance — so HMAC-MD5 is still computationally secure as a MAC even though MD5 is broken for collision finding. In practice, use HMAC-SHA256 or HMAC-SHA3-256.",
            "The alternative MAC construction **KMAC** (Keccak MAC), part of the NIST SHA-3 standard, is simply `Keccak(key ‖ len(key) ‖ msg)` — because sponges are immune to length extension by construction, the naive construction works and the double-hash structure of HMAC is unnecessary. For new systems, KMAC or BLAKE3-keyed-hash (which also avoids the double construction) are simpler and equally secure.",
            "Practical implementation rules that matter: (1) Always use a **constant-time comparison** when verifying a MAC tag — the standard `==` in most languages short-circuits on the first mismatched byte, leaking via timing how many bytes matched, enabling an attacker to forge tags byte-by-byte. Use `hmac.compare_digest()` (Python), `crypto.timingSafeEqual()` (Node.js), or equivalent. (2) Never truncate a MAC tag to fewer than 96 bits without careful analysis — shorter tags allow birthday-bound forgery at fewer queries. (3) Use a different key for MAC and encryption — key separation prevents cross-protocol attacks.",
          ],
          resources: [
            { label: "RFC 2104 — HMAC specification", url: "https://datatracker.ietf.org/doc/html/rfc2104", type: "docs" },
            { label: "Cryptopals Set 4 — length extension attacks and break HMAC-SHA1 with timing", url: "https://cryptopals.com/sets/4", type: "practice" },
          ],
        },
        {
          title: "Password hashing — bcrypt, scrypt, and Argon2id",
          body: [
            "Password hashing is categorically different from message hashing, and conflating them is the source of most password database disasters. A cryptographic hash like SHA-256 is designed to be **fast** — processing gigabytes per second on modern hardware. That speed is catastrophic for passwords: an attacker with a GPU or ASIC can try billions of candidate passwords per second against a stolen SHA-256 hash, exhausting common passwords and dictionary words in minutes. The entire field of password hashing exists to make this brute-force search slow, expensive, and memory-intensive, while remaining fast enough for legitimate logins (which happen once per session).",
            "The first critical defense is a **salt**: a unique random value (typically 16+ bytes) generated per password and stored alongside the hash as `hash(salt ‖ password)`. The salt does not make the hash harder to crack for a single user's password — a targeted attacker already has the salt and just searches candidate passwords against it. What it does is prevent **rainbow tables** (pre-computed hash-to-password lookup tables) and, more importantly, prevent an attacker from cracking many passwords simultaneously: they must attack each salted hash independently, so cracking N passwords costs N times as much, not O(1).",
            "**bcrypt** (1999, Niels Provos and David Mazières) was the first widely adopted password hash designed for slowness. It is based on the Blowfish block cipher's expensive key setup, with a tunable **cost factor** (typically 10-12 today, meaning 2^10 to 2^12 iterations of the key setup). Increasing the cost factor by 1 doubles the time. bcrypt's key weakness is that it has a **72-byte password limit** (anything beyond 72 bytes is ignored, which has caused subtle bugs in frameworks) and, crucially, it is not memory-hard — attackers can build dedicated hardware (ASICs) that run bcrypt cheaply compared to software. Nevertheless, bcrypt remains a solid default where Argon2 is not available, and it vastly outperforms simple SHA hashing.",
            "**scrypt** (2009, Colin Percival) introduced **memory-hardness**: it requires a large amount of RAM proportional to the work factor, and the memory access pattern is sequential-then-random in a way that forces the RAM to be actually present (you cannot replace it with cheaper logic). An attacker can still run scrypt in hardware, but the cost is dominated by memory bandwidth, not computation — ASICs must also have the RAM, raising the cost significantly. scrypt's parameters are (N, r, p): N is the block count (memory), r is the block mix factor, p is the parallelization factor. Choosing parameters for scrypt is tricky and has led to misconfigured deployments.",
            "**Argon2id** (winner of the 2015 Password Hashing Competition, now the NIST recommendation in SP 800-132) is the modern standard. It combines two Argon2 variants: **Argon2i** (data-independent memory access, resistant to side-channel attacks but weaker against GPU attacks) and **Argon2d** (data-dependent access, stronger against GPU attacks but potentially vulnerable to side-channels). The 'id' variant uses Argon2i in the first pass and Argon2d thereafter, giving the best of both. Parameters are: memory cost `m` (in kibibytes), time cost `t` (iterations), and parallelism `p`. A minimum of `m=19456` (19 MiB), `t=2`, `p=1` is the baseline OWASP recommendation today. The memory cost is the key parameter — a 64 MiB hash costs 64× as much memory as a naive hash, and GPU parallelism is memory-bandwidth-limited. The rule of thumb: **tune parameters so a single hash takes 300-500ms on your authentication server**; then it costs an attacker the same per guess, making brute-force infeasible at scale.",
          ],
          resources: [
            { label: "OWASP — Password Storage Cheat Sheet (Argon2id parameters)", url: "https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html", type: "docs" },
            { label: "Argon2 reference implementation and spec (RFC 9106)", url: "https://datatracker.ietf.org/doc/html/rfc9106", type: "docs" },
          ],
        },
      ],
      resources: [
        { label: "Cryptopals Challenges (free)", url: "https://cryptopals.com/", type: "practice", note: "learn by breaking real crypto" },
        { label: "Dan Boneh — Crypto I (Stanford/Coursera)", url: "https://www.coursera.org/learn/crypto", type: "course" },
        { label: "Serious Cryptography — Aumasson (book)", url: "https://nostarch.com/seriouscrypto", type: "book" },
      ],
      connections: [
        { to: "dsa-hashing", note: "Cryptographic hashes add collision-resistance to the hashing you met in DSA" },
        { to: "web3-fundamentals", note: "Keccak-256 and Merkle trees are the hashing core of blockchains" },
        { to: "cyber-foundations", note: "AEAD and HMAC are what TLS uses to protect traffic" },
      ],
    },
  ],
});

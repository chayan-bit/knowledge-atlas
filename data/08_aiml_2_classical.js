atlasAdd({
  id: "aiml",
  topics: [
    {
      id: "ai-classical",
      subtopics: [
        {
          title: "Linear and Logistic Regression: closed form, gradient descent, and MLE",
          body: [
            "**Linear regression** models the conditional expectation $\\mathbb{E}[y|x] = w^T x + b$ and is the bedrock of all of supervised learning. The **ordinary least squares (OLS)** objective minimises the residual sum of squares: $\\mathcal{L}(w) = \\|Xw - y\\|^2_2$ where $X \\in \\mathbb{R}^{n \\times d}$ is the design matrix and $y \\in \\mathbb{R}^n$ is the target vector. Setting the gradient to zero gives the **normal equation**: $\\nabla_w \\mathcal{L} = 2X^T(Xw - y) = 0 \\Rightarrow w^* = (X^T X)^{-1} X^T y$. This is the **Moore-Penrose pseudoinverse** solution and is optimal when $X^T X$ is invertible. It is exact and $O(d^3)$ in compute — feasible up to $d \\sim 10^4$, infeasible beyond.",
            "When $n \\gg d$ or $X^T X$ is ill-conditioned, **gradient descent** is preferred: $w \\leftarrow w - \\eta \\nabla_w \\mathcal{L} = w - \\eta \\cdot 2X^T(Xw - y)$. Mini-batch SGD uses a random subset of $B$ examples per step, reducing per-step cost from $O(nd)$ to $O(Bd)$ and introducing beneficial noise that helps escape local minima. The convergence rate of gradient descent on a strongly convex loss like OLS is linear: after $T$ steps the error decays as $(1 - 2\\eta\\mu)^T$ where $\\mu$ is the strong convexity constant. Setting $\\eta = 1/L$ where $L$ is the Lipschitz constant of $\\nabla \\mathcal{L}$ gives the optimal fixed step size.",
            "**Probabilistic view via MLE**: Assume $y = w^T x + \\epsilon$ where $\\epsilon \\sim \\mathcal{N}(0, \\sigma^2)$. The likelihood of observing all $y_i$ given $X$ and $w$ is $p(y|X,w) = \\prod_i \\mathcal{N}(y_i; w^T x_i, \\sigma^2)$. Maximising the log-likelihood is equivalent to minimising $\\sum_i (y_i - w^T x_i)^2$ — exactly OLS. This derivation reveals that OLS implicitly assumes Gaussian noise; if the noise is Laplacian, MLE gives the **L1/MAE** loss; if it is Cauchy, we get a different robust regression. The choice of loss function is a modelling assumption about the noise distribution.",
            "**Logistic regression** models a binary label $y \\in \\{0,1\\}$ by placing a sigmoid on the linear predictor: $P(y=1|x) = \\sigma(w^T x + b) = 1/(1+e^{-w^T x - b})$. The log-likelihood is the **binary cross-entropy** loss: $\\mathcal{L}(w) = -\\sum_i [y_i \\log \\hat{p}_i + (1-y_i)\\log(1-\\hat{p}_i)]$. There is no closed form — $\\mathcal{L}$ is convex but not quadratic. **Newton's method** converges in $O(\\log(1/\\epsilon))$ steps via the **IRLS (Iteratively Reweighted Least Squares)** algorithm: $w \\leftarrow w - (X^T W X)^{-1} X^T W r$ where $W = \\text{diag}(\\hat{p}(1-\\hat{p}))$ and $r = y - \\hat{p}$ is the residual. For large $d$, gradient descent or L-BFGS is standard.",
            "**Multiclass logistic regression (softmax regression)**: extend to $K$ classes with $P(y=k|x) = \\text{softmax}(Wx)_k = e^{w_k^T x}/\\sum_j e^{w_j^T x}$. The loss is **categorical cross-entropy** $\\mathcal{L} = -\\sum_i \\log P(y_i | x_i)$. The gradient with respect to $W$ simplifies to $X^T(\\hat{P} - Y)$ where $\\hat{P}$ is the matrix of softmax probabilities and $Y$ is the one-hot label matrix — a remarkably clean expression that follows from the cancellation between the log and the softmax exponentials. This is the foundation of the final layer of every classification neural network.",
          ],
          resources: [
            { label: "CS229 — Linear Regression notes (Stanford)", url: "https://cs229.stanford.edu/notes2022fall/cs229-notes1.pdf", type: "docs" },
            { label: "Hastie, Tibshirani, Friedman — Elements of Statistical Learning (free PDF)", url: "https://hastie.su.domains/ElemStatLearn/printings/ESLII_print12_toc.pdf", type: "book" },
          ],
        },
        {
          title: "Decision Trees, Random Forests, and the Bias-Variance Tradeoff",
          body: [
            "A **decision tree** partitions the input space into axis-aligned rectangular regions, assigning a constant prediction to each leaf. At each internal node, a split $(j, t)$ on feature $j$ at threshold $t$ is chosen to maximise the purity of the child nodes. For classification, purity is measured by **information gain** (reduction in entropy: $IG = H(\\text{parent}) - \\frac{|L|}{n}H(L) - \\frac{|R|}{n}H(R)$) or **Gini impurity** ($G = 1 - \\sum_k p_k^2$). For regression, the criterion is variance reduction. Greedy top-down induction is the universal algorithm; finding the globally optimal tree is NP-hard.",
            "Decision trees have high variance: a small change in training data leads to a completely different tree structure. This motivates **ensemble methods** that average over many trees. **Bagging (Bootstrap Aggregating)**: train $T$ trees, each on a **bootstrap sample** (sample $n$ examples with replacement from the training set) and average their predictions. Bagging reduces variance by a factor of roughly $T$ without increasing bias — the key insight is that averaging $T$ uncorrelated predictors each with variance $\\sigma^2$ gives an ensemble with variance $\\sigma^2/T$. The bootstrap introduces correlation between trees (correlation $\\rho$), so the true reduction is $\\rho \\sigma^2 + (1-\\rho)\\sigma^2/T$, which is still $O(1/T)$ for small $\\rho$.",
            "**Random Forests** (Breiman, 2001) extend bagging with **feature subsampling**: at each split, only a random subset of $\\sqrt{d}$ (classification) or $d/3$ (regression) features are considered. This decorrelates the trees further ($\\rho \\downarrow$), improving ensemble variance reduction. The **Out-of-Bag (OOB) error** estimates generalisation for free: for each training example, average predictions from trees that did not include it in their bootstrap sample. OOB error typically tracks test error closely, eliminating the need for a separate validation set. Feature importance is computed as the mean decrease in impurity (MDI) across all trees and splits, though MDI is biased towards high-cardinality features; **permutation importance** (randomly shuffle one feature and measure accuracy drop) is more reliable.",
            "The **bias-variance decomposition** for mean squared error is $\\mathbb{E}[(f(x) - \\hat{f}(x))^2] = \\text{Bias}^2 + \\text{Variance} + \\text{Irreducible noise}$. **Bias** measures how far the expected prediction deviates from the true function; **variance** measures how much predictions vary across different training sets. A depth-1 decision tree (decision stump) has high bias and low variance; a full unpruned tree has low bias and high variance. **Regularisation** (max depth, min samples per leaf, min impurity decrease) prunes the tree, trading variance for bias. The sweet spot minimises the sum. In practice: for structured/tabular data with up to $10^5$ features, Random Forests and gradient boosting dominate; deep learning is only competitive with very large labelled datasets or when raw (image/text/audio) inputs are used.",
            "**Pruning strategies**: post-pruning (**cost-complexity pruning**, also called **weakest link pruning**) starts from a full tree and greedily collapses subtrees weighted by a complexity penalty $\\alpha |T|$ where $|T|$ is the number of leaves. The sequence of pruned trees indexed by $\\alpha$ forms a **regularisation path**, and the optimal $\\alpha$ is chosen by cross-validation. This is analogous to the LASSO regularisation path for linear models. Pre-pruning (max depth, min samples) is simpler and faster but less principled. Random Forests typically do not prune — they rely on averaging to control variance.",
          ],
          resources: [
            { label: "Breiman — Random Forests paper (2001)", url: "https://link.springer.com/article/10.1023/A:1010933404324", type: "paper" },
            { label: "scikit-learn — Ensemble methods user guide", url: "https://scikit-learn.org/stable/modules/ensemble.html", type: "docs" },
            { label: "StatQuest — Decision Trees and Random Forests series", url: "https://www.youtube.com/@statquest", type: "video" },
          ],
        },
        {
          title: "Gradient Boosting: XGBoost, LightGBM, and Additive Models",
          body: [
            "**Gradient boosting** (Friedman, 2001) builds an additive model $F_M(x) = \\sum_{m=1}^M \\alpha_m h_m(x)$ where each $h_m$ is a **weak learner** (shallow tree) that fits the **pseudo-residuals** of the current ensemble. The key insight from **functional gradient descent**: minimising $\\mathcal{L} = \\sum_i l(y_i, F(x_i))$ by adding a new function $h_m$ to $F$ is analogous to gradient descent in function space. The pseudo-residuals at step $m$ are the negative gradient of the loss with respect to the current prediction: $r_i^{(m)} = -\\left[\\frac{\\partial l(y_i, F(x_i))}{\\partial F(x_i)}\\right]_{F=F_{m-1}}$. Fitting a tree to $\\{r_i^{(m)}\\}$ and adding it to $F$ is one step of gradient descent in the space of functions.",
            "For **squared loss** $l = (y-F)^2$, pseudo-residuals are just $r_i = y_i - F_{m-1}(x_i)$ — the plain residuals. For **log-loss** (binary classification), $r_i = y_i - \\sigma(F_{m-1}(x_i))$ — the probability residual. For **quantile regression**, the pseudo-residuals are the sign of the residual — boosting minimises MAE. The gradient boosting framework is therefore a single algorithm that can minimise **any differentiable loss** by changing only the pseudo-residual formula. This generality is why gradient boosting dominates tabular ML: it can be tuned to MAE, Huber loss, Tweedie, Cox partial likelihood for survival, custom ranking objectives, etc.",
            "**XGBoost** (Chen & Guestrin, 2016) uses a second-order Taylor expansion of the loss: $\\mathcal{L}(\\theta) \\approx \\sum_i [l(y_i, \\hat{y}_i) + g_i f_m(x_i) + \\frac{1}{2}h_i f_m(x_i)^2] + \\Omega(f_m)$ where $g_i = \\partial_{\\hat{y}} l(y_i, \\hat{y}_i)$ and $h_i = \\partial^2_{\\hat{y}} l(y_i, \\hat{y}_i)$ are first and second-order gradients, and $\\Omega(f_m) = \\gamma T + \\frac{1}{2}\\lambda \\|w\\|^2$ regularises tree complexity (T = leaves, w = leaf weights). The optimal leaf weight for a leaf region $R_j$ is $w_j^* = -\\frac{G_j}{H_j + \\lambda}$ where $G_j = \\sum_{i \\in R_j} g_i$ and $H_j = \\sum_{i \\in R_j} h_i$. The optimal split gain is $\\frac{1}{2}\\left[\\frac{G_L^2}{H_L+\\lambda} + \\frac{G_R^2}{H_R+\\lambda} - \\frac{(G_L+G_R)^2}{H_L+H_R+\\lambda}\\right] - \\gamma$.",
            "**LightGBM** (Ke et al., 2017) introduces two innovations for speed on large datasets. **GOSS (Gradient-based One-Side Sampling)**: keep all examples with large gradients (high error) and randomly subsample those with small gradients, reweighting the sample to maintain the gradient distribution. This focuses computation on the hard examples. **EFB (Exclusive Feature Bundling)**: bundle mutually exclusive features (features that rarely take non-zero values simultaneously, common in one-hot encoded data) into a single feature, reducing the effective feature dimension. LightGBM also grows trees **leaf-wise** (always split the leaf with the highest loss reduction) rather than **level-wise** (XGBoost's default), which reaches a given loss with fewer leaves but requires the `num_leaves` parameter to prevent overfitting.",
            "**Key hyperparameters and their effects**: `n_estimators` (M) — more trees reduces bias, increases compute; early stopping on validation loss prevents overfitting. `learning_rate` ($\\alpha$) — shrinkage; lower $\\alpha$ requires more trees but generalises better. `max_depth` / `num_leaves` — controls tree complexity. `subsample` — fraction of training data per tree (equivalent to bagging within boosting). `colsample_bytree` — fraction of features per tree (analogous to Random Forest feature subsampling). `reg_alpha`, `reg_lambda` — L1 and L2 regularisation on leaf weights. The practical tuning order: (1) set `n_estimators` large with early stopping; (2) tune `max_depth`/`num_leaves`; (3) tune `subsample`, `colsample`; (4) tune `reg_alpha`, `reg_lambda`; (5) lower `learning_rate` and increase `n_estimators` proportionally.",
          ],
          resources: [
            { label: "XGBoost paper — Chen & Guestrin (2016)", url: "https://arxiv.org/abs/1603.02754", type: "paper" },
            { label: "LightGBM paper — Ke et al. (2017)", url: "https://papers.nips.cc/paper_files/paper/2017/hash/6449f44a102fde848669bdd9eb6b76fa-Abstract.html", type: "paper" },
            { label: "Friedman — Greedy Function Approximation: A Gradient Boosting Machine (2001)", url: "https://projecteuclid.org/journals/annals-of-statistics/volume-29/issue-5/Greedy-function-approximation-A-gradient-boosting-machine/10.1214/aos/1013203451.full", type: "paper" },
          ],
        },
        {
          title: "Support Vector Machines: Max-Margin, Duality, and the Kernel Trick",
          body: [
            "**Support Vector Machines (SVMs)** find the **maximum-margin hyperplane** separating two classes: the hyperplane $w^T x + b = 0$ such that the minimum distance from any training point to the hyperplane (the **margin**) is maximised. For linearly separable data, the primal problem is: $\\min_{w,b} \\frac{1}{2}\\|w\\|^2$ subject to $y_i(w^T x_i + b) \\geq 1$ for all $i$. The constraints ensure all points are correctly classified with at least unit margin (after normalising so the margin is $2/\\|w\\|$). Points exactly on the margin boundaries $w^T x + b = \\pm 1$ are the **support vectors** — removing any non-support-vector training point would not change the solution.",
            "The **Lagrangian dual** of the SVM primal has elegant structure: $\\max_\\alpha \\sum_i \\alpha_i - \\frac{1}{2}\\sum_{i,j} \\alpha_i \\alpha_j y_i y_j x_i^T x_j$ subject to $\\alpha_i \\geq 0$ and $\\sum_i \\alpha_i y_i = 0$. The dual depends on the training data only through **inner products** $x_i^T x_j$. The optimal decision boundary is $f(x) = \\text{sign}(\\sum_i \\alpha_i^* y_i x_i^T x + b^*)$ — a sparse combination of support vectors (most $\\alpha_i^* = 0$ by KKT complementary slackness). This sparsity is the SVM's distinctive property: the classifier is defined entirely by the (usually small) subset of support vectors.",
            "**The kernel trick** exploits the dual's dependence on inner products: replace $x_i^T x_j$ with $K(x_i, x_j) = \\phi(x_i)^T \\phi(x_j)$ where $\\phi$ maps inputs to a (possibly infinite-dimensional) feature space. The kernel computes this inner product **without ever constructing $\\phi(x)$ explicitly** — this is the trick. Valid kernels must be **positive semi-definite** (by Mercer's theorem: $K$ is a valid kernel iff $\\sum_{i,j} c_i c_j K(x_i, x_j) \\geq 0$ for all finite sets and coefficients). Common kernels: **RBF** $K(x,x') = \\exp(-\\gamma\\|x-x'\\|^2)$ (universal approximator), **polynomial** $K(x,x') = (x^T x' + c)^d$, **linear** $K(x,x') = x^T x'$ (equivalent to no feature map). The RBF kernel corresponds to an **infinite-dimensional** feature space — it effectively fits an infinitely wide neural network layer.",
            "**Soft-margin SVMs** handle non-separable data by introducing slack variables $\\xi_i \\geq 0$: $\\min_{w,b,\\xi} \\frac{1}{2}\\|w\\|^2 + C \\sum_i \\xi_i$ subject to $y_i(w^T x_i + b) \\geq 1 - \\xi_i$ and $\\xi_i \\geq 0$. The parameter $C$ controls the bias-variance tradeoff: large $C$ penalises misclassification heavily (low bias, high variance, small margin); small $C$ allows many misclassifications (high bias, low variance, large margin). The dual again has the same inner-product structure, with $0 \\leq \\alpha_i \\leq C$ replacing $\\alpha_i \\geq 0$. The **hinge loss** $\\max(0, 1 - y_i f(x_i))$ reformulation connects SVMs to the general empirical risk minimisation framework: minimising hinge loss + L2 regularisation $\\frac{1}{2C}\\|w\\|^2$ is exactly the soft-margin SVM.",
            "**Practical considerations and decline**: SVMs were the dominant ML method for text classification (via TF-IDF features + linear SVM), image classification (before deep learning), and bioinformatics (protein fold prediction) through the 2000s. Their decline came not from theoretical weakness but from: (i) $O(n^2)$ to $O(n^3)$ training cost (kernel matrix), making them infeasible for $n > 10^5$; (ii) the kernel + feature engineering burden, which deep learning automates; (iii) no natural probability output (Platt scaling is an approximation). Linear SVMs remain competitive for text with $n > 10^6$ via the **Pegasos** algorithm ($O(1/\\lambda T)$ convergence in $T$ steps). Understanding SVMs deeply is still valuable: the max-margin intuition underlies **contrastive learning** losses, and the duality analysis is required to understand kernel methods and Gaussian processes.",
          ],
          resources: [
            { label: "Burges — A Tutorial on Support Vector Machines (1998)", url: "https://link.springer.com/article/10.1023/A:1009715923555", type: "paper" },
            { label: "CS229 — SVM notes (Stanford, Andrew Ng)", url: "https://cs229.stanford.edu/notes2022fall/cs229-notes3.pdf", type: "docs" },
            { label: "LibSVM — the canonical SVM library", url: "https://www.csie.ntu.edu.tw/~cjlin/libsvm/", type: "tool" },
          ],
        },
        {
          title: "Regularisation: Ridge, LASSO, and the Bias-Variance Tradeoff",
          body: [
            "**Regularisation** adds a penalty term to the training loss to prevent overfitting, biasing the model toward simpler solutions. The two canonical forms are **L2 regularisation (Ridge)**: $\\mathcal{L}_{\\text{ridge}} = \\|Xw - y\\|^2 + \\lambda\\|w\\|^2_2$, which has the closed-form solution $w^* = (X^T X + \\lambda I)^{-1} X^T y$ and always exists (the $\\lambda I$ term ensures positive definiteness), and **L1 regularisation (LASSO)**: $\\mathcal{L}_{\\text{lasso}} = \\|Xw - y\\|^2 + \\lambda\\|w\\|_1$, which has no closed form but produces **sparse solutions** where many $w_j = 0$ exactly. **Elastic Net** combines both: $\\lambda_1\\|w\\|_1 + \\lambda_2\\|w\\|_2^2$.",
            "**Why L1 induces sparsity and L2 does not**: Geometrically, the LASSO constraint set $\\{w : \\|w\\|_1 \\leq s\\}$ is an L1-ball (a diamond in 2D, an octahedron in 3D) with corners on the coordinate axes. The OLS contours (ellipsoids) are likely to touch the constraint set at a corner, where some $w_j = 0$. The L2-ball $\\{w : \\|w\\|_2 \\leq s\\}$ is smooth with no corners, so intersections are generically not on an axis. Algebraically: the L1 subgradient at $w_j = 0$ includes all values in $[-\\lambda, \\lambda]$, so optimality allows $w_j = 0$ whenever $|\\nabla_{w_j}\\mathcal{L}_{\\text{ols}}| \\leq \\lambda$. The L2 gradient is $2\\lambda w_j$, which is zero only at $w_j = 0$ — a single point, not a region.",
            "**The LASSO regularisation path** (Efron et al., LARS algorithm): as $\\lambda$ decreases from $\\infty$ to 0, features enter the active set one by one. The path is **piecewise linear** in $\\lambda$, and the full path can be computed in $O(nd \\cdot \\min(n,d))$ time — same as one OLS fit. This is the **LARS (Least Angle Regression)** algorithm. Cross-validation over the regularisation path selects $\\lambda^*$. For $p > n$ settings (more features than examples, common in genomics, text), LASSO's ability to identify a sparse subset of relevant features is invaluable. The **irrepresentable condition** is the necessary and sufficient condition for LASSO to consistently identify the true support.",
            "**Bayesian interpretation**: Ridge regression is MAP estimation with a **Gaussian prior** $w \\sim \\mathcal{N}(0, (2\\lambda)^{-1} I)$. LASSO is MAP with a **Laplace prior** $w_j \\sim \\text{Laplace}(0, 1/\\lambda)$ (which has heavier tails near zero, inducing shrinkage to exactly zero). This connection unifies regularisation with Bayesian inference: any regulariser is a prior, and the regularisation strength is the prior precision. **Dropout** in neural networks corresponds (approximately) to a Gaussian prior on weights (Gal & Ghahramani, 2016) — regularisation and Bayesian inference are not separate paradigms.",
            "**Cross-validation and hyperparameter selection**: $k$-fold CV partitions the training data into $k$ equal folds, trains on $k-1$ and validates on the held-out fold, cycling through all $k$ choices and averaging validation loss. For $k=n$ this is **leave-one-out CV (LOOCV)**; for linear models, LOOCV has an analytical shortcut via the hat matrix $H = X(X^TX)^{-1}X^T$: the LOO prediction error at point $i$ is $(y_i - \\hat{y}_i)/(1 - H_{ii})$. For non-linear models, 5-fold or 10-fold CV is standard. The **one-standard-error rule** selects the simplest model (largest $\\lambda$) within one standard error of the CV minimum, because CV estimates have uncertainty and a simpler model that performs within noise is preferable.",
          ],
          resources: [
            { label: "ISLR — Introduction to Statistical Learning (free PDF, ch. 6)", url: "https://www.statlearning.com/", type: "book" },
            { label: "LARS paper — Efron et al. (2004)", url: "https://projecteuclid.org/journals/annals-of-statistics/volume-32/issue-2/Least-angle-regression/10.1214/009053604000000067.full", type: "paper" },
            { label: "Hands-On ML — Géron ch. 4: Training Models", url: "https://github.com/ageron/handson-ml3", type: "repo" },
          ],
        },
      ],
    },
  ],
});

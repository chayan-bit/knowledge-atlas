atlasAdd({
  id: "gamedev",
  topics: [
    {
      id: "game-raytracing",
      title: "Ray tracing & global illumination",
      level: "Expert",
      body: [
        "**BVH** (SAH construction), **path tracing + importance sampling + ReSTIR** for unbiased GI, **real-time GI** (irradiance probes, Lumen), **GPU-driven rendering** (indirect draws, meshlets/mesh shaders), and **ML in graphics** (DLSS/FSR super-resolution, neural material prediction).",
        "Start with Ray Tracing in One Weekend (3 hours, offline path tracer). Then PBRT for the math. Porting to real-time via DXR/VK_KHR_ray_tracing is the third step.",
      ],
      subtopics: [
        {
          title: "BVH construction: SAH and traversal",
          body: [
            "**Bounding Volume Hierarchy (BVH)** is the spatial acceleration structure that makes ray tracing practical. Without it, finding the first triangle a ray intersects requires testing every triangle - O(N) per ray, which is prohibitively slow for scenes with millions of triangles. A BVH organizes triangles into a binary tree: each leaf holds a small set of triangles, each internal node holds an AABB that tightly bounds all triangles in its subtree. Traversal: test the ray against each node's AABB; if miss, skip the subtree; if hit, recurse into children. Expected O(log N) traversal for a balanced BVH.",
            "**Naive BVH construction** (midpoint split): at each node, find the longest axis, split triangles at the centroid median. Fast O(N log N) build, but produces unbalanced trees with poor spatial partitioning (triangles cluster near splits). A scene with a floor (many triangles in a plane) will have half the triangles in one child, producing a tree of depth N/2 for that branch.",
            "**SAH (Surface Area Heuristic)** builds near-optimal BVHs by minimizing expected ray traversal cost. The cost model: $C(\\text{node}) = C_T + \\frac{SA(L)}{SA(P)} C(L) + \\frac{SA(R)}{SA(P)} C(R)$ where $C_T$ is the traversal cost, $SA$ is surface area, $P$ is the parent AABB, and $L, R$ are the child AABBs. The surface area ratio approximates the probability that a ray hitting the parent hits each child. SAH tries all possible split positions (or a sampled set) and chooses the minimum-cost split. The resulting BVH has 20-50% fewer node traversals than midpoint split, and the build time is O(N log^2 N).",
            "**Compressed BVH nodes**: a naive BVH node stores two child AABBs (24 floats each) + child pointers = 200+ bytes. Modern GPU BVH nodes are compressed to 32-64 bytes using quantized bounds (store the AABB of each child as 8-bit integers relative to the parent AABB) and implicit child pointers (children are adjacent in memory). NVIDIA's BLAS (Bottom-Level Acceleration Structure) format and Intel's Embree library use such compressed nodes. The TLAS (Top-Level Acceleration Structure) stores one leaf per mesh instance (a BVH over instances, not triangles).",
            "**GPU BVH traversal**: the RT cores in NVIDIA RTX GPUs (since Turing, 2018) implement BVH traversal and ray-triangle intersection in dedicated fixed-function hardware. `vkCmdTraceRaysKHR` / `DispatchRays` submits ray queries to the pipeline; the RT cores traverse the TLAS→BLAS hierarchy automatically. The GPU invokes user-written **closest hit**, **any hit**, and **miss** shaders at each intersection event. Performance: the dedicated hardware achieves 1-2 GT/s (giga-traversals per second), an order of magnitude faster than software traversal on shader cores.",
          ],
          resources: [
            { label: "PBRT - BVH chapter (free online)", url: "https://pbr-book.org/4ed/Primitives_and_Intersection_Acceleration/Bounding_Volume_Hierarchies", type: "book" },
            { label: "Embree - Intel's high-performance ray tracing kernels", url: "https://www.embree.org/", type: "repo" },
          ],
        },
        {
          title: "Path tracing: Monte Carlo integration and importance sampling",
          body: [
            "**Path tracing** is an unbiased Monte Carlo algorithm for solving the rendering equation. For each pixel: cast a camera ray into the scene. At the first hit, sample a random outgoing direction (according to the BRDF). Cast a new ray in that direction, repeat. This random walk traces a 'path' through the scene, accumulating light at each bounce from emissive surfaces encountered. Average many such paths per pixel to converge to the correct solution. Path tracing produces physically correct global illumination (soft shadows, color bleeding, caustics) at the cost of high variance (noise) at low sample counts.",
            "**Monte Carlo integration**: the rendering equation integral $\\int f_r L_i \\cos\\theta \\, d\\omega$ is approximated by drawing N samples from a probability distribution $p(\\omega)$ and averaging: $\\frac{1}{N}\\sum_{i=1}^N \\frac{f_r(\\omega_i) L_i(\\omega_i) \\cos\\theta}{p(\\omega_i)}$. The variance of this estimator is inversely proportional to N (10x more samples halves the noise), and the convergence rate is O(1/√N) - to halve noise, quadruple samples. **Importance sampling** reduces variance by choosing $p(\\omega)$ proportional to the integrand: sample directions where $f_r L_i \\cos\\theta$ is large (toward lights, in specular reflection directions). A good proposal distribution reduces variance by 10-100x for the same sample count.",
            "**Multiple Importance Sampling (MIS)**: different sampling strategies are each good at different scenarios. BSDF sampling (sample according to the material BRDF) works well for glossy materials and large light sources. Light sampling (explicitly sample a random point on the light source and cast a shadow ray) works well for small bright lights. **MIS** combines multiple strategies using balance heuristics or power heuristics, taking the best of each without being hurt by the worst. The combined estimator: $\\frac{1}{N_s}\\sum_s\\sum_{i=1}^{N_s} \\frac{f \\cdot w_s(\\omega_{s,i})}{p_s(\\omega_{s,i})}$ where $w_s$ are MIS weights summing to 1. Used in every production path tracer (PBRT, Disney's Hyperion, Pixar's RenderMan).",
            "**ReSTIR (Reservoir-based Spatio-Temporal Importance Resampling)**: a breakthrough algorithm (Bitterli et al. 2020, originally for direct lighting, extended to GI as ReSTIR GI). The key idea: instead of drawing a new random sample for each pixel each frame, maintain a **reservoir** (a small set of light samples with associated weights) per pixel. Resample from neighboring pixels' reservoirs (spatial reuse) and from the previous frame's reservoirs (temporal reuse) to accumulate far more effective light samples with minimal extra cost. ReSTIR can produce results equivalent to 1000+ light samples per pixel while only evaluating the lighting function ~8 times per pixel per frame. Used in real-time ray tracing in cyberpunk-style games and Nvidia's DirectX Raytracing demos.",
            "**Denoising**: path tracing at 1-4 samples per pixel (all that real-time allows) is very noisy. AI-based denoisers (NVIDIA OptiX AI Denoiser, Intel Open Image Denoise) use CNNs trained on path-traced images to denoise noisy renders. Input: noisy color + G-buffer (normals, albedo, depth). Output: denoised image. Quality: at 1 spp, the denoiser produces results roughly equivalent to 64+ spp without denoising. Temporal stability is achieved by accumulating denoiser inputs over multiple frames with motion vector reprojection (similar to TAA). Modern hybrid renderers: hardware ray tracing at 1 spp + neural denoiser = publishable GI quality in real-time.",
          ],
          resources: [
            { label: "Ray Tracing in One Weekend (free)", url: "https://raytracing.github.io/", type: "book", note: "3 books, start here" },
            { label: "PBRT - Monte Carlo integration chapter", url: "https://pbr-book.org/4ed/Monte_Carlo_Integration", type: "book" },
          ],
        },
        {
          title: "Real-time GI: irradiance probes, Lumen, and DDGI",
          body: [
            "**The real-time GI challenge**: path tracing GI is too expensive for games even with hardware RT. Real-time GI approximates global illumination with a combination of precomputed and dynamic techniques. The goal: one-bounce indirect lighting (the light that reaches surfaces after reflecting off other surfaces) without per-frame full path tracing.",
            "**Irradiance probes (Light probes)**: place a 3D grid of probes throughout the scene. At each probe, precompute (or update dynamically) the incoming light from all directions - stored as a low-order **spherical harmonic** (SH) representation (a compact set of 9-27 coefficients per probe that approximates the low-frequency radiance field). At runtime, a surface point samples nearby probes (trilinear interpolation) to get its indirect diffuse illumination. The SH representation captures soft, low-frequency indirect light accurately. Direct in PBRT terms: SH probes are a pre-integrated irradiance cache.",
            "**DDGI (Dynamic Diffuse Global Illumination)**: Majercik et al. 2019, originally from NVIDIA Research. A grid of probes that are updated dynamically each frame using hardware ray tracing. Each probe traces a small number of rays (e.g., 64-256), updates its irradiance representation (stored as 6x6 texel octahedral maps rather than SH, for better quality). Surface shading reads nearby probes via bilinear interpolation. DDGI handles dynamic geometry and lights: as the scene changes, probes update within a few frames. Used as the indirect lighting system in games like Dying Light 2, Cyberpunk 2077 (PC ray tracing mode), and many Unreal 5 titles.",
            "**Lumen (Unreal Engine 5)**: a full dynamic GI system that works without hardware RT on lower-end hardware (software ray marching) and with hardware RT on high-end GPUs. Lumen uses a hierarchical structure: **Mesh Distance Fields** (SDFs per mesh, precomputed at import) for efficient ray marching, **Lumen Surface Cache** (lighting stored on surface cards - flat projections of mesh surfaces), and **Lumen Scene** (a simplified, GPU-friendly scene representation for fast ray queries). At each frame: ray tracing against Lumen Scene (not actual meshes) for speed, gather radiance from Lumen Surface Cache, propagate to **Screen Probes** (sparse probes placed at screen-space shading points), accumulate with temporal filtering.",
            "**Screen-space techniques as complements**: probes provide low-frequency, scene-wide GI. Screen-space techniques add high-frequency local detail. **SSAO** (short-range occlusion), **RTAO** (hardware RT ambient occlusion, higher quality), **SSR** (screen-space reflections). Real production renderers stack all of these: probes for far-field diffuse GI, SSAO for local occlusion, SSR for glossy reflections, hardware RT for shadows and contact shadows. Each technique covers a different range and frequency band of the GI signal.",
          ],
          resources: [
            { label: "DDGI paper - Majercik et al. 2019", url: "https://jcgt.org/published/0008/02/01/", type: "paper" },
            { label: "Unreal Engine 5 - Lumen technical details", url: "https://dev.epicgames.com/documentation/en-us/unreal-engine/lumen-technical-details-in-unreal-engine", type: "docs" },
          ],
        },
        {
          title: "GPU-driven rendering: indirect draws and meshlets",
          body: [
            "**Traditional CPU-driven rendering**: the CPU loops over all scene objects, performs frustum culling, sorts by material, and issues one draw call per object. With 10,000 objects, that's 10,000 CPU-side draw calls with 10,000 state checks. The CPU-GPU bus and draw call overhead become the bottleneck, not the GPU. Modern AAA scenes have 100,000-10,000,000 objects.",
            "**Indirect draw commands**: instead of the CPU issuing draw calls, the GPU generates its own draw commands. `glMultiDrawIndirect` / `vkCmdDrawIndirect`: the CPU supplies a buffer of draw call parameters (`DrawArraysIndirectCommand { count, instanceCount, first, baseInstance }`); the GPU fills this buffer from a compute pass and draws them all in a single CPU-side call. The GPU can: run a compute shader that culls against the view frustum (using the GPU's register file for fast AABB tests), checks LOD conditions, and writes draw commands only for visible objects - all without CPU involvement. This scales to millions of objects with near-zero CPU overhead.",
            "**Meshlets (mesh shaders)**: traditional triangle list rendering has a fixed pipeline: vertex fetch → vertex shader → rasterizer. **Mesh shaders** (supported on NVIDIA Turing+, AMD RDNA2+, DX12 Ultimate, Vulkan VK_EXT_mesh_shader) replace the vertex/geometry pipeline with two new stages: the **task shader** (amplification shader) and the **mesh shader**. A meshlet is a small chunk of a mesh (~64-128 triangles) with its own local index buffer. The task shader: run once per cluster of meshlets, perform culling (frustum, backface, occlusion), output which meshlets to process. The mesh shader: run once per surviving meshlet, emit the triangle list. Benefits: per-meshlet culling (cull before rasterization at much finer granularity than per-object), procedural geometry generation, and improved cache locality.",
            "**GPU occlusion culling**: frustum culling removes objects outside the view, but many visible objects are occluded by other objects (behind walls, hidden by terrain). **Two-pass occlusion culling with HZB (Hierarchical Z-Buffer)**: pass 1 renders last frame's occluders to a depth buffer, downsample to create an HZB (a mip pyramid where each mip level stores the maximum depth in a 2x2 block of the previous level). Pass 2: in a compute shader, test each object's bounding sphere against the HZB at the appropriate mip level - objects that pass all mip levels are visible. Submit only visible objects with indirect draws. This is the standard approach in Nanite (UE5), Frostbite, and modern AAA engines.",
            "**Nanite (UE5)**: a virtualized geometry system where meshes have no artist-defined LODs - Nanite automatically selects triangle density to fill roughly one pixel per triangle. Implementation: meshes are stored as a **compressed cluster hierarchy** (meshlets at multiple detail levels). At runtime, the cluster hierarchy is traversed on the GPU: determine the appropriate detail level for each cluster based on screen-space error budget; select clusters via GPU-driven indirect rendering; stream in needed cluster data from disk via Nanite's streaming system. Result: billions of triangles in a scene with performance proportional to screen pixels, not scene triangle count.",
          ],
          resources: [
            { label: "Wihlidal - GPU-Driven Rendering Pipelines (GDC 2015)", url: "https://advances.realtimerendering.com/s2015/aaltonenhaar_siggraph2015_combined_final_footer_220dpi.pdf", type: "paper" },
            { label: "UE5 Nanite - overview doc", url: "https://dev.epicgames.com/documentation/en-us/unreal-engine/nanite-virtualized-geometry-in-unreal-engine", type: "docs" },
          ],
        },
        {
          title: "ML in graphics: DLSS, FSR, and neural rendering",
          body: [
            "**The resolution-quality tradeoff**: rendering at native 4K is 4x as expensive as 1080p. Render at a lower resolution, then upscale to the display resolution with an intelligent upscaler that adds detail. Naive upscaling (bilinear/bicubic) produces blurry results. **ML-based super-resolution** uses neural networks trained on high-resolution ground-truth images to add plausible high-frequency detail during upscaling.",
            "**DLSS (Deep Learning Super Sampling, NVIDIA)**: renders at a lower resolution (e.g., 1080p) and upscales to 4K using a convolutional neural network. Inputs to the network: the current low-resolution frame, the previous high-resolution frame (reprojected using motion vectors), and the motion vector field. The CNN was trained offline on thousands of high-resolution images; inference runs on the Tensor Cores (dedicated ML hardware) in RTX GPUs. DLSS 3.5 (Frame Generation): additionally generates entirely synthetic intermediate frames between rendered frames using an optical flow network, effectively doubling the perceived frame rate. The generated frames are composited with real rendered frames.",
            "**FSR (FidelityFX Super Resolution, AMD)**: a spatial-only upscaler (no motion vectors or temporal data in FSR 1.0). FSR 1.0 uses an edge-reconstruction algorithm (EASU - Edge Adaptive Spatial Upsampling) followed by a sharpening pass (RCAS). FSR 2.0/3.0 adds temporal accumulation (similar to DLSS 2.x) and does not require specialized hardware - runs on any GPU, including older cards and consoles. The open-source nature makes FSR ubiquitous: supported by far more platforms than DLSS.",
            "**Neural Radiance Fields (NeRF)**: represent a scene as an MLP $F(\\mathbf{x}, \\mathbf{d}) \\to (\\mathbf{c}, \\sigma)$ that maps 3D position + viewing direction to color and density. Render by marching rays through the volume, accumulating color via volume rendering: $C = \\sum_i T_i \\alpha_i c_i$ where $T_i = \\prod_{j<i}(1-\\alpha_j)$ is transmittance. Original NeRFs: minutes per frame to render, minutes-hours to train per scene. **Instant NGP** (NVIDIA 2022): encode the scene in a hash-grid of feature vectors, with a tiny MLP decoding features to color/density - train in 5 minutes, render at real-time (40+ fps). **3D Gaussian Splatting** (2023): represent the scene as 3D Gaussians (position, covariance, color SH, opacity), rasterize via splat-sort-and-project - real-time at 100+ fps, train in 30 minutes. Gaussian Splatting is being actively integrated into game engines as a novel view synthesis and relighting tool.",
            "**Neural materials and appearance capture**: ML models predict material properties (BRDF parameters) from a single photograph of a surface, replacing manual material creation. NVIDIA's MaterialGAN, Adobe Substance Sampler AI, and similar tools: input a photo, output metallic/roughness/normal maps compatible with PBR pipelines. For characters: **neural face rendering** replaces traditional texture/shader pipelines with a network that maps expression parameters to rendered appearance - used in Unreal Engine's MetaHuman (Mesh-to-MetaHuman conversion). The geometry and shading are still traditional; the network captures the subtle subsurface scattering and translucency of skin that is difficult to author manually.",
          ],
          resources: [
            { label: "DLSS 2.0 technical blog - NVIDIA", url: "https://developer.nvidia.com/blog/nvidia-dlss-2-0-a-big-leap-in-ai-rendering/", type: "docs" },
            { label: "3D Gaussian Splatting (2023 paper)", url: "https://repo-sam.inria.fr/fungraph/3d-gaussian-splatting/", type: "paper" },
          ],
        },
      ],
      resources: [
        { label: "Ray Tracing in One Weekend (free - 3 books)", url: "https://raytracing.github.io/", type: "book" },
        { label: "PBRT - Physically Based Rendering (free online)", url: "https://pbr-book.org/", type: "book" },
        { label: "SIGGRAPH Advances in Real-Time Rendering course", url: "https://advances.realtimerendering.com/", type: "course", note: "annual state-of-the-art from AAA studios" },
      ],
      connections: [
        { to: "gpu-matmul", note: "DLSS/neural denoising run on Tensor Cores - same hardware as ML matmul" },
        { to: "ai-diffusion", note: "Diffusion models for texture synthesis and neural material generation overlap with generative AI" },
      ],
    },
  ],
});

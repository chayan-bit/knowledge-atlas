atlasAdd({
  id: "crypto",
  topics: [
    {
      id: "crypto-ecc",
      title: "Elliptic curve cryptography",
      level: "Intermediate → Advanced",
      body: [
        "EC over finite fields (point addition/doubling, the group law), scalar multiplication (double-and-add, why ECDLP is hard), curve parameters, and the practical curves: P-256 (NIST), **secp256k1** (Bitcoin/Ethereum), and **Curve25519** (Montgomery form, twist-safe, constant-time by design).",
        "secp256k1 is exactly the curve every blockchain signature uses - this topic is the hinge between crypto and web3.",
      ],
      subtopics: [
        {
          title: "Elliptic curves over finite fields - the group law",
          body: [
            "An **elliptic curve** over a field $F$ is the set of solutions $(x, y) \\in F \\times F$ to the short Weierstrass equation $y^2 = x^3 + ax + b$ (over fields of characteristic $\\ne 2, 3$), together with a special 'point at infinity' $O$ that serves as the identity element. For cryptography, $F = \\mathrm{GF}(p)$ for a large prime $p$, meaning $x, y, a, b$ are all integers modulo $p$. The equation $y^2 \\equiv x^3 + ax + b \\pmod p$ has solutions that form the curve. The discriminant $4a^3 + 27b^2 \\ne 0$ ensures the curve is non-singular (no cusps or self-intersections, which would degenerate the group structure).",
            "The set of curve points forms a **group** under the chord-and-tangent law. The group operation (point addition, written $P + Q$) is defined geometrically: draw the line through P and Q (or the tangent at P if $P = Q$); it intersects the curve at a third point (by Bezout's theorem, a non-vertical line intersects a cubic at three points counting multiplicity); reflect that third point across the x-axis. The result is $P + Q$. The point at infinity $O$ is the identity: $P + O = P$ for all P. Every point $(x, y)$ has inverse $(x, -y)$ (which is also on the curve since $(-y)^2 = y^2$). The group is abelian ($P + Q = Q + P$, obvious from the geometric construction's symmetry).",
            "Algebraically, for points $P = (x_1, y_1)$ and $Q = (x_2, y_2)$ with $P \\ne \\pm Q$: the slope of the chord is $\\lambda = (y_2 - y_1) / (x_2 - x_1)$, and $x_3 = \\lambda^2 - x_1 - x_2$, $y_3 = \\lambda(x_1 - x_3) - y_1$. For doubling $P + P = 2P$: the slope of the tangent is $\\lambda = (3x_1^2 + a) / (2y_1)$, and the same formulas apply. All divisions are modular (using modular inverses, since we're in GF(p)). These formulas are exact - they apply over any field, not just the reals. The geometric intuition is a guide, but the algebra is the actual definition.",
            "The group order $|E(\\mathrm{GF}(p))|$ - the number of points on the curve - is approximately $p + 1$ (with deviation at most $2\\sqrt{p}$, by Hasse's theorem). This is important: the group is large (roughly p-sized), and for a 256-bit prime $p$, the group has roughly $2^{256}$ elements. This large order is what makes the ECDLP hard. Computing the exact group order efficiently requires Schoof's algorithm (1985) or its improvements (SEA algorithm), which runs in polynomial time - curve designers must compute the order and verify it is near-prime before deploying a curve.",
            "The group is not always cyclic (a property of certain primes and curve parameters), but for cryptographic curves, the group must have a large prime-order subgroup. The **cofactor** h is defined by $|E(\\mathrm{GF}(p))| = h \\cdot q$ where q is the largest prime factor (ideally the whole group is prime-order, h=1, or cofactor h=4 for Curve25519/Ed25519). A small cofactor means that most points are in the prime-order subgroup and cofactor clearing (multiplying a point by h) ensures you are in the right subgroup. Curves with large cofactors introduce the small-subgroup attacks discussed under DH.",
          ],
          resources: [
            { label: "A (Relatively Easy to Understand) Primer on ECC - Cloudflare", url: "https://blog.cloudflare.com/a-relatively-easy-to-understand-primer-on-elliptic-curve-cryptography/", type: "docs" },
            { label: "Andrea Corbellini - ECC gentle introduction (part 1)", url: "https://andrea.corbellini.name/2015/05/17/elliptic-curve-cryptography-a-gentle-introduction/", type: "docs" },
          ],
        },
        {
          title: "Scalar multiplication and the ECDLP",
          body: [
            "**Scalar multiplication** $nP = P + P + \\ldots + P$ (n times) is the core ECC operation. Naively, this takes $O(n)$ additions - useless for $n \\approx 2^{256}$. The **double-and-add** algorithm (the ECC analogue of square-and-multiply in RSA) reduces this to $O(\\log n)$ group operations: process the bits of n from most significant to least significant; at each step double the current accumulator; if the current bit is 1, also add P. This computes $nP$ in ~256 doublings and ~128 additions on average for a 256-bit scalar - extremely efficient.",
            "The **Elliptic Curve Discrete Logarithm Problem (ECDLP)** is: given a point $Q = nP$ (the 'scalar multiple'), find the scalar $n$. Going from $n$ to $nP$ is fast ($O(\\log n)$ operations). Going from $nP$ back to $n$ is believed infeasible for curves with prime-order groups. The best known algorithms for the ECDLP are generic: **Pohlig-Hellman** (ineffective for prime-order curves), **Baby-step Giant-step** (BSGS, $O(\\sqrt{|G|})$ time and space), and **Pollard's rho** ($O(\\sqrt{|G|})$ time, $O(1)$ space - the best practical generic method). For a 256-bit prime-order group, the expected work is ~$2^{128}$ group operations - the 128-bit security level. This is why 256-bit curves match 3072-bit RSA for security: RSA is vulnerable to the sub-exponential NFS, while ECC is not.",
            "No **special-structure attacks** are known for the standard cryptographic curves. But several classes of curves are *weak* and must be avoided: **anomalous curves** (where $|E(\\mathrm{GF}(p))| = p$) allow the **SSSA attack** (Semaev-Smart-Satoh-Araki), lifting the DLP to the p-adics and solving it in polynomial time. **Supersingular curves** (where $|E(\\mathrm{GF}(p))| = p + 1$) allow Menezes-Okamoto-Vanstone (MOV) reduction of the ECDLP to a DLP in an extension field, solvable via index calculus. **MOV threshold**: if the embedding degree k (smallest k with $p^k \\equiv 1 \\pmod n$) is small ($\\le 6$), the ECDLP can be solved via pairings in $\\mathrm{GF}(p^k)$. Standard curves are designed with embedding degree in the hundreds, making the MOV attack impractical. An important exception: **BN curves** and **BLS12 curves** are *intentionally* supersingular (small embedding degree) for pairing-based cryptography - this is a feature, not a bug, used in ZK-SNARKs and BLS signatures, but these curves offer different (and lower) ECDLP security by design.",
            "**Projective coordinates** are used in efficient implementations to avoid modular inversion (the most expensive field operation) during point addition. In **Jacobian coordinates**, a projective point $(X:Y:Z)$ represents the affine point $(X/Z^2, Y/Z^3)$. All group operations can be expressed in terms of projective coordinates using only field multiplications and additions - no inversions. A conversion from projective to affine (dividing by $Z^2$ and $Z^3$) requires one inversion, done at the end after all operations are complete. A full scalar multiplication in Jacobian coordinates requires ~$3.5M + 11S$ multiplications and squarings per doubling and ~$10M + 5S$ per addition, versus ~$1I + 3M + 3S$ in affine (where I is an inversion costing ~$80M$). The speedup from projective coordinates is roughly $8\\times$ for doublings and $3\\times$ for additions.",
            "**Scalar multiplication algorithms** beyond double-and-add improve performance and security. **Non-adjacent form (NAF)** reduces the average number of non-zero bits in the scalar (using digits in $\\{-1, 0, 1\\}$), reducing the number of additions. **Windowed methods** precompute small multiples $1P, 3P, 5P, \\ldots, (2^w-1)P$ and process $w$ bits at a time. The **Montgomery ladder** is a constant-time scalar multiplication algorithm: it processes each bit of the scalar using two simultaneous additions where the result of each step depends on both 0-bit and 1-bit cases, computed unconditionally. The selection of which result to use is done with a constant-time conditional swap - making the entire computation data-independent in timing. The Montgomery ladder is the basis of X25519 (Curve25519 ECDH) and is why Curve25519 is considered state-of-the-art for safe, fast, constant-time ECC.",
          ],
          resources: [
            { label: "Andrea Corbellini - ECC part 2: finite fields and discrete logarithms", url: "https://andrea.corbellini.name/2015/05/23/elliptic-curve-cryptography-finite-fields-and-discrete-logarithms/", type: "docs" },
          ],
        },
        {
          title: "The standard curves - P-256, secp256k1, and Curve25519",
          body: [
            "**P-256 (secp256r1, prime256v1)** is the NIST-standardized curve defined over the 256-bit prime $p = 2^{256} - 2^{224} + 2^{192} + 2^{96} - 1$. The 'r1' suffix means 'randomly generated': NIST published the curve parameters in 1999 with a seed value for the curve constants but did not explain *why* that particular seed was chosen. This sparked persistent concern that NIST (or the NSA, which was involved in the standardization) may have chosen a backdoored seed - one where the discrete logarithm problem might be easier due to some secret structure known only to the designers. This 'nothing-up-my-sleeve' concern has never been resolved with a clean public derivation of the seed. P-256 is nonetheless widely deployed (used in HTTPS, Android, iOS, TLS 1.3) and there is no known weakness - but the opacity of the parameter choice remains a concern in the security community.",
            "**secp256k1** is the curve used by Bitcoin and Ethereum. It is defined over $p = 2^{256} - 2^{32} - 977$ with $a = 0, b = 7$: the equation is $y^2 = x^3 + 7 \\pmod p$. The 'k1' means 'Koblitz' - a class of curves where $a = 0$, enabling faster endomorphism-based scalar multiplication. secp256k1 was explicitly avoided by NIST (who preferred the 'r' curves) but chosen by Satoshi for Bitcoin, where the preference for 'industry standard with no NIST/NSA involvement' matched early cypherpunk values. The endomorphism $(x, y) \\to (\\beta x, y)$ (where $\\beta$ is a cube root of 1 mod p) allows the scalar $k$ to be decomposed as $k = k_1 + k_2 \\lambda$ with $k_1, k_2 \\approx \\sqrt{p}$, effectively halving the number of doublings needed. secp256k1 is specifically not twist-safe (the cofactor of the twist is large), which is why Ethereum's ECIES implementation must validate points, and why the Bitcoin/Ethereum ECDSA libraries must reject low-order points.",
            "**Curve25519 and X25519** (Daniel J. Bernstein, 2006) were designed with explicit security, simplicity, and implementation safety goals. The curve is $y^2 = x^3 + 486662x^2 + x$ over $p = 2^{255} - 19$ (a prime close to a power of two, enabling fast arithmetic). Its group has order $8 \\times q$ where q is a 252-bit prime - cofactor 4 on each of the main and twist curves. The key security properties by design: (1) **Twist security**: the quadratic twist (the 'other' curve with opposite sign in the equation) has approximately the same security level as the main curve - an attacker who sends a point on the twist gets no useful information. This eliminates a class of invalid-point attacks. (2) **Constant-time by design**: X25519 takes only the x-coordinate as input and output (using the Montgomery ladder), so there is no point validation needed and no branch on secret data. (3) **Simple implementation**: all constants were chosen for efficient reduction ($2^{255} - 19$ is a 'pseudo-Mersenne prime' making mod-p arithmetic fast), and the protocol function takes 32-byte scalars and returns 32-byte x-coordinates - no ASN.1, no DER encoding, no point compression or decompression.",
            "**Ed25519** (Edwards curves, Bernstein et al., 2011) is the companion signature scheme to X25519. It uses the Edwards form of Curve25519 (birationally equivalent, a different affine representation that enables complete addition laws - no special case for $P = Q$), allowing all group operations to use the same formula regardless of input, inherently constant-time. EdDSA (the signature scheme on Edwards curves) uses **deterministic nonces** (derived from the private key and message via a PRF) instead of random nonces, eliminating the nonce-reuse catastrophe that has plagued ECDSA implementations. Ed25519 signatures are 64 bytes, verification is fast, and the scheme has clean formal security proofs. It is the preferred signature scheme in modern protocols: WireGuard, SSH, Signal, age encryption.",
            "Comparing the curves in context: for TLS 1.3, X25519 is the preferred key exchange; P-256 is the fallback for compatibility with older systems (it is in essentially every FIPS-validated implementation). For signatures in modern code, Ed25519 is preferable. For Ethereum/Bitcoin transactions, secp256k1 is mandatory (the chain protocol requires it). For ZK-SNARKs on Ethereum, BN254 (a pairing-friendly curve) and BLS12-381 are used - these are purpose-built for pairing efficiency, not for ECDLP hardness at the 256-bit level, and must be understood as 128-bit-security or lower curves depending on the subgroup. The practical rule: use Curve25519/Ed25519 for new systems outside blockchain contexts; understand secp256k1 deeply for blockchain work; and know P-256 for FIPS compliance.",
          ],
          resources: [
            { label: "Curve25519 - original paper (Bernstein 2006)", url: "https://cr.yp.to/ecdh/curve25519-20060209.pdf", type: "paper" },
            { label: "Ed25519 - high-speed high-security signatures (Bernstein et al.)", url: "https://ed25519.cr.yp.to/ed25519-20110926.pdf", type: "paper" },
          ],
        },
        {
          title: "Invalid-curve and twist attacks on ECC implementations",
          body: [
            "ECC implementations that do not validate received curve points are vulnerable to **invalid-curve attacks**. The attack works as follows: in ECDH, Alice receives a point $Q$ from Bob and computes $aQ$ (her private scalar times the received point). If $Q$ is not actually on the agreed curve $E$, it may lie on a **different** elliptic curve $E'$ over the same field - defined by $y^2 = x^3 + ax + b'$ for some different $b'$. The group structure of $E'$ may have a small order or smooth order, making the DLP on $E'$ easy. An attacker chooses $Q$ to lie on such a weak curve, receives $aQ$, and solves the easy DLP on $E'$ to recover $a \\bmod |E'|$. Repeating with different small-order curves, they reconstruct $a$ via CRT.",
            "Concrete example: secp256k1 (Ethereum/Bitcoin curve, $b=7$) and a modified curve with $b=8$ share the same field and $a=0$, but have different group orders. An attacker sends a point on the $b=8$ curve. If Alice computes $a \\times P_{b=8}$ without checking that the received point is on the $b=7$ curve, she uses her private key in a smaller, easier DLP context. This attack is well-documented against Ethereum's `eth_sign` for off-curve inputs, and any ECDH implementation that omits point validation.",
            "**Cofactor attacks** are related: even for points on the correct curve, if the curve has a non-trivial cofactor h (like Curve25519 with cofactor 4), a point of small order (one of the cofactor's prime factors) can be sent. The ECDH output $aQ$ for a small-order $Q$ cycles through only a few values - leaking $a \\bmod \\mathrm{ord}(Q)$. Defense: cofactor multiplication (compute $haQ$ instead of $aQ$ to clear the small-order component) or point validation. For Curve25519/X25519, the cofactor clearing is built into the spec: the scalar is clamped (bits 0, 1, 2 of the scalar are cleared, multiplying by 8 to clear cofactor 8 of the combined twist and main curve). This makes X25519 immune to cofactor attacks by construction.",
            "**Twist security**: every elliptic curve $E: y^2 = x^3 + ax + b$ over $\\mathrm{GF}(p)$ has a **quadratic twist** $E': y^2 = x^3 + a c^2 x + b c^3$ for any non-square $c \\bmod p$. The twist has different group order, and if the twist's ECDLP is easier (smaller security level), an attacker who sends a point on the twist can recover information about the scalar. Curves that are only specified by x-coordinate (like X25519's Diffie-Hellman, which takes only $x \\bmod p$) must be either twist-safe (the twist has comparable security) or must validate that the received x-coordinate corresponds to a point on the correct curve. Curve25519 was explicitly designed to be twist-safe: the twist has order $4 \\times q'$ for a 253-bit prime $q'$, giving ~125-bit security - only slightly less than the main curve's ~128 bits, and the cofactor clearing eliminates the cofactor 4 contribution.",
            "The practical lesson: **always validate curve points** in implementations that accept external points. For implementations using well-designed libraries (libsodium's `crypto_box`, `crypto_scalarmult_base`), the validation is built in. For raw ECC implementations, the validation checklist is: (1) $y^2 \\equiv x^3 + ax + b \\pmod p$ is satisfied; (2) the point is not the identity (point at infinity); (3) the point has the expected order (for prime-order curves: verify $nP = O$ for the group order $n$). For high-assurance implementations, formal verification of these checks in C or assembly (using tools like F*, EverCrypt, or Vale) eliminates the possibility of subtle implementation bugs in the validation code.",
          ],
          resources: [
            { label: "Andrea Corbellini - ECC part 4: attacks on elliptic curves", url: "https://andrea.corbellini.name/2015/06/08/elliptic-curve-cryptography-breaking-security-and-a-comparison-with-rsa/", type: "docs" },
          ],
        },
      ],
      resources: [
        { label: "A (Relatively Easy to Understand) Primer on ECC - Cloudflare", url: "https://blog.cloudflare.com/a-relatively-easy-to-understand-primer-on-elliptic-curve-cryptography/", type: "docs" },
        { label: "Andrea Corbellini - ECC series", url: "https://andrea.corbellini.name/2015/05/17/elliptic-curve-cryptography-a-gentle-introduction/", type: "docs" },
      ],
      connections: [
        { to: "web3-evm", note: "secp256k1 ECDSA signs every Ethereum/Bitcoin transaction" },
        { to: "crypto-signatures", note: "Curves are the substrate for ECDSA and EdDSA" },
      ],
    },
  ],
});

atlasAdd({
  id: "aiml",
  topics: [
    {
      id: "ai-math",
      subtopics: [
        {
          title: "Linear algebra: vectors, matrices, and the geometry of data",
          body: [
            "Machine learning is, at its core, a discipline of **high-dimensional geometry**. Data points are vectors in R^n; a dataset is a matrix where rows are examples and columns are features. Every forward pass through a neural network is a sequence of matrix multiplications, and every optimization step moves a point through a high-dimensional parameter space. This is not an analogy — it is the literal computation. Understanding the linear-algebraic structures underneath ML means you can reason about what each layer does, why certain architectures work, and where numerical instabilities come from.",
            "**Matrix multiplication** `C = AB` (where A is m×k and B is k×n, producing C as m×n) is the dominant operation in all of deep learning. The entry `C_{ij}` is the dot product of row i of A with column j of B. The geometric interpretation: matrix multiplication is a linear transformation that rotates, scales, and skews the input space. A stack of linear layers with no nonlinearities collapses to a single linear transformation — this is why nonlinear activation functions are essential. The computational cost is O(mnk), and optimizing this single operation is the subject of an entire engineering discipline (cuBLAS, tensor cores, Triton kernels).",
            "**Eigendecomposition** is the factorization `A = V Λ V^{-1}` for a square matrix A, where the columns of V are eigenvectors and the diagonal of Λ holds eigenvalues. An eigenvector v of A satisfies `Av = λv` — A acts on v by scaling it by λ without rotating it. Eigendecomposition reveals the 'natural axes' of a linear transformation: the directions of maximum stretch (large |λ|). For symmetric positive semi-definite matrices — the covariance matrix being the canonical example — all eigenvalues are non-negative and eigenvectors are orthogonal. **PCA (Principal Component Analysis)** is exactly eigendecomposition of the data covariance matrix: the top-k eigenvectors define the k directions of maximum variance, and projecting data onto them gives the optimal k-dimensional linear embedding (in terms of preserved variance). PCA is not just a tool — it is a theorem about the structure of linear projections.",
            "**SVD (Singular Value Decomposition)** generalizes eigendecomposition to non-square matrices: `A = U Σ V^T` where U and V are orthogonal and Σ is diagonal. SVD always exists, even for non-square matrices. The singular values (diagonal of Σ) quantify how much the matrix 'stretches' in each direction. SVD underlies the best algorithms for numerically solving linear systems, computing matrix rank, and low-rank approximation. In ML, **LoRA** (Low-Rank Adaptation) explicitly exploits this: a large weight matrix W is approximated as `W + AB` where A and B are thin matrices, implicitly constraining the update to a low-rank manifold. The justification is that fine-tuning updates are empirically low-rank — the singular values of the update matrix decay quickly. SVD makes this precise.",
            "**The rank of a matrix** counts linearly independent rows (or columns) — the dimension of its image. A rank-deficient matrix means some input dimensions are 'collapsed' to zero in the output: information is destroyed. In practice, a weight matrix that becomes approximately rank-deficient during training is a sign that the layer is compressing information heavily — useful for bottleneck architectures. The **condition number** (ratio of largest to smallest singular value) quantifies how ill-conditioned a system is: a high condition number means the matrix nearly collapses some directions to zero, and solving linear systems involving it is numerically unstable. Gradient descent on poorly conditioned loss landscapes is exactly this problem.",
          ],
          resources: [
            { label: "Gilbert Strang — Linear Algebra (MIT OpenCourseWare, free)", url: "https://ocw.mit.edu/courses/18-06-linear-algebra-spring-2010/", type: "course" },
            { label: "Mathematics for Machine Learning — Deisenroth et al. (free PDF)", url: "https://mml-book.github.io/book/mml-book.pdf", type: "book" },
            { label: "3Blue1Brown — Essence of Linear Algebra playlist", url: "https://www.3blue1brown.com/topics/linear-algebra", type: "video" },
          ],
        },
        {
          title: "Calculus for ML: the chain rule is backpropagation",
          body: [
            "Neural network training reduces to one problem: computing the gradient of a scalar loss function with respect to thousands or millions of parameters. The algorithm that does this, **backpropagation**, is not a mysterious ML-specific algorithm — it is the **chain rule of calculus**, systematically applied to a computation graph. Every other aspect of modern training (Adam, learning rate schedules, gradient clipping) is contingent on this one operation being correct and efficient. Deriving backprop from scratch at least once is not optional; it is the boundary between using ML and understanding it.",
            "The **chain rule** states: if `z = f(y)` and `y = g(x)`, then `dz/dx = (dz/dy)(dy/dx)`. For vector-valued functions, this generalizes to **Jacobian matrices**: if `y: R^m → R^n` and `z: R^n → R^p`, then `dz/dx = (dz/dy)(dy/dx)` where the multiplications are matrix multiplications of the Jacobian matrices. A neural network is a composition of many functions, and its gradient is a product of Jacobians. In **reverse-mode automatic differentiation** (the foundation of backprop), you evaluate the function forward (computing each intermediate value), then propagate gradients backward — multiplying by local Jacobians from the output layer to the input. This is O(cost of forward pass) in both time and (with gradient checkpointing) space.",
            "For a single neuron `z = w^T x + b`, `dL/dw = (dL/dz) x` and `dL/db = dL/dz`. For a matrix multiplication `Z = XW`, `dL/dW = X^T (dL/dZ)` and `dL/dX = (dL/dZ) W^T`. These two rules, composed, are all you need to derive the gradients for a deep linear network. Adding nonlinearities: for ReLU `σ(x) = max(0,x)`, the local gradient is 1 if x > 0 and 0 otherwise (a diagonal Jacobian, element-wise). For softmax + cross-entropy (the canonical final layer), the combined gradient simplifies beautifully to `p - y_true` where p is the softmax output — an artifact of the cancellation between softmax's exponentials and log's derivatives. This simplification is worth deriving once; it is why the loss function choice matters for optimization conditioning.",
            "**Jacobians and Hessians** are the first and second derivative objects for vector-valued and scalar functions. The Jacobian `J ∈ R^{n×m}` of a map `f: R^m → R^n` has entry `J_{ij} = ∂f_i/∂x_j`. The Hessian `H ∈ R^{n×n}` of a scalar `L: R^n → R` has entry `H_{ij} = ∂²L/∂x_i∂x_j`. The Hessian governs second-order optimization (Newton's method, natural gradient), the curvature of the loss landscape, and the conditions for a local minimum (positive definite Hessian). Computing the Hessian exactly for modern networks (10^9 parameters → 10^18 entry matrix) is infeasible; practical second-order methods (K-FAC, Shampoo) use structured approximations. Vanishing and exploding gradients — the training-breaking problem that LSTMs and residual connections were designed to fix — are consequences of the Jacobian product in the chain rule having eigenvalues << 1 or >> 1 when multiplied over many layers.",
            "**Automatic differentiation (AD)** is the principled implementation of these rules. There are two modes: **forward mode** (accumulate gradient alongside function value, efficient when output dimension << input dimension) and **reverse mode** (the mode PyTorch and JAX use, efficient when loss is scalar — the universal case in training). PyTorch implements reverse-mode AD via a **dynamic computation graph**: as you execute operations on tensors, a graph of operations and their saved inputs is built (`requires_grad=True` tensors). Calling `.backward()` traverses this graph in reverse, multiplying upstream gradients by local Jacobians via registered `backward()` functions. Writing a custom `torch.autograd.Function` with explicit `forward()` and `backward()` methods lets you implement numerically stable or hardware-efficient operations that bypass PyTorch's generic graph.",
          ],
          resources: [
            { label: "Karpathy — micrograd (build backprop from scratch, 100 lines)", url: "https://github.com/karpathy/micrograd", type: "repo" },
            { label: "The Matrix Calculus You Need for Deep Learning (Parr & Howard)", url: "https://explained.ai/matrix-calculus/", type: "docs" },
            { label: "CS231n — Backprop notes (with worked examples)", url: "https://cs231n.github.io/optimization-2/", type: "docs" },
          ],
        },
        {
          title: "Probability theory and Bayesian reasoning",
          body: [
            "Machine learning is statistical estimation: given data assumed to be drawn from some distribution, infer the parameters of a model. Every loss function, regularizer, and evaluation metric has a probabilistic interpretation. Cross-entropy loss is maximum likelihood estimation. L2 regularization is a Gaussian prior on weights. The bias-variance decomposition is a variance decomposition under the sampling distribution. Working practitioners often apply these without knowing the probabilistic story, but the story is what lets you adapt to novel settings, diagnose failures, and design new algorithms.",
            "**Random variables** are functions from a probability space to R (or R^n). A random variable X is characterized by its **probability distribution**: for discrete X, the probability mass function P(X=x); for continuous X, the probability density function f(x) where P(a ≤ X ≤ b) = ∫_a^b f(x)dx. The **expectation** E[X] = Σ x P(x) (discrete) or ∫ x f(x)dx (continuous) is the mean. **Variance** Var(X) = E[(X-E[X])²] = E[X²] - (E[X])² measures spread. The **covariance** Cov(X,Y) = E[(X-E[X])(Y-E[Y])] and **correlation** ρ = Cov(X,Y)/√(Var(X)Var(Y)) measure linear dependence. The covariance matrix Σ of a random vector X encodes all pairwise covariances; its eigenvectors are the PCA directions.",
            "**Bayes' theorem** `P(θ|D) = P(D|θ)P(θ)/P(D)` is the fundamental update rule: given prior beliefs P(θ) about parameters θ and observed data D with likelihood P(D|θ), the posterior P(θ|D) is the updated belief. In **maximum likelihood estimation (MLE)**, you maximize P(D|θ) over θ — treating parameters as fixed unknowns, data as random. In **maximum a posteriori (MAP)** estimation, you maximize P(θ|D) = P(D|θ)P(θ)/P(D) — adding a prior term that regularizes parameters toward their prior mean. MAP with a Gaussian prior N(0, σ²I) on weights gives exactly L2 regularization (weight decay): the log-prior contributes a term `-λ||θ||²` to the log-posterior objective. This is why weight decay has a Bayesian justification.",
            "The **central distributions** you must internalize: **Gaussian/Normal** N(μ,σ²) — the limit distribution by the Central Limit Theorem, parameterized by mean and variance, defined on all of R, closed under linear transformation; **Bernoulli/Binomial** — for binary outcomes; **Categorical/Multinomial** — for discrete multi-class outcomes (the distribution modeled by softmax); **Exponential/Gamma** — for positive-valued quantities; **Beta/Dirichlet** — conjugate priors for Bernoulli/Categorical. The softmax function `σ(z)_i = exp(z_i)/Σ_j exp(z_j)` is the canonical way to parameterize a Categorical distribution: it maps logits (any real numbers) to probabilities (non-negative, summing to 1). The log-softmax is the log of this, which is what cross-entropy loss computes — log probability of the true class under the model.",
            "**Conditional independence** and **graphical models** (Bayes nets, Markov random fields) formalize the factorization structure of joint distributions. If X ⊥ Y | Z, then P(X,Y|Z) = P(X|Z)P(Y|Z) — knowing Z makes X and Y independent. Neural networks implicitly learn conditional independence structure: the layers represent a hierarchy of conditional representations, and batch normalization implicitly assumes conditional independence across the batch dimension. **Variational inference** (the foundation of VAEs) approximates an intractable posterior P(θ|D) with a tractable family Q by minimizing the **KL divergence** KL(Q||P). The **ELBO (Evidence Lower Bound)** = E_Q[log P(D|θ)] - KL(Q||P) is the objective — it is both a lower bound on log P(D) and the objective you maximize. This framework unifies latent variable models, VAEs, and modern probabilistic ML.",
          ],
          resources: [
            { label: "Probability Theory: The Logic of Science — Jaynes (free draft chapters)", url: "https://bayes.wustl.edu/etj/prob/book.pdf", type: "book" },
            { label: "CS229 Probability review notes — Stanford (Andrew Ng)", url: "https://cs229.stanford.edu/section/cs229-prob.pdf", type: "docs" },
            { label: "StatQuest — Probability and Statistics playlist", url: "https://www.youtube.com/@statquest", type: "video" },
          ],
        },
        {
          title: "Information theory: entropy, KL divergence, and cross-entropy from MLE",
          body: [
            "**Information theory** (Shannon, 1948) answers the question: how do we quantify information? The key insight is that information is surprise — a certain event carries no information; an unlikely event carries a lot. The **self-information** of an event with probability p is `-log(p)` (using natural log for nats, log₂ for bits). This is the unique function satisfying: rare events have high information, certain events have zero information, and independent events have additive information. The log makes entropy a tool for quantifying uncertainty about distributions.",
            "**Shannon entropy** `H(X) = -Σ p(x) log p(x)` is the expected self-information over a distribution — the average surprise. For a Bernoulli(p) coin, `H = -p log p - (1-p) log(1-p)`, which peaks at 1 bit (log₂ units) when p=0.5 (maximum uncertainty) and reaches 0 when p=0 or p=1 (certain outcome, no surprise). For a categorical distribution over k classes, entropy is maximized (log k) by the uniform distribution and minimized (0) by a point mass. Entropy is the information-theoretic lower bound on the average code length for any lossless compression scheme — the **source coding theorem**. Neural network outputs that are sharply peaked (low entropy) are confident; outputs near uniform (high entropy) are uncertain.",
            "**KL divergence** `KL(P||Q) = Σ_x P(x) log(P(x)/Q(x))` measures how much P differs from Q in information-theoretic terms — specifically, the extra bits needed to encode samples from P using a code optimized for Q. It is non-negative (by Jensen's inequality, since log is concave) and zero iff P=Q. It is **not symmetric** — `KL(P||Q) ≠ KL(Q||P)` in general. This asymmetry matters: `KL(P||Q)` (P is 'truth', Q is approximation) penalizes Q having low probability where P has high probability (mode-covering); `KL(Q||P)` (used in variational inference) penalizes Q having high probability where P has low probability (mode-seeking). VAEs minimize `KL(Q||P)` and consequently find modes of the posterior rather than covering its full mass.",
            "**Cross-entropy** `H(P,Q) = -Σ P(x) log Q(x)` is the expected code length when data follows P but code is optimized for Q. The identity `H(P,Q) = H(P) + KL(P||Q)` reveals that minimizing cross-entropy is equivalent to minimizing KL divergence when P is the data distribution (H(P) is fixed). This is the probabilistic underpinning of the cross-entropy loss in classification: the label distribution P is a one-hot vector (e.g., class 3 → P=[0,0,0,1,0,...]), and Q is the model's softmax output. Cross-entropy loss = `-log Q(true class)` = negative log-likelihood = negative log of the model's assigned probability to the correct class. Minimizing this is **maximum likelihood estimation**: you are maximizing P(data|θ), the likelihood of the training data under the model's parameters.",
            "**Mutual information** `I(X;Y) = H(X) - H(X|Y) = KL(P(X,Y)||P(X)P(Y))` quantifies how much knowing Y reduces uncertainty about X. It is zero iff X and Y are independent, and equals H(X) iff Y determines X completely. MI is the fundamental measure of 'how much information one variable carries about another' and appears throughout ML: in information bottleneck (compression vs. prediction tradeoff), in feature selection (pick features with high MI with labels), and in the theory of representation learning (good representations maximize MI between input and compressed representation while minimizing complexity). The **information bottleneck principle** says a good intermediate representation Z of input X for predicting Y should have high `I(Z;Y)` and low `I(Z;X)` — keep what's relevant, discard what's irrelevant. This gives a unified framework for understanding why deep networks generalize.",
          ],
          resources: [
            { label: "Visual Information Theory — colah's blog (best intuitive intro)", url: "https://colah.github.io/posts/2015-09-Visual-Information/", type: "docs" },
            { label: "Elements of Information Theory — Cover & Thomas (the textbook)", url: "https://onlinelibrary.wiley.com/doi/book/10.1002/047174882X", type: "book" },
            { label: "CS229 — Cross-entropy and MLE connection (Stanford notes)", url: "https://cs229.stanford.edu/notes2022fall/cs229-notes1.pdf", type: "docs" },
          ],
        },
      ],
    },
  ],
});

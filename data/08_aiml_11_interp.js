atlasAdd({
  id: "aiml",
  topics: [
    {
      id: "ai-interp",
      subtopics: [
        {
          title: "Circuits Hypothesis and Induction Heads",
          body: [
            "**Mechanistic interpretability** treats a trained neural network as an object to be reverse-engineered: its weights encode algorithms, and the goal is to find human-understandable descriptions of those algorithms. The **circuits hypothesis** (Olah et al., 2020) proposes that neural networks implement modular computational units called **circuits** — subgraphs of neurons/attention heads connected by weights that together implement a specific function. A circuit for 'curve detection' in a CNN, for example, consists of neurons that detect oriented edges (Gabor-like) feeding into neurons that detect curved edges (half-circles) feeding into neurons that detect full curves. This compositional structure suggests that networks learn interpretable primitives that compose.",
            "**Induction heads** are the canonical mechanistic circuit in transformer language models (Olah et al., 2021). An induction head is a pair of attention heads — one **previous-token head** (attends to the token preceding the current token, copying its identity into the residual stream) and one **induction head** (attends to the token that followed the same token in a previous context, completing the pattern). Together they implement **in-context copying**: if the sequence contains `...A B...A`, the induction head predicts `B` after the second `A` by (a) looking for where `A` appeared before and (b) attending to what followed it. Induction heads are universal — they form in the first or second layer of nearly every transformer trained on text, regardless of model size.",
            "**The residual stream as a communication channel**: in a transformer, the residual connection means each layer's output is $x_{l+1} = x_l + f_l(x_l)$ where $f_l$ is the attention or FFN sublayer. The **residual stream** $x_l$ is a shared communication channel that all layers read from and write to. Attention heads and MLP layers can be understood as **read-write operations** on this shared channel. The **Mathematical Framework for Transformer Circuits** (Elhage et al., 2021) makes this precise: a one-layer transformer with attention head $h$ computes $O_h = V_h A_h = (W_V^h X)(W_Q^h X, W_K^h X) \\to W_O^h O_h$ — a composition of linear operations that can be understood as routing information from source tokens to destination tokens via the attention pattern.",
            "**Logit lens** (nostalgebraist, 2020) is a simple interpretability technique: apply the model's unembedding matrix ($W_U \\in \\mathbb{R}^{d \\times V}$) to the residual stream at each layer (not just the final layer) and decode the resulting probability distribution over vocabulary. This reveals what the model 'thinks' the next token is at each layer, providing a layer-by-layer window into the prediction formation process. Early layers produce uncertain predictions; middle layers often already have a strong signal for syntactic predictions; late layers refine semantic and world-knowledge predictions. **Tuned lens** (Belrose et al., 2023) trains a probe at each layer to translate the intermediate residual stream to the final layer's coordinate system, providing more faithful predictions than the raw logit lens.",
            "**Activation patching** (also called **causal mediation analysis**) is the key mechanistic interpretability experimental tool. To test whether an internal component (an attention head, an MLP layer, a neuron) is causally responsible for a model's behaviour on some task: (1) run the model on a **clean** input that produces the behaviour; (2) run it on a **corrupted** input that doesn't; (3) **patch** the activation of the component from the clean run into the corrupted run; (4) measure the recovery in model behaviour. If patching recovers the behaviour, the component is causally necessary. This enables **circuit discovery**: find the minimal set of components that are jointly sufficient to explain a task behaviour. The IOI (Indirect Object Identification) circuit (Wang et al., 2022) — the set of attention heads in GPT-2 responsible for identifying that 'Mary gave John a book; She gave him...' correctly refers 'him' to John — was discovered via systematic activation patching across all attention heads.",
          ],
          resources: [
            { label: "Distill — Zoom In: Circuits (Olah et al., 2020)", url: "https://distill.pub/2020/circuits/zoom-in/", type: "docs" },
            { label: "Mathematical Framework for Transformer Circuits (Elhage et al., 2021)", url: "https://transformer-circuits.pub/2021/framework/index.html", type: "paper" },
            { label: "IOI Circuit paper — Wang et al. (2022)", url: "https://arxiv.org/abs/2211.00593", type: "paper" },
            { label: "Logit Lens — nostalgebraist (2020)", url: "https://www.lesswrong.com/posts/AcKRB8wDpdaN6v6ru/interpreting-gpt-the-logit-lens", type: "docs" },
          ],
        },
        {
          title: "Superposition, Polysemanticity, and Sparse Autoencoders",
          body: [
            "**The superposition hypothesis** (Elhage et al., 2022) proposes that neural networks represent more features than they have dimensions, by encoding multiple features in overlapping directions. Consider a model with $d$ dimensions but $F \\gg d$ features to represent. If features are **sparse** (most are inactive for any given input), they can be packed into $d$ dimensions via **almost-orthogonal** directions — two random unit vectors in $\\mathbb{R}^d$ have expected dot product $O(1/\\sqrt{d})$, so many features can coexist with small interference. The toy model of superposition: train an autoencoder to compress $F$ sparse features through a bottleneck of dimension $d < F$ and reconstruct them. The trained encoder learns to pack $F$ features into $d$ dimensions in a specific geometric arrangement — features corresponding to regular polytopes (tetrahedra, antiprisms) tiling the unit sphere.",
            "**Polysemanticity** is the empirical manifestation of superposition: individual neurons (or attention heads) respond to multiple unrelated concepts. The canonical example: **neuron 2097** in a small MLP (studied in Anthropic's 1-layer transformer work) responds to tokens related to DNA, President Obama, and basketball — semantically unrelated concepts packed into the same neuron because superposition is occurring. Polysemanticity makes mechanistic interpretation via individual neuron analysis nearly impossible — the same neuron participates in multiple unrelated circuits. It also means that **linear probing** (training a linear classifier on neuron activations to predict a concept) gives an inflated picture of how cleanly concepts are represented.",
            "**Sparse Autoencoders (SAEs)** (Bricken et al., 2023; Cunningham et al., 2023) are the primary tool for finding monosemantic (single-meaning) features inside LLMs via superposition unpacking. Architecture: an encoder projects the residual stream from $\\mathbb{R}^d$ to $\\mathbb{R}^M$ with $M \\gg d$ (e.g., $d=512$, $M=16384$), applies ReLU, and a decoder projects back: $\\hat{x} = W_{\\text{dec}} \\text{ReLU}(W_{\\text{enc}} x + b_{\\text{enc}}) + b_{\\text{dec}}$. Training objective: $\\mathcal{L} = \\|x - \\hat{x}\\|_2^2 + \\lambda \\|\\text{ReLU}(W_{\\text{enc}} x + b_{\\text{enc}})\\|_1$. The L1 sparsity penalty encourages most features to be inactive for any given input, forcing the SAE to find the smallest set of active features that explain $x$ — ideally, each active feature corresponds to a single interpretable concept.",
            "**Evaluating SAE features**: for each SAE feature (one row of $W_{\\text{dec}}$), find the training examples that maximally activate it. If the top-10 activating examples are semantically coherent (e.g., all mathematical equations, or all names of French cities, or all code comments about error handling), the feature is monosemantic. **Automated interpretability** (Bills et al., 2023) uses an LLM to generate descriptions of features from their activating examples and then validates by predicting which new examples should activate. Anthropic's Claude-3 Sonnet SAE analysis (Templeton et al., 2024) found millions of interpretable features in the residual stream, including features for 'scam and fraud', 'the Golden Gate Bridge', and 'the concept of recursion in code' — features that appear in the residual stream and causally influence downstream predictions.",
            "**The geometry of features in superposition**: Elhage et al.'s toy model experiments reveal specific structures. Features that tend to co-occur (positively correlated) are arranged in **antipodal pairs** — they are almost opposite in activation space, allowing the model to distinguish 'A present' from 'B present' cleanly. Features that are **mutually exclusive** (never co-active) are arranged in **polyhedral** patterns — tetrahedra ($4$ features in $\\mathbb{R}^2$), triangular prisms ($6$ features in $\\mathbb{R}^3$). The number of features that can be packed into $d$ dimensions is approximately $d/\\log(1/s)$ where $s$ is the feature sparsity — more sparse features pack more efficiently. This explains why sparsity of training data leads to denser superposition.",
          ],
          resources: [
            { label: "Toy Models of Superposition — Elhage et al. (2022)", url: "https://transformer-circuits.pub/2022/toy_model/index.html", type: "paper" },
            { label: "Towards Monosemanticity — Bricken et al. (2023, SAEs)", url: "https://transformer-circuits.pub/2023/monosemanticity/index.html", type: "paper" },
            { label: "Neel Nanda — Mech Interp Glossary", url: "https://www.neelnanda.io/mechanistic-interpretability/glossary", type: "docs" },
          ],
        },
        {
          title: "Constitutional AI, RLHF Alignment, and Scalable Oversight",
          body: [
            "**The alignment problem** is: how do we ensure that as AI systems become more capable, they pursue goals that are actually beneficial? The core difficulty is that specifying human values precisely is hard, and systems optimised for a proxy metric may find adversarial solutions (Goodhart's Law: 'When a measure becomes a target, it ceases to be a good measure'). A concrete example: a system rewarded for 'the user says thank you' might develop strategies to elicit thanks rather than to actually help. Alignment research is the engineering discipline of preventing this — building systems that pursue the intended objective, not just the specified proxy.",
            "**Constitutional AI (CAI)** (Bai et al., 2022) is Anthropic's approach to scalable alignment without individual human rating of model outputs. The 'constitution' is a list of principles (e.g., 'Choose the response that is least likely to contain factually incorrect information', 'Prefer responses that are helpful, harmless, and honest'). The pipeline: (1) **Red-teaming**: prompt the model to generate harmful outputs. (2) **AI critique**: prompt the model to critique its own output using a constitution principle and suggest a revision. (3) **Revision**: generate the revised, safer output. (4) **SL-CAI**: fine-tune on the revised outputs. (5) **RLAIF**: generate preference data using the model itself (with constitution-based prompts) and train a reward model; run RLHF with this AI-generated reward model. This avoids the scaling bottleneck of human labelling.",
            "**Mesa-optimisation and deceptive alignment** (Hubinger et al., 2019) identifies a structural risk in trained systems. A **mesa-optimiser** is an AI system that itself performs optimisation — it has learned to search for solutions using an internal objective. The risk: the mesa-objective (what the system actually optimises) may diverge from the base objective (what the trainer specified). **Deceptive alignment** is the scenario where a sufficiently capable mesa-optimiser learns to *appear* aligned during training (to maximise its training reward) while pursuing different goals during deployment — a system that has 'learned to play the game' of appearing safe without being safe. This is theoretically possible for sufficiently capable optimisers with world-models that distinguish training from deployment.",
            "**Scalable oversight** addresses a core challenge: as AI systems become more capable than humans on specific tasks, human feedback becomes unreliable — humans cannot evaluate a proof they don't understand, or code that exploits a subtle security vulnerability. Proposed solutions: (1) **Debate** (Irving et al., 2018): two AI systems argue for opposing answers; a human judge evaluates the debate. The conjecture: even if humans cannot verify an answer directly, they can detect flaws in arguments — an AI that generates misleading arguments is vulnerable to the other AI exposing those flaws. (2) **Recursive reward modelling**: use an AI assistant to help human evaluators assess AI outputs, recursively — the assistant explains the complex output in terms the human can evaluate. (3) **Iterated amplification** (Christiano et al., 2018): train a model to assist a human in evaluating tasks that would be too complex for the human alone, then use the assisted human as the reward source — bootstrapping capability evaluations.",
            "**Evaluation and red-teaming**: the practical near-term safety work. **Evals** (model evaluations for dangerous capabilities) test whether a model can autonomously perform actions that could cause catastrophic harm — developing bioweapons from literature, performing cyberattacks, deceiving humans consistently. The **Responsible Scaling Policy** (RSP) framework (Anthropic, 2023) gates deployment on eval thresholds: before deploying a new model, demonstrate that it cannot autonomously perform certain dangerous actions above a threshold level. **Red-teaming** (manual and automated) probes models for harmful behaviours, jailbreaks, and capability leaks. **METR** (Model Evaluation and Threat Research) conducts independent dangerous-capability evals. **Constitutional Classifiers** are lightweight models trained to detect harmful outputs, used as a runtime filter layer before serving.",
          ],
          resources: [
            { label: "Constitutional AI paper — Bai et al. (2022)", url: "https://arxiv.org/abs/2212.08073", type: "paper" },
            { label: "Risks from Learned Optimisation — Hubinger et al. (2019, mesa-optimisation)", url: "https://arxiv.org/abs/1906.01820", type: "paper" },
            { label: "AI Debate — Irving et al. (2018)", url: "https://arxiv.org/abs/1805.00899", type: "paper" },
            { label: "Anthropic Transformer Circuits thread", url: "https://transformer-circuits.pub/", type: "docs" },
            { label: "ARENA curriculum — Alignment Research Engineer Accelerator", url: "https://www.arena.education/", type: "course" },
          ],
        },
        {
          title: "TransformerLens and Practical Mech Interp Tooling",
          body: [
            "**TransformerLens** (formerly EasyTransformer, Neel Nanda 2022) is the standard library for mechanistic interpretability research on transformer language models. It provides: (1) **Cached activations** — run a forward pass and extract the residual stream, attention patterns, MLP activations, and attention head outputs at every layer and every position with a single call; (2) **Hook functions** — register Python callbacks at any internal activation to read, modify, or ablate it during the forward pass; (3) **Standardised model interface** — converts HuggingFace models (GPT-2, LLaMA, Mistral, etc.) to a canonical internal format with consistent naming. The key pattern: `logits, cache = model.run_with_cache(tokens)` returns the output and a dictionary of all internal activations.",
            "**Activation patching with TransformerLens**: the standard mech interp experiment flow. Define clean and corrupted inputs (e.g., 'The dog chased the cat because it was scared' vs 'The dog chased the cat because it was hungry'). Run both through the model and cache activations. For each component (attention head at layer $l$, head $h$): patch its output in the corrupted run with the clean run's activations; measure the change in the logit difference for the targeted tokens. This produces a **heatmap of causal importance** across all $(l, h)$ pairs. Components with high patch score are causally necessary for the behaviour. The `hook_point` system in TransformerLens lets you do this with `model.run_with_hooks(corrupted_tokens, fwd_hooks=[(hook_name, patch_fn)])` where `patch_fn` replaces the activation with the cached clean value.",
            "**Attention pattern analysis**: `cache['pattern', layer]` gives the $[\\text{batch}, \\text{head}, \\text{seq}, \\text{seq}]$ attention matrix — what each head attends to at each position. Visualising attention patterns (with `circuitsvis` or custom matplotlib) reveals head specialisations: some heads consistently attend to the previous token (previous-token heads), some attend from the current position to itself (no-op heads), some attend to syntactically related positions (e.g., the verb from the subject), and some implement the induction pattern (attend from position $i$ to position $j < i$ where the token at $j$ is the same as at $i-1$).",
            "**The SAE integration**: sparse autoencoders can be trained on TransformerLens-extracted residual stream activations and then loaded back as a hook function. The pattern: collect $\\sim 10^8$ activation vectors from the residual stream at a specific layer across diverse text; train the SAE; then use `model.run_with_hooks` to replace the residual stream with the SAE's reconstruction and measure the downstream performance degradation (the **reconstruction score**). Features with high frequency and high downstream effect are the most important for the model's behaviour at that layer. **SAE Lens** (Joseph Bloom) is the standard library for training and analysing SAEs on TransformerLens-compatible models.",
            "**Causal scrubbing** (Chan et al., 2022) is a rigorous framework for validating a proposed circuit. Given a hypothesis (e.g., 'these 5 attention heads implement the IOI circuit'), causal scrubbing tests it by replacing the activations of the hypothesised components with activations from semantically compatible inputs (inputs that should produce the same intermediate representation according to the hypothesis) and all other components with activations from random inputs. If the hypothesis is correct, the model's output should be unchanged — only the hypothesised components matter. Causal scrubbing is more rigorous than activation patching because it tests the full hypothesis structure, not just component necessity. The implementation requires a **computation graph** that maps inputs to expected intermediate activations, which must be provided by the researcher — making it expensive but definitive.",
          ],
          resources: [
            { label: "TransformerLens documentation — Neel Nanda", url: "https://transformerlensorg.github.io/TransformerLens/", type: "docs" },
            { label: "Neel Nanda YouTube channel (tutorials, paper explanations)", url: "https://www.youtube.com/@neelnanda2469/videos", type: "video" },
            { label: "Causal Scrubbing — Chan et al. (2022)", url: "https://arxiv.org/abs/2209.04748", type: "paper" },
            { label: "Neel Nanda — Mech Interp explainer and resources", url: "https://www.neelnanda.io/mechanistic-interpretability", type: "docs" },
          ],
        },
      ],
    },
  ],
});

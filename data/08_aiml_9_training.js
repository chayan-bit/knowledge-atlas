atlasAdd({
  id: "aiml",
  topics: [
    {
      id: "ai-training",
      subtopics: [
        {
          title: "FlashAttention: IO-Aware Exact Attention via Tiling",
          body: [
            "Standard attention $\\text{softmax}(QK^T/\\sqrt{d})V$ materialises the $n \\times n$ attention matrix in **HBM (High Bandwidth Memory)** — the main GPU DRAM. For $n = 4096$ and FP16: the attention matrix is $4096^2 \\times 2$ bytes $= 32$ MB per head, per layer, per sample. With $H=32$ heads and batch size $B=8$: 8 GB just for attention matrices. HBM bandwidth is $\\sim 2$ TB/s for an A100 — reading/writing 8 GB takes 4 ms per attention computation. FlashAttention (Dao et al., 2022) achieves the same arithmetic result without ever materialising the full matrix in HBM.",
            "**Tiling**: partition $Q, K, V$ into blocks that fit in **SRAM (L2/shared memory, ~40 MB on A100)** and compute attention block by block. For a block of queries $Q_i$ and key-value block $K_j, V_j$: compute $S_{ij} = Q_i K_j^T / \\sqrt{d}$ in SRAM (never written to HBM), apply softmax incrementally, accumulate the weighted sum $O_i += \\text{softmax}(S_{ij}) V_j$ with a running correction for the normalisation constant. The challenge: softmax requires the maximum $m = \\max_j S_{ij}$ for numerical stability. FlashAttention uses the **online softmax** trick: maintain a running maximum $m_i$ and normalisation $l_i$. When processing block $j+1$: $m_i^{\\text{new}} = \\max(m_i, m_{i,j+1})$, rescale the accumulated output $O_i \\leftarrow O_i \\cdot e^{m_i - m_i^{\\text{new}}}$, add the new block's contribution.",
            "**IO complexity**: standard attention: $O(n^2)$ HBM reads/writes (materialise and read back the $n\\times n$ matrix). FlashAttention: $O(n^2 d / M)$ HBM reads/writes where $M$ is the SRAM size — for $M \\gg d$, this is $O(n^2)$ fewer memory operations by a factor of $M/d$. On A100 with $M = 40$ MB and $d = 64$: $\\sim 640\\times$ fewer HBM accesses asymptotically. In practice: **2-4x wall-clock speedup** and **10-20x memory saving** for long sequences. This is the dominant technique enabling long-context transformers (100k+ tokens).",
            "**FlashAttention-2** (Dao, 2023) improves on v1 by: (1) reducing non-matmul FLOPs (the online softmax bookkeeping was expensive); (2) partitioning work to maximise parallelism across attention heads and sequence positions; (3) better SRAM utilisation on Ampere GPUs. FlashAttention-3 (Shah et al., 2024) targets Hopper (H100) architecture specifics: exploits **warp specialisation** (different warps do compute vs memory operations simultaneously), uses **FP8 precision** for matmuls with software accumulation in FP32, and **asynchronous pipeline** between GEMM and softmax — achieving $75\%$ of H100 peak A100-equivalent throughput.",
            "**Flash Attention for decoder inference**: during autoregressive decoding, the query is a single new token and the keys/values are the KV cache of all previous tokens. This is a **memory-bound** rather than **compute-bound** operation (only one row of the attention matrix). The main bottleneck is loading the KV cache from HBM. **Flash Decoding** (Dao, 2023) parallelises across the sequence length dimension (previously impossible since different threads would accumulate the same output row): each thread block computes partial softmax over a subset of keys, then a second pass combines them using the log-sum-exp trick. Enables $8\\times$ faster decoding for long contexts.",
          ],
          resources: [
            { label: "FlashAttention paper — Dao et al. (2022)", url: "https://arxiv.org/abs/2205.14135", type: "paper" },
            { label: "FlashAttention-2 — Dao (2023)", url: "https://arxiv.org/abs/2307.08691", type: "paper" },
            { label: "FlashAttention-3 — Shah et al. (2024)", url: "https://arxiv.org/abs/2407.08608", type: "paper" },
          ],
        },
        {
          title: "Parallelism: Tensor, Pipeline, Data, and Expert Parallelism",
          body: [
            "Training a model with $N$ parameters on a single GPU requires $\\sim 16N$ bytes (FP16 params + grads + FP32 master weights + Adam moments) plus activations. A 7B model needs $\\sim 112$ GB — a single A100 has 80 GB. **Model parallelism** distributes the model itself across GPUs. The four parallelism dimensions are orthogonal and composable: **DP** (data), **TP** (tensor), **PP** (pipeline), **EP** (expert) form the axes of the **5D parallelism** scheme used in Megatron-LM.",
            "**Tensor Parallelism (TP)** (Shoeybi et al., 2019) shards individual weight matrices across GPUs. For a linear layer $Y = XW$ with $W \\in \\mathbb{R}^{d \\times 4d}$: split $W$ column-wise across $N$ GPUs: each GPU holds $W_i \\in \\mathbb{R}^{d \\times 4d/N}$ and computes $Y_i = X W_i$ (the full $X$ is on every GPU). For attention: split query/key/value projection heads across GPUs; each GPU handles $H/N$ heads. An **all-reduce** (sum across GPUs) is needed after the final projection. Communication volume: $O(B \\cdot s \\cdot d)$ per all-reduce (batch × sequence × hidden dim), which occurs once per transformer layer per forward pass. TP is **intra-node** (requires high-bandwidth NVLink; PCIe is too slow).",
            "**Pipeline Parallelism (PP)**: assign consecutive transformer layers to different GPUs (stage). The forward pass sends activations between stages; the backward pass sends gradients. Naive pipeline: stage 1 computes forward, sends to stage 2, then is **idle** while stage 2-onwards compute — the **pipeline bubble** wastes $\\sim (P-1)/P$ of time for $P$ stages. **GPipe**: split the batch into $m$ micro-batches, pipeline them to reduce bubble fraction to $(P-1)/(m+P-1)$. **PipeDream**: interleave forward and backward passes (1F1B schedule) to minimise both bubble and memory. **DualPipe** (DeepSeek, 2025): overlaps forward and backward of different micro-batches completely, nearly eliminating the pipeline bubble.",
            "**Sequence Parallelism (SP)** distributes the sequence dimension across GPUs within a tensor-parallel group — used for activations that are not split by TP (LayerNorm, dropout). In TP, the attention heads are split but the input sequence is replicated; SP splits the sequence so each GPU holds a contiguous subsequence. An **all-gather** before TP and a **reduce-scatter** after reconstructs/distributes activations. SP reduces activation memory by $N_{\\text{tp}}\\times$ while maintaining the same TP communication pattern. **Context Parallelism (CP)** extends SP to the attention operation: each GPU holds a subsequence for Q, K, V; an all-gather of K, V is performed before attention (using ring attention patterns to amortise communication).",
            "**Mixture of Experts (MoE)** is a form of **Expert Parallelism (EP)** where each transformer's FFN is replaced by $E$ expert FFNs, and each token is routed to $K$ of them ($K=2$ typically). A small **router network** (one linear + softmax) selects the top-$K$ experts per token. Only $K/E$ of parameters are activated per token — a 64-expert model activates $2/64 \\approx 3\\%$ of FFN parameters per token, giving $32\\times$ theoretical capacity at fixed inference compute. EP places different experts on different GPUs; routing requires **all-to-all** collective (every GPU sends tokens to every other GPU). The main challenge: **load balancing** — without a balancing loss, all tokens route to the same experts. The **auxiliary load-balancing loss** $L_{\\text{bal}} = \\alpha \\sum_e f_e p_e$ (where $f_e$ is the fraction of tokens routed to expert $e$ and $p_e$ is the mean router probability for expert $e$) encourages uniform routing. DeepSeek's fine-grained MoE (DeepSeekMoE) uses many small experts with $K$ large for even better routing granularity.",
          ],
          resources: [
            { label: "Megatron-LM — Tensor Parallelism paper (Shoeybi et al., 2019)", url: "https://arxiv.org/abs/1909.08053", type: "paper" },
            { label: "ZeRO / FSDP — Rajbhandari et al. (2020)", url: "https://arxiv.org/abs/1910.02054", type: "paper" },
            { label: "HuggingFace UltraScale Playbook (distributed training guide)", url: "https://huggingface.co/spaces/nanotron/ultrascale-playbook", type: "docs" },
          ],
        },
        {
          title: "Gradient Checkpointing, Mixed Precision, and Memory Optimisation",
          body: [
            "**Activation memory** during training stores all intermediate tensors needed for the backward pass — for a transformer with $L$ layers, $B$ batch size, $s$ sequence length, $d$ hidden dim: activation memory is $O(L \\cdot B \\cdot s \\cdot d)$ per FP32 byte — for LLaMA-7B ($L=32, d=4096$) with $B=1, s=4096$: $\\sim 2$ GB just for activations (at FP16). With large $B$ or $s$, activations dominate memory. **Gradient checkpointing** recomputes activations during backward instead of storing them: store only the output of each checkpoint (e.g., every transformer block boundary), recompute the intermediate activations on demand during backward. Memory savings: $O(\\sqrt{L})$ instead of $O(L)$ for an optimal checkpoint placement. Cost: $\\sim 33\\%$ extra FLOPs (roughly one extra forward pass).",
            "**Mixed precision training** uses FP16 (or BF16) for forward and backward passes while maintaining FP32 **master weights** and running the optimiser step in FP32. Reasons: (a) FP32 master weights prevent precision loss from repeated weight updates (each update $\\Delta w \\sim 10^{-5}$ while $|w| \\sim 1$ — ratio is $10^5$, which FP16 cannot represent; FP32 has $\\sim 10^7$ precision). (b) Gradient accumulation must happen in FP32 to avoid rounding errors. (c) Loss scaling (GradScaler) multiplies the loss by a large factor (e.g., 65536) before backward to prevent FP16 underflow, then unscales before the optimiser step. BF16 has the same range as FP32 (8 exponent bits vs FP16's 5) but less mantissa precision — it rarely requires loss scaling and is now preferred for Ampere+ GPUs.",
            "**Optimiser state sharding**: Adam maintains two extra tensors per parameter ($m$ and $v$, same size as $\\theta$) in FP32 — adding $2 \\times 4\\Psi$ bytes on top of $4\\Psi$ for FP32 weights and $2\\Psi$ for FP16 weights/gradients, total $16\\Psi$ bytes per parameter. For a 7B model: $\\sim 112$ GB on one GPU. **Adafactor** (Shazeer & Stern, 2018) stores the second moment as a factored rank-1 approximation: $v_{ij} \\approx r_i c_j$ (row and column factors) — reducing from $O(mn)$ to $O(m+n)$ memory per matrix. **Lion optimizer** (Chen et al., 2023) maintains only momentum (not the second moment) and uses the sign of the gradient — $1/3$ the memory of Adam. **Sophia** (Liu et al., 2023) uses a diagonal Hessian approximation for second-order optimisation at Adam's memory cost.",
            "**Activation offloading** moves activations to CPU memory and transfers them back on demand during backward — trading GPU-CPU PCIe bandwidth for GPU memory. Modern PCIe 5.0 bandwidth is $\\sim 64$ GB/s vs GPU HBM bandwidth $\\sim 2$ TB/s, so this incurs a $\\sim 30\\times$ slowdown for activation-memory-bound workloads. Useful only when the GPU would otherwise OOM. **Paged optimiser states** (QLoRA) use NVIDIA's unified memory to page optimiser states to CPU on demand — transparent, no code changes, but adds latency spikes when pages fault. **8-bit optimisers** (Dettmers et al., 2022) quantise optimiser states to INT8, halving their memory at small accuracy cost.",
            "**Profiling tools for training efficiency**: `torch.profiler` captures per-operation time and memory statistics, exportable to Chrome trace format. `nvidia-smi dmon` monitors GPU utilisation, memory, and temperature. Key metrics: **MFU (Model FLOP Utilisation)** = actual throughput / peak theoretical throughput. Typical MFU: 30-50% for well-tuned training jobs (vs 100% theoretical). Low MFU indicates bottlenecks: small batch size (memory-bound kernels), data loading (CPU-GPU transfer), or excessive communication overhead. **nsight systems** provides timeline-level visibility including CUDA kernel launches, NCCL collectives, and PCIe transfers.",
          ],
          resources: [
            { label: "Gradient checkpointing — Chen et al. (2016)", url: "https://arxiv.org/abs/1604.06174", type: "paper" },
            { label: "PyTorch Profiler tutorial", url: "https://docs.pytorch.org/tutorials/recipes/recipes/profiler_recipe.html", type: "docs" },
            { label: "8-bit optimisers — Dettmers et al. (2022)", url: "https://arxiv.org/abs/2110.02861", type: "paper" },
          ],
        },
        {
          title: "Speculative Decoding, Distillation, and Efficient Inference",
          body: [
            "**Knowledge distillation** (Hinton et al., 2015) transfers knowledge from a large **teacher** model $T$ to a small **student** model $S$ by training $S$ on the teacher's **soft targets** (output probability distributions) rather than hard one-hot labels. The distillation loss: $\\mathcal{L}_{\\text{KD}} = \\alpha \\mathcal{L}_{\\text{CE}}(y, S) + (1-\\alpha) T^2 \\text{KL}(T/T || S/T)$ where $T$ is the temperature (divides logits before softmax) and $\\alpha$ balances hard and soft losses. Soft targets encode the teacher's **dark knowledge** — the relative probabilities between non-correct classes (e.g., a truck image has higher probability for car than for cat) carry structural information not present in one-hot labels. This transfers more effectively to the student than supervised training alone.",
            "**Speculative decoding mechanics** (already covered in LLM section but deeper here): the core bottleneck of LLM inference is **memory bandwidth**, not compute. Generating one token requires loading all $N$ parameters from HBM ($\\sim 2N$ bytes for FP16) — at 2 TB/s HBM bandwidth, this takes $N/10^{12}$ seconds. For a 70B model: 70B × 2 bytes / 2 TB/s = 70 ms per token — the **roofline limit**. The target model call is memory-bound regardless of how many tokens it processes in parallel (up to the attention window). Speculative decoding exploits this: processing $K$ tokens in one target model call costs the same in memory bandwidth as processing 1 (since model weights are loaded once), but produces $K$ tokens if all are accepted — $K\\times$ throughput improvement.",
            "**Quantisation for inference** — INT4 vs INT8 vs FP8: **INT8 (W8A8)** quantises both weights and activations to 8-bit integers. The key operation $y = Wx$ is computed as $y = (W_q \\cdot s_W)(x_q \\cdot s_x) = s_W s_x (W_q x_q)$ where the integer matmul $W_q x_q$ uses hardware INT8 tensor cores. Accuracy loss is minimal ($< 0.5\\%$ on most benchmarks). **INT4 weight-only quantisation** (GPTQ, AWQ) quantises only weights to 4-bit but dequantises them to FP16 before the matmul — avoids INT4 accumulation errors but is $\\sim 1.5\\times$ faster than FP16 weight-only (less data to load from HBM). **FP8 (E4M3, E5M2)** is the H100 native format — hardware-accelerated matmuls in FP8 with FP16 accumulation, giving $2\\times$ the tensor core throughput of BF16 at similar accuracy for inference.",
            "**Model compression beyond quantisation**: **pruning** removes individual weights (unstructured) or entire neurons/attention heads (structured) based on saliency ($|w|$, gradient magnitude, or Hessian-based scores). Unstructured pruning requires sparse matmul hardware (NVIDIA's 2:4 sparsity pattern — exactly 2 of every 4 weights are zero — is supported by sparse tensor cores with $2\\times$ throughput). **Layer dropping** removes entire transformer blocks (typically middle layers) — effective because middle layers contribute least to output quality per sensitivity analysis. **KV cache compression**: instead of caching all past keys/values, methods like **SnapKV**, **StreamingLLM**, and **H2O** identify and retain only the $K$ most important KV pairs per head based on attention scores, enabling $10\\times$ context extension with bounded KV cache memory.",
          ],
          resources: [
            { label: "Knowledge Distillation — Hinton et al. (2015)", url: "https://arxiv.org/abs/1503.02531", type: "paper" },
            { label: "GPTQ — post-training quantisation (Frantar et al., 2022)", url: "https://arxiv.org/abs/2210.17323", type: "paper" },
            { label: "vLLM documentation — inference engine internals", url: "https://docs.vllm.ai/", type: "docs" },
          ],
        },
      ],
    },
  ],
});

atlasAdd({
  id: "compilers",
  topics: [
    {
      id: "comp-semantics",
      title: "Semantic analysis & type checking",
      level: "Intermediate",
      body: [
        "Scoped **symbol tables**, the static/dynamic and nominal/structural distinctions, **type checking** via attribute grammars (synthesized vs inherited attributes), and the **visitor pattern** to separate passes from the AST.",
        "This is where 'is this program meaningful?' gets answered, distinct from 'is it grammatically correct?'.",
      ],
      subtopics: [
        {
          title: "Symbol tables and scope management",
          body: [
            "A **symbol table** is a mapping from names to **symbol records** — data structures carrying everything the compiler knows about a named entity: its kind (variable, function, type, parameter), its type, its storage location or slot index, and whether it has been initialized. The parser produces a flat token stream and an AST; the semantic analysis phase populates the symbol table, then uses it to annotate each name reference in the AST with a pointer to the correct symbol record. Every subsequent pass — type checking, code generation, optimization — reads symbol records rather than looking up names by string, making those passes fast and correct.",
            "Scope is implemented by making the symbol table a **stack of scopes**. When the semantic analyzer enters a block (`{`), it pushes a new empty scope frame. When it exits (`}`), it pops and discards that frame (or archivally detaches it, if the language has closures that keep it alive). Name lookup searches from the top of the stack downward — innermost scope first — returning the first match. Name insertion goes into the current top frame. This straightforwardly handles **shadowing**: a new binding in an inner scope hides (but does not delete) the same name in an outer scope. A common pitfall in implementation: forgetting to search all the way to the global scope when looking up a name, or accidentally inserting a new binding into the wrong scope frame.",
            "Languages differ significantly in their scoping rules. **C** has **block scope** for local variables (declared anywhere in a block, visible from the declaration point to the end of the block) and file scope for globals. **JavaScript** (pre-ES6) has **function scope** for `var` declarations — visible throughout the entire function, not just from the declaration point — which is the source of the infamous hoisting behavior (`var x = 1` in the middle of a function acts as if `var x` was at the top). **Python** has LEGB scope lookup (Local → Enclosing function → Global → Built-in). **Rust** has block scope and a distinct **use-before-declare** rule — you cannot use a variable before its `let` binding, and re-binding with `let` in the same scope creates a new shadow (not mutation).",
            "**Forward references** complicate single-pass symbol table construction. In Java and C#, a class body can reference a method defined later in the same class, so a single top-to-bottom pass cannot resolve all names. The standard solution is a **two-pass** approach: pass 1 scans all declarations (without resolving bodies) to populate the symbol table with the names of all classes, methods, and fields; pass 2 resolves references in expression and statement bodies against the now-complete table. C avoids this with **header files**: declarations must appear before uses in the translation unit, and the `#include` mechanism lets you pull in forward declarations. Mutual recursion between functions in C is handled by explicitly writing a forward declaration `void foo();` before the definition.",
          ],
          resources: [
            { label: "Engineering a Compiler — Cooper & Torczon, chapter 5 (symbol tables)", url: "https://www.elsevier.com/books/engineering-a-compiler/cooper/978-0-12-815412-0", type: "book" },
            { label: "Crafting Interpreters — Resolving and Binding (the resolver pass)", url: "https://craftinginterpreters.com/resolving-and-binding.html", type: "book" },
          ],
        },
        {
          title: "Attribute grammars: decorating the AST",
          body: [
            "An **attribute grammar** augments a context-free grammar by associating **attributes** (computed values) with each grammar symbol and **semantic rules** specifying how to compute each attribute in terms of other attributes on the same production. The two kinds of attribute: a **synthesized attribute** of a node `A` is computed from the attributes of `A`'s children — it flows *up* the parse tree. An **inherited attribute** of a node `A` is computed from the attribute of `A`'s parent or siblings — it flows *down* the parse tree. A grammar where all attributes are synthesized is an **S-attributed grammar** and can be evaluated in a single bottom-up pass; a grammar with inherited attributes may require multiple passes or a more complex evaluation strategy.",
            "The classic example of a synthesized attribute is **type propagation**: `BinaryExpr(+, left, right)` has a synthesized type attribute computed from `left.type` and `right.type` by the rule `if left.type == int && right.type == int then result.type = int; else if …`. The classic example of an inherited attribute is **expected type**: in `f(x, y)`, the expected types of the arguments are inherited from the function signature — the symbol table entry for `f` — and flow down into the argument expressions to guide type checking or coercion. In practice, compilers implement attribute evaluation not by generating an evaluator from a formal attribute grammar specification but by writing explicit visitor passes that thread the relevant information through the tree.",
            "The **visitor pattern** (discussed in the Frontend topic) is the practical mechanism for implementing attribute grammar evaluation. A type-checking visitor carries a `TypeEnvironment` (mapping names to types, essentially the type-level symbol table) as state, visits each AST node, computes the node's type, and either annotates the node or returns the type as the visitor method's return value. The synthesized-attribute flow corresponds to bottom-up visitors (the visitor returns a value up from children to parent); the inherited-attribute flow corresponds to the environment passed down as a parameter. Most real type checkers mix both: the environment is threaded down as a parameter (inherited), and the computed type is returned up (synthesized).",
            "A subtle design question: should the type checker **mutate** the AST nodes to attach type annotations, or should it build a separate **type map** (AST node → type) without touching the AST? Mutating the AST is convenient (the code generator can read the type directly from each expression node) but makes the AST stateful and less clean. A separate type map preserves AST immutability and is often the right choice when the same AST is reused across multiple compilation units or when incremental compilation requires precise invalidation. Rust's compiler (rustc) maintains a separate `TyCtxt` (type context) that stores inferred types keyed by node IDs, completely separate from the HIR (High-level IR) that replaces the AST after parsing.",
          ],
          resources: [
            { label: "Dragon Book — Aho et al, chapter 5: Syntax-Directed Translation (attribute grammars)", url: "https://suif.stanford.edu/dragonbook/", type: "book" },
          ],
        },
        {
          title: "Type checking: rules, inference, and coercions",
          body: [
            "Type checking answers the question: **is this expression well-typed?** A **type system** consists of a set of **typing rules** — logical inference rules of the form $\\Gamma \\vdash e : \\tau$ meaning 'in type environment $\\Gamma$, expression e has type $\\tau$' — together with rules for when assignments, function calls, and operations are valid. A **type checker** implements these rules as a recursive function over the AST: `typecheck(Γ, e)` returns the type of `e` in environment `Γ`, or signals a type error. The distinction between **type checking** (verify that a user-supplied type annotation is correct) and **type inference** (deduce the type without annotation) is important: Java and C do type checking; Haskell and OCaml do type inference; TypeScript and Rust do both.",
            "**Subtyping** complicates type checking. In a language with subtyping ($\\mathrm{int} <: \\mathrm{float}$, $\\mathrm{Cat} <: \\mathrm{Animal}$), a type rule like 'the two branches of an if-else must have the same type' must be relaxed to 'the types of the two branches must have a common **least upper bound** (lub)'. Computing the lub requires a type hierarchy (for nominal subtyping as in Java) or a set of structural rules (for structural subtyping as in TypeScript). The lub of `Cat` and `Dog` under a nominal hierarchy with `Animal` above both is `Animal`; under structural typing it is the intersection of their field sets. Type widening (using a value of a more specific type where a more general type is expected) is **covariant** and safe; the converse (type narrowing) requires an explicit cast and may fail at runtime.",
            "**Implicit coercions** are a perpetual design tension. In C, `int + double` silently promotes the int to double; in Python 3, `1 + 1.0` is `2.0` by implicit coercion of the int; in Rust, there are almost no implicit coercions (you must write `1 as f64 + 1.0`). Coercions make code more concise but obscure the actual computation and can cause subtle bugs (C's integer promotion rules famously interact with bitwise operations in non-obvious ways). A type checker that supports coercions must integrate **coercion search** into its rules: when checking `f(e)` where `f` expects type $\\tau$ and `e` has type $\\sigma \\ne \\tau$, search for a coercion path $\\sigma \\to \\tau$ and, if found, automatically insert the coercion into the AST. This is the mechanism behind Scala's implicit conversions and Haskell's `Num` class defaulting.",
            "**Type errors vs type mismatches vs undefined behavior**: a well-designed type system aims to make invalid programs **unrepresentable** — to catch errors at compile time that would otherwise manifest as crashes, memory corruption, or wrong answers at runtime. The null pointer (`NullPointerException` in Java, segfault in C) is the canonical example of an error that a type system can prevent (Kotlin's nullable types, Rust's `Option<T>`). The power of a type system is measured by how many of these errors it catches without forcing too many false positives (overly rejecting valid programs). The type checking pass is the front-line defense; every type error you catch here is a runtime crash you prevent for your users.",
          ],
          resources: [
            { label: "Types and Programming Languages (TAPL) — Pierce, chapters 8-9 (simply-typed lambda calculus)", url: "https://www.cis.upenn.edu/~bcpierce/tapl/", type: "book" },
            { label: "Crafting Interpreters — Type Checking (Lox's type rules, chapter 10)", url: "https://craftinginterpreters.com/evaluating-expressions.html", type: "book" },
          ],
        },
        {
          title: "The visitor pattern for compiler passes",
          body: [
            "The **Visitor pattern** solves the **Expression Problem** as it appears in compilers: given a fixed hierarchy of AST node types and a growing set of operations over them (resolve names, check types, optimize, generate code), how do you add operations without modifying node classes? The answer is double dispatch. Each AST node class implements `accept(Visitor v)` by calling `v.visitX(this)` where `X` is the specific node type. Each operation implements `Visitor` with one method per node type. Adding a new operation is a new `Visitor` implementation with no changes to any node class; it is the mirror of the standard OOP principle where adding a new subclass is cheap but adding a new method is expensive.",
            "In practice, compiler passes are visitors that carry state. The **resolver** visitor carries a scope stack as field state, entering and exiting scopes as it visits block nodes and function nodes. The **type checker** visitor carries a type environment and accumulates a list of errors. The **code generator** visitor carries an output buffer or instruction list and emits instructions as it visits expression and statement nodes. The separation of these concerns — each visitor knows only its own state and what it does at each node — makes the compiler's pass structure explicit and independently testable: you can unit-test the resolver without running the type checker.",
            "A common anti-pattern is **leaking pass concerns into node classes**. If the type checker adds a `type` field to every `ExprNode`, and the code generator adds a `slot` field, and the optimizer adds a `constant_value` field, the AST classes become cluttered with concerns from multiple passes and their lifecycle (when is this field valid?) becomes unclear. The cleaner alternative is a **side table**: a hash map from node identity (address or unique ID) to the pass-specific annotation. This keeps the AST classes clean and makes it obvious that the annotation is only valid during a specific pass. rustc uses this approach extensively — the `TypeckResults` table maps expression IDs to inferred types, and it is keyed into per-function and invalidated when the function is re-type-checked.",
            "For passes with complex inter-node dependencies, the pure visitor pattern is insufficient and compilers introduce a **worklist algorithm** or a **constraint-generation + solving** phase. Type inference (Hindley-Milner) does not check types in a single tree walk; it generates **unification constraints** by walking the tree and then solves them with Robinson's algorithm. This decouples the constraint generation (which mirrors the tree structure) from the constraint solving (which requires a global fixed point). Similarly, whole-program dataflow analyses (liveness, reaching definitions, points-to analysis) cannot be done by a single tree walk because information from callee functions affects callers and vice versa — they require iterative worklist algorithms over the call graph, which is firmly outside the visitor pattern's scope.",
          ],
          resources: [
            { label: "Crafting Interpreters — Statements and State (visitor pattern in action)", url: "https://craftinginterpreters.com/statements-and-state.html", type: "book" },
          ],
        },
        {
          title: "Overloading, namespacing, and name mangling",
          body: [
            "**Function overloading** (same name, different parameter types) requires the type checker to perform **overload resolution**: given a call `f(e1, e2)`, collect all declarations of `f` visible in scope, compute the types of `e1` and `e2`, and select the most specific applicable overload. The rules for 'most specific' vary significantly: C++ has an elaborate 13-step ranking (exact match > promotion > conversion > user-defined conversion > …) with a complex partial ordering on overload sets; Java is simpler (exact match first, then widening conversions); Rust has no overloading of free functions (only traits, which are resolved differently). Getting overload resolution right is one of the most complex parts of implementing a C++ compiler.",
            "**Namespaces and modules** create a hierarchical symbol table. A reference `std::vector` resolves `std` in the global symbol table (finding the `std` namespace), then resolves `vector` within that namespace. Nested namespaces, using-declarations (`using namespace std`), and ADL (argument-dependent lookup in C++, where the namespaces of argument types are searched for free functions) compound the complexity. The symbol table for a language with namespaces is effectively a **tree** mirroring the namespace hierarchy, and lookup is a tree traversal with fallback rules.",
            "**Name mangling** is the mechanism by which compilers encode overloaded function names and namespace qualifiers into unique object-file symbols, since linkers traditionally deal with a flat symbol namespace. The C++ mangling of `std::vector<int>::push_back(int const&)` becomes the symbol `_ZNSt6vectorIiSaIiEE9push_backERKi` in the Itanium ABI — a self-describing encoding of the namespace, class, template arguments, and parameter types. Name mangling is what allows C++ to link multiple overloads and template instantiations correctly, and it is also why C++ symbol names in `nm` or debuggers look cryptic. Tools like `c++filt` demangle them. Rust uses a similar scheme; Go avoids it entirely by not supporting overloading.",
          ],
          resources: [
            { label: "Itanium C++ ABI — name mangling specification (the authoritative reference)", url: "https://itanium-cxx-abi.github.io/cxx-abi/abi.html#mangling", type: "docs" },
          ],
        },
      ],
      resources: [
        { label: "Engineering a Compiler — Cooper & Torczon (overview)", url: "https://www.elsevier.com/books/engineering-a-compiler/cooper/978-0-12-815412-0", type: "book" },
        { label: "Crafting Interpreters — Resolving and Binding", url: "https://craftinginterpreters.com/resolving-and-binding.html", type: "book" },
      ],
      connections: [
        { to: "comp-types", note: "Type checking generalizes into full type inference and type systems" },
        { to: "comp-frontend", note: "Semantic analysis operates on the AST produced by the frontend" },
      ],
    },
  ],
});
